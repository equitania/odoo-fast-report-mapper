---
phase: "04-release-preparation"
plan: "04"
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - README.md
  - CLAUDE.md
  - ~/.claude/skills/fr-mapper/SKILL.md
autonomous: true
requirements:
  - DOCS-02
  - DOCS-04
  - DOCS-05

must_haves:
  truths:
    - "D-04: README.md is updated in place — bilingual single-file structure (DE first, then EN) intact, contents refreshed for v1.0"
    - "D-05: README.md Architecture section shows single-package layout with zero odoo_report_helper references"
    - "README.md prominently links to MIGRATION.md in both language halves (DOCS-02)"
    - "README.md Development section mypy command targets odoo_fast_report_mapper/ only"
    - "CLAUDE.md File Structure block shows single-package layout"
    - "SKILL.md version is 1.0.0 and shows no odoo_report_helper/ subtree"
  artifacts:
    - path: "README.md"
      provides: "Updated bilingual user documentation"
      contains: "single-package layout"
    - path: "CLAUDE.md"
      provides: "Updated developer context file"
      contains: "_connection.py"
    - path: "~/.claude/skills/fr-mapper/SKILL.md"
      provides: "Updated Claude skill file"
      contains: "1.0.0"
  key_links:
    - from: "README.md Architecture block"
      to: "odoo_fast_report_mapper/ directory"
      via: "file tree listing"
      pattern: "odoo_fast_report_mapper/"
---

<objective>
Update README.md, CLAUDE.md (project), and ~/.claude/skills/fr-mapper/SKILL.md to reflect the v1.0 single-package layout — remove all active references to odoo_report_helper, update Architecture sections, correct the mypy command, and bump the version shown in SKILL.md.

Purpose: All developer-facing and user-facing documentation must be accurate for v1.0. These three files currently describe a two-package layout that was eliminated in Phase 1.

Output: README.md Architecture section updated, mypy command fixed; CLAUDE.md File Structure block updated; SKILL.md version 1.0.0 + single-package structure + v1.0.0 version history entry.

Note: SKILL.md is at ~/.claude/skills/fr-mapper/SKILL.md — it lives outside the git repo and is NOT committed to git. It is updated by the executor as a file write only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-release-preparation/04-CONTEXT.md
@.planning/phases/04-release-preparation/04-PATTERNS.md
@.planning/phases/04-release-preparation/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update README.md — architecture block, mypy command, test badge, MIGRATION.md link</name>
  <files>README.md</files>
  <read_first>
    - README.md (lines 1–10 for header; lines 305–375 for Architecture section and Development section — these contain all changes needed)
    - .planning/phases/04-release-preparation/04-PATTERNS.md (section "README.md" — exact target Architecture block and mypy command line)
    - odoo_fast_report_mapper/ directory listing (confirm current file names in the package: _cli.py, _connection.py, _report.py, _utils.py, eq_odoo_connection.py, eq_report.py, eq_utils.py, lang_utils.py, logging_config.py, progress.py, __version__.py, exceptions.py, _lang_utils.py, _logging.py, _yaml_dumper.py, _progress.py, _odoo_types.py)
  </read_first>
  <action>
    Make four targeted changes to README.md:

    1. Architecture block (lines 310–328): Replace the entire fenced code block (from "```" before "odoo-fast-report-mapper/" through the closing "```") with the updated single-package tree from PATTERNS.md. The new tree shows only odoo_fast_report_mapper/ with its actual current files — no odoo_report_helper/ entry. Match the files visible in the actual directory: __version__.py, _cli.py, _connection.py, eq_odoo_connection.py, eq_report.py, eq_utils.py, lang_utils.py, logging_config.py, progress.py. Add a comment "(single-package layout as of v1.0)" on the main package line. Tests count in the comment on tests/ line: check with `uv run pytest --collect-only -q 2>/dev/null | tail -1` to get the current count.

    2. Development section → Testing & Quality → type checking line (line 363): change "mypy odoo_fast_report_mapper/ odoo_report_helper/" to "mypy odoo_fast_report_mapper/". Only the mypy line changes; all surrounding lines stay.

    3. Test badge (line 6): update the badge text from "344 passed" to the current test count. Run `uv run pytest --collect-only -q 2>/dev/null | tail -2` to find the current number before editing.

    4. MIGRATION.md link (DOCS-02: "MIGRATION.md prominent verlinkt"): add a prominent link to MIGRATION.md in BOTH language halves — in the German half a short "Upgrade auf v1.0" note near the introduction linking to [MIGRATION.md](MIGRATION.md) (German text uses proper quotation pairs „…" if quotes are needed), and in the English half an equivalent "Upgrading to v1.0" note linking to [MIGRATION.md](MIGRATION.md). Placement: directly below each half's introduction paragraph or as a dedicated "### Upgrade / Upgrading" subsection — executor's choice, but it must be visible near the top of each half, not buried in a footer. Note: MIGRATION.md is created in parallel by plan 04-03 (same wave) — the link target not existing yet at edit time is acceptable; plan 04-05 verifies the file exists.

    Do NOT change the bilingual structure, the Data Flow section, or any other sections. The Architecture section heading ("## Architecture") stays as-is. Changes apply to BOTH the German and English halves if the Architecture section is duplicated — check whether it appears once or twice and update all occurrences.
  </action>
  <verify>
    <automated>cd /Users/picard/gitbase/PyPi-Projects/odoo-fast-report-mapper && grep -c "odoo_report_helper" README.md | grep -q "^0$" && echo "No odoo_report_helper references" && grep -q "single-package layout" README.md && grep "mypy" README.md | grep -v "odoo_report_helper" | grep -q "odoo_fast_report_mapper/" && [ "$(grep -c 'MIGRATION.md' README.md)" -ge 2 ] && echo "README checks passed (incl. MIGRATION.md links)"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "odoo_report_helper" README.md returns 0
    - Architecture block contains "single-package layout as of v1.0" comment
    - Architecture block does NOT contain "odoo_report_helper/"
    - The mypy line in Development section: "mypy odoo_fast_report_mapper/" (no odoo_report_helper/ suffix)
    - README.md still has "## Deutsche Dokumentation" and "## English Documentation" (bilingual structure intact)
    - Test badge on line 6 shows a number ≥ 344 passed
    - grep -c "MIGRATION.md" README.md returns ≥ 2 (one link per language half)
  </acceptance_criteria>
  <done>README.md has no odoo_report_helper references; Architecture and mypy command reflect single-package v1.0 layout; MIGRATION.md is prominently linked in both language halves.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update CLAUDE.md (project) File Structure block</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (lines 100–145 for the File Structure section and Dependencies section that reference odoo_report_helper)
  </read_first>
  <action>
    In CLAUDE.md, update the File Structure fenced code block (lines 116–133 approx). Replace the current two-package tree that shows both odoo_fast_report_mapper/ and odoo_report_helper/ with the single-package tree matching the current codebase.

    New tree content (adapt from PATTERNS.md README Architecture block pattern):
    odoo-fast-report-mapper/
    ├── odoo_fast_report_mapper/          # Main package (single-package layout as of v1.0)
    │   ├── __version__.py               # Version source (pyproject.toml dynamic)
    │   ├── _cli.py                      # CLI entry point (Click)
    │   ├── _connection.py               # OdooConnection base class
    │   ├── eq_odoo_connection.py        # EqOdooConnection: mapping, testing, collecting
    │   ├── eq_report.py                 # EqReport objects & validation
    │   ├── eq_utils.py                  # YAML collection, .env config, conversions
    │   ├── lang_utils.py                # Language normalization & multi-lang utilities
    │   ├── logging_config.py            # Centralized logging with color + rotation
    │   └── progress.py                  # tqdm-based progress tracking
    ├── tests/                           # Unit tests
    ├── yaml_examples/                   # Configuration templates
    └── pyproject.toml                   # Package configuration & build system

    Note: yaml_examples/ no longer has a connection_yaml/ subdirectory (superseded by .env-based config) — remove that sub-entry if present.

    Also update the Dependencies section if it lists odoo_report_helper as a source file path. The Python Requirements line "Python >= 3.8" should be updated to "Python >= 3.12" (matching current pyproject.toml requires-python).

    Do NOT change any other sections of CLAUDE.md (development commands, running the tool, testing, configuration examples).
  </action>
  <verify>
    <automated>cd /Users/picard/gitbase/PyPi-Projects/odoo-fast-report-mapper && grep -c "odoo_report_helper" CLAUDE.md | grep -q "^0$" && echo "CLAUDE.md clean"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "odoo_report_helper" CLAUDE.md returns 0
    - File Structure block shows "single-package layout as of v1.0" comment
    - File Structure block does NOT show a connection_yaml/ subdirectory under yaml_examples/
    - Python Requirements states "Python >= 3.12"
    - All other CLAUDE.md sections (commands, running the tool, configuration examples) are unchanged
  </acceptance_criteria>
  <done>CLAUDE.md File Structure block and Python requirements updated to v1.0 single-package layout.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update ~/.claude/skills/fr-mapper/SKILL.md for v1.0</name>
  <files>~/.claude/skills/fr-mapper/SKILL.md</files>
  <read_first>
    - ~/.claude/skills/fr-mapper/SKILL.md (full file — especially: header description line with version, Project Structure section, Architecture/Class Hierarchy section, Version History section, Development Commands mypy line)
    - .planning/phases/04-release-preparation/04-PATTERNS.md (section "~/.claude/skills/fr-mapper/SKILL.md" — exact 5 changes listed: version, project structure, version history, architecture/class hierarchy, mypy command)
  </read_first>
  <action>
    Update ~/.claude/skills/fr-mapper/SKILL.md (absolute path: /Users/picard/.claude/skills/fr-mapper/SKILL.md). This file is NOT in the git repo — no git commit is needed or possible for this file.

    Make these 5 targeted changes (per PATTERNS.md DOCS-04 section):

    1. Version in header description (line ~11): change "v0.9.7" to "v1.0.0".

    2. Project Structure section: remove the odoo_report_helper/ subtree block entirely (the 4–5 lines showing odoo_report_helper/ and its sub-files). Update __version__.py comment to "# Version: 1.0.0". The tree should only show odoo_fast_report_mapper/ as the single package.

    3. Version History section: prepend a new v1.0.0 entry. The entry must summarize the same breaking changes as RELEASE_NOTES.md: single-package layout, mypy strict, RPC fix, breaking import changes. Format consistent with existing entries in that section.

    4. Architecture / Class Hierarchy section: any reference to "OdooConnection (odoo_report_helper)" as a parent class should be updated to show "OdooConnection" from "odoo_fast_report_mapper._connection". Remove the "(odoo_report_helper)" qualifier.

    5. Development Commands mypy line: change "mypy odoo_fast_report_mapper/ odoo_report_helper/" to "mypy odoo_fast_report_mapper/".

    Do NOT change the overall SKILL.md structure, skill hierarchy tables, usage examples, or any other sections beyond the 5 items above.
  </action>
  <verify>
    <automated>grep -c "odoo_report_helper" /Users/picard/.claude/skills/fr-mapper/SKILL.md | grep -q "^0$" && echo "SKILL.md clean" && grep -q "1.0.0" /Users/picard/.claude/skills/fr-mapper/SKILL.md && echo "Version 1.0.0 present"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "odoo_report_helper" /Users/picard/.claude/skills/fr-mapper/SKILL.md returns 0
    - File contains "v1.0.0" in the header description line
    - Version History section contains an entry for v1.0.0
    - Project Structure section shows only odoo_fast_report_mapper/ (no odoo_report_helper/ subtree)
    - mypy line in Development Commands: "mypy odoo_fast_report_mapper/" (no odoo_report_helper/ suffix)
    - File is valid UTF-8 and retains its overall structure
  </acceptance_criteria>
  <done>SKILL.md updated: version 1.0.0, single-package structure, v1.0.0 version history entry, corrected mypy command.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README.md → PyPI description | README.md is used as the package description on PyPI — must not contain internal paths or credentials |
| SKILL.md → Claude agent context | SKILL.md is injected into Claude's context for future sessions — stale architecture descriptions cause wrong agent behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Information Disclosure | README.md on PyPI | accept | Architecture section shows only directory structure and file roles — no credentials, no internal URLs |
| T-04-08 | Tampering | SKILL.md stale data | mitigate | All five SKILL.md changes remove references to odoo_report_helper which no longer exists — stale paths would cause wrong import advice in future Claude sessions |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
1. `grep -c "odoo_report_helper" README.md` returns 0
2. `grep -c "odoo_report_helper" CLAUDE.md` returns 0
3. `grep -c "odoo_report_helper" /Users/picard/.claude/skills/fr-mapper/SKILL.md` returns 0
4. `grep "single-package layout" README.md` returns the Architecture block comment
5. `grep "1.0.0" /Users/picard/.claude/skills/fr-mapper/SKILL.md` returns the header + version history lines
6. `grep "mypy" README.md | grep "odoo_report_helper"` returns no output
7. `grep -c "MIGRATION.md" README.md` returns ≥ 2 (link present in both language halves)
</verification>

<success_criteria>
- README.md, CLAUDE.md, SKILL.md: zero remaining references to odoo_report_helper
- README.md Architecture block: single-package tree
- CLAUDE.md File Structure: single-package tree, Python >= 3.12
- SKILL.md: version 1.0.0, v1.0.0 version history, single-package structure, corrected mypy command
- SKILL.md is not committed to git (it lives at ~/.claude/skills/fr-mapper/ outside the repo)
</success_criteria>

<output>
Create `.planning/phases/04-release-preparation/04-04-SUMMARY.md` when done
</output>
