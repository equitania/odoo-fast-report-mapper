---
phase: "04-release-preparation"
plan: "03"
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - MIGRATION.md
  - RELEASE_NOTES.md
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02
  - DOCS-03

must_haves:
  truths:
    - "MIGRATION.md exists at repo root with bilingual DE/EN structure"
    - "MIGRATION.md covers all import-path changes, removed classes, and Phase 1.1 behavior changes"
    - "RELEASE_NOTES.md has a v1.0.0 entry with Breaking Changes section at top"
    - "MIGRATION.md is included in the sdist (MANIFEST.in already updated in Plan 01)"
  artifacts:
    - path: "MIGRATION.md"
      provides: "Bilingual migration guide for upgraders"
      contains: "odoo_report_helper"
    - path: "RELEASE_NOTES.md"
      provides: "Changelog with v1.0.0 at top"
      contains: "## Version 1.0.0 (12.06.2026)"
  key_links:
    - from: "RELEASE_NOTES.md v1.0 entry"
      to: "MIGRATION.md"
      via: "See MIGRATION.md link"
      pattern: "MIGRATION.md"
---

<objective>
Create MIGRATION.md (new bilingual DE/EN file) and prepend the v1.0.0 entry to RELEASE_NOTES.md.

Purpose: Users upgrading from pre-1.0 versions need a concrete guide to update their import paths and understand behavior changes. The RELEASE_NOTES entry completes the changelog and links to MIGRATION.md.

Output: MIGRATION.md at repo root covering import-path mapping, removed classes (ProgressBar, ReportProgress, create_progress_bar), Phase 1.1 behavior changes (ValueError, api_key precedence), tqdm direct-usage example, mypy-strict implications. RELEASE_NOTES.md with v1.0.0 as the newest entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-release-preparation/04-CONTEXT.md
@.planning/phases/04-release-preparation/04-PATTERNS.md
@.planning/phases/04-release-preparation/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create MIGRATION.md — bilingual DE/EN migration guide</name>
  <files>MIGRATION.md</files>
  <read_first>
    - README.md (lines 1–11 for the bilingual header pattern: Language/Sprache nav, German first, English second)
    - RELEASE_NOTES.md (lines 1–10 to see what breaking changes are already summarized there — avoid duplication beyond what belongs in a reference guide)
    - .planning/phases/04-release-preparation/04-PATTERNS.md (section "MIGRATION.md (doc, new file)" for exact content scope from D-02)
    - .planning/phases/01.1-correctness-bug-fixes-resolve-data-corruption-and-crash-bugs/01.1-CONTEXT.md (Phase 1.1 behavior changes: ValueError on empty name_dict, api_key precedence)
    - odoo_fast_report_mapper/progress.py (confirm progress_bar() function still exists so tqdm-direct-usage pattern is accurate)
  </read_first>
  <action>
    Create a new file MIGRATION.md at the repo root. Use the bilingual DE-first then EN structure matching README.md: Language/Sprache navigation header → "## Deutsche Dokumentation" section → horizontal rule → "## English Documentation" section.

    German typography rules: all German quotation marks as „…" pairs (U+201E opening, U+201C closing), never ASCII "…".

    Content for both language sections must cover ALL of the following, in this section order:
    1. Breaking Changes (imports) — complete import-path mapping table from odoo_report_helper.X to odoo_fast_report_mapper.X equivalents. Key mappings: OdooConnection (odoo_report_helper.odoo_connection → odoo_fast_report_mapper._connection), Report (odoo_report_helper.report → odoo_fast_report_mapper._report), PathDoesNotExistError (odoo_report_helper.exceptions → odoo_fast_report_mapper.exceptions), parse_yaml_folder (odoo_report_helper.utils → odoo_fast_report_mapper.eq_utils).
    2. Removed Classes — before/after snippets for ProgressBar, ReportProgress, create_progress_bar. The replacement pattern is tqdm direct usage: "from tqdm import tqdm". Note that progress_bar() wrapper in odoo_fast_report_mapper.progress still exists for internal use but the three public classes are removed.
    3. Behavior Changes (Phase 1.1) — two documented changes: (a) build_name_search_domain({}) now raises ValueError instead of silently producing an unbounded search; (b) api_key in YAML/env takes precedence over password silently (no warning when both are set).
    4. Mypy Strict Implications — if a downstream consumer type-checks against this library using mypy --strict, the import paths change and the removed-class type stubs are gone. Brief note on expected errors to fix.
    5. Quick Migration Checklist — bullet list of 4–5 concrete steps.

    File begins with: # Migration Guide — odoo-fast-report-mapper v0.9.x → v1.0

    The MANIFEST.in already includes MIGRATION.md (done in Plan 01), so this file will ship in the sdist automatically.
  </action>
  <verify>
    <automated>cd /Users/picard/gitbase/PyPi-Projects/odoo-fast-report-mapper && test -f MIGRATION.md && grep -q "odoo_report_helper" MIGRATION.md && grep -q "ProgressBar" MIGRATION.md && grep -q "ValueError" MIGRATION.md && grep -q "api_key" MIGRATION.md && grep -q "tqdm" MIGRATION.md && echo "MIGRATION.md content checks passed"</automated>
  </verify>
  <acceptance_criteria>
    - MIGRATION.md exists at repo root
    - Contains "## Deutsche Dokumentation" and "## English Documentation" sections
    - Contains a Language/Sprache navigation link at the top (matching README.md header pattern)
    - All German text uses „…" quotation pairs (no ASCII "…" as closing quote in German prose)
    - Contains import-path mapping for: OdooConnection, Report, PathDoesNotExistError, parse_yaml_folder
    - Contains before/after snippets for ProgressBar, ReportProgress, create_progress_bar removal
    - Contains "from tqdm import tqdm" as the replacement pattern for ProgressBar usage
    - Contains behavior change notes: ValueError for empty name_dict, api_key precedence
    - Contains Mypy Strict Implications section
    - Contains a Quick Migration Checklist section
    - grep -q "odoo_report_helper" MIGRATION.md exits 0 (the old paths are referenced for migration instructions)
  </acceptance_criteria>
  <done>MIGRATION.md created at repo root with bilingual DE/EN content covering all import changes, removed classes, and behavior changes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Prepend v1.0.0 entry to RELEASE_NOTES.md</name>
  <files>RELEASE_NOTES.md</files>
  <read_first>
    - RELEASE_NOTES.md (lines 1–10 — current header format "## Version X.Y.Z (DD.MM.YYYY)" and existing newest entry to insert before)
    - .planning/phases/04-release-preparation/04-PATTERNS.md (section "RELEASE_NOTES.md" — exact entry content from PATTERNS.md including all subsections)
  </read_first>
  <action>
    Prepend the v1.0.0 changelog entry to RELEASE_NOTES.md between line 2 (blank line after "# Release Notes") and the existing "## Version 0.9.7.3" entry.

    The new entry uses the exact header format: "## Version 1.0.0 (12.06.2026)" (today's date is 12.06.2026 per environment).

    Subsection order (per DOCS-03: Breaking Changes must be first):
    - ### Breaking Changes
    - ### Added
    - ### Changed
    - ### Fixed (if applicable — Phase 1.1 fixes are documented here briefly with a note to see git history)
    - ### Removed

    Content for each subsection as documented in PATTERNS.md "RELEASE_NOTES.md" section. Key items:
    Breaking Changes: removal of odoo_report_helper package, all API now in odoo_fast_report_mapper, import-path change examples, reference to MIGRATION.md.
    Added: MIGRATION.md bilingual migration guide, Migration Guide link in pyproject.toml [project.urls].
    Changed: version 1.0.0 (clean SemVer), Development Status 5 - Production/Stable, CI matrix extended to Python 3.14, mypy CI step now blocking with --strict, CI perf gate blocking.
    Removed: Legacy pre-GSD planning files (IMPROVEMENT_PLAN.md, REVIEW.md, TASK_TRACKING.md), CLAUDE.md removed from sdist.

    Bullet format: dash + space, no trailing period, consistent with existing entries.
    All text in RELEASE_NOTES is English (existing format is English).
  </action>
  <verify>
    <automated>cd /Users/picard/gitbase/PyPi-Projects/odoo-fast-report-mapper && head -5 RELEASE_NOTES.md | grep -q "Version 1.0.0" && grep -q "Breaking Changes" RELEASE_NOTES.md && grep -q "0.9.7.3" RELEASE_NOTES.md && echo "RELEASE_NOTES structure OK"</automated>
  </verify>
  <acceptance_criteria>
    - RELEASE_NOTES.md first version entry (after "# Release Notes" header): "## Version 1.0.0 (12.06.2026)"
    - "### Breaking Changes" subsection appears before "### Added" in the v1.0.0 entry
    - v1.0.0 entry contains a reference to MIGRATION.md
    - Existing entries (0.9.7.3, 0.9.7.2, 0.9.7.1, 0.9.7) are still present below the new entry
    - "# Release Notes" header on line 1 is unchanged
  </acceptance_criteria>
  <done>RELEASE_NOTES.md has v1.0.0 as the newest entry with Breaking Changes section at top, linking to MIGRATION.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MIGRATION.md → PyPI sdist | This file ships to PyPI users — must not contain internal paths, credentials, or confidential info |
| MIGRATION.md code snippets | Before/after code examples must use placeholder values — no real credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Information Disclosure | MIGRATION.md code snippets | mitigate | All example code uses placeholder values (e.g. "your_api_key", "your_password"); no real credentials appear in doc |
| T-04-06 | Information Disclosure | RELEASE_NOTES.md | accept | Changelog is public-facing; content is factual feature descriptions with no sensitive information |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
1. `test -f MIGRATION.md` exits 0
2. `grep -c "Deutsche Dokumentation" MIGRATION.md` returns 1
3. `grep -c "English Documentation" MIGRATION.md` returns 1
4. `grep "ProgressBar" MIGRATION.md` returns multiple lines (class is documented in both sections)
5. `head -5 RELEASE_NOTES.md | grep "Version 1.0.0"` returns the v1.0.0 header
6. `grep "Breaking Changes" RELEASE_NOTES.md` returns the subsection header
7. `grep "0.9.7.3" RELEASE_NOTES.md` returns the existing entry (not removed)
</verification>

<success_criteria>
- MIGRATION.md exists: bilingual, covers import changes + removed classes + behavior changes + mypy implications + checklist
- RELEASE_NOTES.md: v1.0.0 entry at top, Breaking Changes first, references MIGRATION.md
- All existing RELEASE_NOTES entries preserved below the new entry
</success_criteria>

<output>
Create `.planning/phases/04-release-preparation/04-03-SUMMARY.md` when done
</output>
