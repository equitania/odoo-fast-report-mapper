---
phase: 03-performance
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/test_benchmark_rpc.py
autonomous: true
requirements:
  - PERF-01
  - PERF-04

must_haves:
  truths:
    - "tests/test_benchmark_rpc.py exists and is collected by pytest"
    - "A 10-report × 50-field collect run via mocks executes against the CURRENT (unfixed) _connection.py"
    - "The test logs IR_MODEL.search and IR_FIELDS.search call counts but does NOT assert on them (no threshold yet)"
    - "The RPC_CEILING constant is present at the top of the file (set to PLACEHOLDER sentinel until Wave 2)"
    - "The test passes on the current codebase (no assertion failure, just measurement)"
  artifacts:
    - path: "tests/test_benchmark_rpc.py"
      provides: "Mock-Counter benchmark infrastructure and baseline measurement"
      min_lines: 80
  key_links:
    - from: "tests/test_benchmark_rpc.py"
      to: "odoo_fast_report_mapper/_connection.py"
      via: "import OdooConnection; call collect_report_entries via mocks"
      pattern: "from odoo_fast_report_mapper._connection import OdooConnection"
---

<objective>
Create tests/test_benchmark_rpc.py with a 10-report × 50-field mock-driven collect run.
This Wave 1 plan measures the CURRENT call counts for IR_MODEL.search and IR_FIELDS.search
inside add_field_to_dictionary without asserting on them — establishing the baseline for
PERF-04 and the infrastructure for PERF-01 and PERF-03.

Purpose: The exact RPC ceiling (PERF-04) cannot be pre-determined; the baseline measurement
must run before any production code change. This plan produces the infrastructure and
documents the BEFORE state. Wave 2 (03-02) converts the measurement into an asserting
regression test after the fix is applied.

Output: tests/test_benchmark_rpc.py with test_rpc_baseline_call_count (no assertion on
inner search counts) and the RPC_CEILING placeholder constant.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-performance/03-RESEARCH.md
@.planning/phases/03-performance/03-PATTERNS.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Create test_benchmark_rpc.py with 10×50 mock infrastructure and baseline measurement</name>
  <files>tests/test_benchmark_rpc.py</files>

  <read_first>
    - tests/test_connection.py lines 1–67 (copyright header, imports, _make_connection, _setup_env helpers — copy verbatim)
    - tests/test_connection.py lines 1094–1165 (TestCollectReportEntries.test_writes_yaml_files_to_output_path — canonical field-object mock setup)
    - tests/test_connection.py lines 1199–1233 (test_dependencies_only_contain_field_specific_modules — ir.module.module guard pattern)
    - odoo_fast_report_mapper/_connection.py lines 583–731 (collect_report_entries + add_field_to_dictionary — what is being measured)
    - .planning/phases/03-performance/03-RESEARCH.md §Pattern 2 and §Code Examples (baseline measurement approach)
    - .planning/phases/03-performance/03-PATTERNS.md (all patterns — field-object mock, report-object mock, env_map wiring, SearchCallCounter)
  </read_first>

  <behavior>
    - test_rpc_baseline_call_count(tmp_path): runs collect_report_entries with 10 report mocks, each with 50 field objects. Uses two separate MagicMock instances: mock_ir_model (used only inside add_field_to_dictionary for IR_MODEL.search) and mock_ir_model_fields (used both for outer search and per-field browse). After the run, prints call counts with print() — no assert on call counts. The test passes on the current (unfixed) codebase.
    - test_add_field_to_dictionary_calls_ir_model_search_currently(tmp_path): direct call to add_field_to_dictionary for a single field, asserts mock_ir_model.search.call_count == 1 and mock_ir_fields.search.call_count == 1 to document the current (pre-fix) behavior. This test documents BEFORE state explicitly.
    - RPC_CEILING constant: integer, placed after imports, set to -1 (sentinel meaning "not yet measured"). A comment explains it will be updated in Wave 2 (plan 03-02) after the fix is applied.
  </behavior>

  <action>
    Create tests/test_benchmark_rpc.py. Structure:

    1. Copyright header (same text as tests/test_connection.py line 1–2).
    2. Module docstring: "RPC call-count benchmark for collect_report_entries. PERF-01, PERF-03, PERF-04."
    3. Imports: copy exactly from test_connection.py lines 6–13 (urllib.error, unittest.mock.MagicMock/patch, pytest, OdooConnection). Add import for os.
    4. RPC_CEILING: int = -1 constant with comment: "# PLACEHOLDER — set to measured after-fix value in plan 03-02. -1 means not yet established."
    5. Copy _make_connection helper verbatim from test_connection.py lines 22–53.
    6. Copy _setup_env helper verbatim from test_connection.py lines 61–67.
    7. Helper function _build_field_mock(report_ids, model_name, field_name, modules_str) that returns a MagicMock with .eq_report_ids.ids, .model_id.model, .name, .modules set from the parameters.
    8. Helper function _build_report_mock(report_name, model_name) that sets all required MagicMock attributes for an ir.actions.report browse result (copy from PATTERNS.md report-object mock pattern, attributes: report_name, report_type="fast_report", name, model, eq_export_type="pdf", eq_ignore_images=True, eq_handling_html_fields="standard", multi=False, attachment_use=False, attachment, print_report_name, eq_calculated_field_ids=[], eq_print_button=False, eq_multiprint="standard", company_id with .id=False and __bool__ returning False, with_context returning itself).
    9. test_rpc_baseline_call_count(tmp_path):
       - Creates conn = _make_connection().
       - Sets conn.connection.env.user.company_ids.ids = [1], __bool__ = True.
       - Creates 10 report mocks (report IDs 10..19) using _build_report_mock.
       - Creates a single mock_ir_report where .search.return_value = list(range(10, 20)) and .browse side_effect returns the correct mock per report_id.
       - Creates 50 field mocks per report (500 total field IDs 1000..1499 spread across reports, 50 each). Each field_mock.eq_report_ids.ids = [report_id for that field]. model names cycle over ["sale.order", "account.move", "res.partner", "stock.picking", "purchase.order"]. modules cycles over ["sale", "account", "base", "", "sale, account"].
       - mock_ir_model_fields: .search.return_value = list(range(1000, 1500)); .browse side_effect returns the field mock for the given field_id.
       - mock_ir_model (used inside add_field_to_dictionary): .search.return_value = [1] (non-empty, so the inner search proceeds and is countable).
       - mock_ir_fields_inner (second ir.model.fields proxy for the inner search inside add_field_to_dictionary): .search.return_value = [10]; .browse.return_value sets a MagicMock with .modules = "sale".
       - NOTE: Because both outer and inner paths use the same "ir.model.fields" env key, use the env_map mock approach from PATTERNS.md — wire mock_ir_model_fields for the outer key and let the inner calls also go to mock_ir_model_fields (the call counts will include both outer and inner; separate counting by tracking call args or accepting total count). Use total call_count and document what each component contributes in print() output.
       - Calls conn.get_installed_languages = MagicMock(return_value=[{"code": "de_DE", "iso_code": "de", "name": "German"}]).
       - Calls conn.collect_report_entries(str(output_dir)).
       - Prints: "BASELINE ir.model.search calls: {mock_ir_model.search.call_count}" and "BASELINE ir.model.fields.search total calls: {mock_ir_model_fields.search.call_count}".
       - No assertion on call counts (measurement only). Add pytest.skip-free pass: just let the test complete.
    10. test_add_field_to_dictionary_calls_ir_model_search_currently():
        - Creates conn = _make_connection().
        - Wires mock_ir_model.search.return_value = [1] and mock_ir_fields.search.return_value = [10] and mock_field_obj.modules = "sale" via _setup_env (copy _setup_field_env pattern from TestAddFieldToDictionary in PATTERNS.md).
        - Calls conn.add_field_to_dictionary({}, 100, "sale.order", "amount_total", False).
        - Asserts mock_ir_model.search.call_count == 1 (documents current BEFORE behavior).
        - Asserts mock_ir_fields.search.call_count == 1 (documents current BEFORE behavior).
        - Docstring: "BEFORE state: add_field_to_dictionary currently fires 2 RPC calls per field. This test documents the pre-fix behavior and will be DELETED in plan 03-02 once the fix is applied."

    All per RESEARCH.md pitfall guidance: preserve modules or "" guard awareness in mock setup; cover both company_id=False and company_id=int paths in the 500-field loop (alternate per field).
  </action>

  <verify>
    <automated>cd /Users/picard/gitbase/PyPi-Projects/odoo-fast-report-mapper && .venv/bin/python -m pytest tests/test_benchmark_rpc.py -v 2>&amp;1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - tests/test_benchmark_rpc.py exists and is importable.
    - pytest --collect-only shows test_rpc_baseline_call_count and test_add_field_to_dictionary_calls_ir_model_search_currently.
    - Both tests PASS on the current (unfixed) codebase.
    - test_add_field_to_dictionary_calls_ir_model_search_currently asserts call_count == 1 for both ir_model.search and ir_fields.search.
    - test_rpc_baseline_call_count prints BASELINE lines to stdout (visible with pytest -s).
    - RPC_CEILING: int = -1 is present in the file (grep RPC_CEILING tests/test_benchmark_rpc.py shows a hit).
    - Full suite still passes: .venv/bin/python -m pytest tests/ -q -m "not integration" exits 0.
    - mypy still clean: .venv/bin/python -m mypy odoo_fast_report_mapper/ reports 0 errors (no production code changes in this plan).
  </acceptance_criteria>

  <done>
    tests/test_benchmark_rpc.py exists, both benchmark tests pass on the current codebase, the BEFORE call-count is documented in test_add_field_to_dictionary_calls_ir_model_search_currently assertions (1 IR_MODEL.search + 1 IR_FIELDS.search per field), and RPC_CEILING = -1 placeholder is in place. Full test suite green. mypy 0 errors.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code → production code | Test imports and calls OdooConnection methods; no new trust boundary introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | tests/test_benchmark_rpc.py (new test file) | accept | Test-only file; no production data path; no new attack surface introduced |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan; Standard Stack section of RESEARCH.md confirms all deps already present |

**Security note:** This plan adds a test file only. No new attack surface is introduced. No user input is processed, no authentication logic is touched, no data is written to disk except pytest tmp_path. ASVS L1 compliance is unaffected. The threat register entries are accept-disposition because there is nothing to mitigate.
</threat_model>

<verification>
## Phase Gate (Wave 1)

```bash
cd /Users/picard/gitbase/PyPi-Projects/odoo-fast-report-mapper

# Both new tests pass
.venv/bin/python -m pytest tests/test_benchmark_rpc.py -v

# Full suite still green (347 existing tests + new ones)
.venv/bin/python -m pytest tests/ -q -m "not integration"

# mypy still 0 errors (no production code touched)
.venv/bin/python -m mypy odoo_fast_report_mapper/

# RPC_CEILING placeholder present
grep -n "RPC_CEILING" tests/test_benchmark_rpc.py
```
</verification>

<success_criteria>
- tests/test_benchmark_rpc.py collected by pytest.
- test_add_field_to_dictionary_calls_ir_model_search_currently passes, documenting 1+1 search calls per field as BEFORE state.
- test_rpc_baseline_call_count passes (no assertion on counts, measurement only).
- RPC_CEILING = -1 present as sentinel.
- All 347+ existing tests still pass.
- mypy odoo_fast_report_mapper/ reports 0 errors.
</success_criteria>

<output>
Create `.planning/phases/03-performance/03-01-SUMMARY.md` when done.
</output>
