---
phase: 01.1-correctness-bug-fixes-resolve-data-corruption-and-crash-bugs
audited: 2026-05-29T00:00:00Z
asvs_level: 1
block_on: high
threats_total: 13
threats_closed: 13
threats_open: 0
register_authored_at_plan_time: true
---

# SECURITY.md — odoo-fast-report-mapper

## Phase 1.1 — Correctness Bug Fixes (Security Audit)

**Audited:** 2026-05-29
**ASVS Level:** 1
**Threats Closed:** 13/13
**Open Threats:** 0

---

## Threat Verification

| Threat ID | Category | Disposition | Evidence |
|-----------|----------|-------------|----------|
| T-01.1-01 | Denial of Service | mitigate | `_connection.py:665` — `if not model_id:` guard before `model_id[0]` access; returns early with warning |
| T-01.1-02 | Tampering | mitigate | `_report.py:141-144` — `add_calculated_fields` does not call `self_clean()`; comment documents the intentional omission |
| T-01.1-03a | Tampering | mitigate | `_connection.py:203` — `check_dependencies` annotated `-> tuple[bool, list[str]]`; caller at line 289 unpacks both values |
| T-01.1-03b | Elevation of Privilege | mitigate | `_connection.py:290-294` — `if not dependencies_installed and not_installed_modules: return None, False`; map_reports line 241-243 checks `if not ok:` and appends to `failed_reports`, skipping creation |
| T-01.1-04a | Tampering | mitigate | `_lang_utils.py:82-83` — `if not name_dict: raise ValueError(...)` at start of `build_name_search_domain` |
| T-01.1-04b | Information Disclosure | mitigate | `_lang_utils.py:82-83` — same guard prevents empty-dict path that would produce an unbounded all-reports `ir.actions.report` search |
| T-01.1-05 | Spoofing | mitigate | `_utils.py:76,95-100,116` — `urlparse(url).hostname` extracts bare hostname; passed directly to `ODOO(hostname, ...)` stripping scheme, path, credentials, and trailing backslashes |
| T-01.1-06 | Tampering | mitigate | `_report.py:32-35` — `if not isinstance(entry_name, dict) or not entry_name: raise TypeError(...)` in `Report.__init__` |
| T-01.1-07a | Spoofing | mitigate | `_utils.py:282-299` (YAML path) and `_utils.py:449-515` (env path) — `api_key` resolved; `auth_method='api_key'` passed to `OdooConnection.__init__` which stores it in `self.auth_method` |
| T-01.1-07b | Elevation of Privilege | accept | Accepted: OdooRPC / odoorpc-toolbox library is trusted to implement the API-key authentication flow correctly. No new packages installed. |
| T-01.1-08a | Denial of Service | mitigate | `_connection.py:669-671` — `if not field_id:` guard before `IR_FIELDS.browse(field_id)`; returns early with debug log |
| T-01.1-08b | Information Disclosure | mitigate | `_connection.py:674` — `modules_dependencies = [m for m in raw_modules if m]` filters empty strings; line 675 short-circuits if list is empty |
| T-01.1-SC | Tampering | accept | Accepted: No new packages were installed as part of this phase. Supply-chain risk surface unchanged. |

---

## Accepted Risks Log

| Risk ID | Rationale |
|---------|-----------|
| T-01.1-07b | odoorpc-toolbox is a declared project dependency (pyproject.toml). API-key auth implementation is the library's responsibility. Odoo >= 14 version enforcement is present at `_connection.py:120-131` via `check_api_key_compatibility()`. |
| T-01.1-SC | Phase 1.1 introduced no new dependencies. pyproject.toml is the single source of truth; no requirements.txt exists. |

---

## Unregistered Flags

None. No new attack surface was identified during this audit that lacked a threat mapping.

---

## Audit Trail

### Security Audit 2026-05-29

| Metric | Count |
|--------|-------|
| Threats found | 13 |
| Closed | 13 |
| Open | 0 |

- All 9 bug fixes (BUG-01..09) declared in the Phase 1.1 threat register have corresponding mitigations verified by direct source grep.
- The `_create_or_update_report` / `map_reports` call chain (T-01.1-03b) was confirmed end-to-end: `check_dependencies` returns a tuple, the caller unpacks it, the `if not ok:` check routes to `failed_reports`, and `IR_ACTIONS_REPORT.create()` is never reached for reports with missing dependencies.
- `prepare_connection` (T-01.1-05) uses `parsed.hostname` not `parsed.netloc`, which strips embedded credentials (user:pass@host patterns) automatically.
- Implementation files were not modified during this audit.
