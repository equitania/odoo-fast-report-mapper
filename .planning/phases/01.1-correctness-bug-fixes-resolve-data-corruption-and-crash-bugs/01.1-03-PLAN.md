---
phase: 01.1-correctness-bug-fixes-resolve-data-corruption-and-crash-bugs
plan: 03
type: execute
wave: 2
depends_on:
  - 01.1-01
files_modified:
  - odoo_fast_report_mapper/_connection.py
  - tests/test_connection.py
autonomous: true
requirements:
  - BUG-03

must_haves:
  truths:
    - "check_dependencies() has explicit return annotation -> tuple[bool, list[str]]"
    - "A regression test proves that map_reports() skips a report and does NOT call IR_ACTIONS_REPORT.create() when a dependency is missing"
    - "The existing caller at _connection.py:289 still unpacks the tuple correctly"
    - "All existing TestCheckDependencies tests continue to pass"
  artifacts:
    - path: "odoo_fast_report_mapper/_connection.py"
      provides: "explicit tuple[bool, list[str]] return annotation on check_dependencies"
      contains: "-> tuple[bool, list[str]]"
    - path: "tests/test_connection.py"
      provides: "regression test for BUG-03 in TestCheckDependencies"
      contains: "test_map_reports_skips_report_when_dependency_missing"
  key_links:
    - from: "check_dependencies"
      to: "map_reports caller at _connection.py:289"
      via: "tuple unpack: dependencies_installed, not_installed_modules = self.check_dependencies(...)"
      pattern: "dependencies_installed, not_installed_modules"
---

<objective>
Fix BUG-03 (CR-04): check_dependencies() return contract is formalized with an explicit -> tuple[bool, list[str]] annotation. The base-class LSP-violation surface (bool(tuple) always-true bypass) was already eliminated by Phase 1 consolidation; this plan locks the contract statically and adds a regression test proving that a missing dependency actually blocks map_reports().

Purpose: Without the annotation, a future caller could accidentally unpack or re-use the return value as a bare bool, re-introducing the always-True bypass. The regression test makes this detectable at CI level.
Output: Annotated check_dependencies + regression test in TestCheckDependencies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/BASELINE-REVIEW.md
@.planning/phases/01.1-correctness-bug-fixes-resolve-data-corruption-and-crash-bugs/01.1-CONTEXT.md
@.planning/phases/01.1-correctness-bug-fixes-resolve-data-corruption-and-crash-bugs/01.1-PATTERNS.md
@.planning/phases/01.1-correctness-bug-fixes-resolve-data-corruption-and-crash-bugs/01.1-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit return annotation to check_dependencies</name>
  <files>odoo_fast_report_mapper/_connection.py</files>
  <read_first>
    - odoo_fast_report_mapper/_connection.py (lines 200-220 for check_dependencies signature and body; line 289 for caller tuple unpack)
    - .planning/BASELINE-REVIEW.md (CR-04, lines 150-179 — return-type mismatch, recommended fix Option B)
    - .planning/phases/01.1-correctness-bug-fixes-resolve-data-corruption-and-crash-bugs/01.1-PATTERNS.md (BUG-03 section — existing implementation, fix annotation, caller pattern)
    - .planning/phases/01.1-correctness-bug-fixes-resolve-data-corruption-and-crash-bugs/01.1-CONTEXT.md (BUG-03 discretion note — annotation only, no logic change; annotation policy: only BUG-03 and BUG-06 get annotations in Phase 1.1)
  </read_first>
  <action>
    In _connection.py, locate check_dependencies (around line 203). Change the method signature from "def check_dependencies(self, dependencies):" to "def check_dependencies(self, dependencies) -> tuple[bool, list[str]]:". No other changes to the method body or the caller at line 289. Python 3.12 supports "tuple[bool, list[str]]" natively without "from __future__ import annotations" — do not add that import (per D-Claude's Discretion annotation imports in CONTEXT.md).
  </action>
  <verify>
    <automated>cd /Users/picard/gitbase/PyPi-Projects/odoo-fast-report-mapper && grep -n "def check_dependencies" odoo_fast_report_mapper/_connection.py</automated>
  </verify>
  <done>check_dependencies signature contains "-> tuple[bool, list[str]]"; no logic changes; python -c "from odoo_fast_report_mapper._connection import OdooConnection" exits 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add map_reports dependency-blocking regression test</name>
  <files>tests/test_connection.py</files>
  <read_first>
    - tests/test_connection.py (lines 484-542 for existing TestCheckDependencies class; lines 550-634 for _make_map_reports_env and _make_simple_report helpers; lines 22-67 for _make_connection/_setup_env)
    - .planning/phases/01.1-correctness-bug-fixes-resolve-data-corruption-and-crash-bugs/01.1-PATTERNS.md (BUG-03 section — test_map_reports_skips_report_when_dependency_missing pattern, mock_ir_module.search.side_effect usage)
  </read_first>
  <behavior>
    - test_map_reports_skips_report_when_dependency_missing: mocks ir.module.module search to return non-empty for "sale" but empty for "account", calls map_reports([report]) with report having dependencies=["sale", "account"], asserts the returned failed list has exactly 1 entry, asserts ir.actions.report create() was never called
    - Test is added to the existing TestCheckDependencies class (not a new class)
    - Uses _make_connection(), _make_map_reports_env(), _make_simple_report() helpers already in the file
  </behavior>
  <action>
    Add test method test_map_reports_skips_report_when_dependency_missing to the existing TestCheckDependencies class in tests/test_connection.py. The test: (1) creates conn = _make_connection(), (2) calls env = _make_map_reports_env(conn), (3) creates mock_ir_module = MagicMock() and sets mock_ir_module.search.side_effect = [[1], []] so the first call ("sale") returns [1] and the second call ("account") returns [], (4) calls _setup_env(conn, {**env, "ir.module.module": mock_ir_module}), (5) creates report = _make_simple_report(dependencies=["sale", "account"]), (6) calls failed = conn.map_reports([report]), (7) asserts len(failed) == 1, (8) asserts env["ir.actions.report"].create.assert_not_called(). Inspect the existing test class and helper signatures carefully before writing — do not assume the exact fixture/helper API; read the actual implementations at the line ranges specified.
  </action>
  <verify>
    <automated>cd /Users/picard/gitbase/PyPi-Projects/odoo-fast-report-mapper && python -m pytest tests/test_connection.py::TestCheckDependencies::test_map_reports_skips_report_when_dependency_missing -v 2>&1 | tail -10</automated>
  </verify>
  <done>pytest tests/test_connection.py::TestCheckDependencies::test_map_reports_skips_report_when_dependency_missing exits 0; full TestCheckDependencies suite still passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| map_reports logic → check_dependencies return value | Caller must correctly unpack tuple; bare-bool usage would bypass dependency block |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.1-03a | Tampering | check_dependencies / map_reports | mitigate | BUG-03 (MEDIUM — integrity/validation-bypass concern): return annotation locks contract; regression test proves dependency-missing path actually blocks report creation |
| T-01.1-03b | Elevation of Privilege | map_reports | mitigate | Without the fix, a missing dependency could allow report mapping to proceed despite unmet module prerequisites, potentially writing incorrect report records to Odoo |
| T-01.1-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
grep -n "tuple\[bool, list\[str\]\]" odoo_fast_report_mapper/_connection.py shows annotation on check_dependencies
pytest tests/test_connection.py::TestCheckDependencies -v exits 0 (all tests including new one)
pytest tests/test_connection.py -v exits 0 (no regressions from plan 01)
</verification>

<success_criteria>
- check_dependencies signature: "-> tuple[bool, list[str]]" present in _connection.py
- Caller at _connection.py:289 unchanged (still unpacks correctly)
- Regression test test_map_reports_skips_report_when_dependency_missing passes
- ir.actions.report.create() is not called when a dependency is missing
- All pre-existing TestCheckDependencies and test_connection.py tests still pass
</success_criteria>

<output>
Create .planning/phases/01.1-correctness-bug-fixes-resolve-data-corruption-and-crash-bugs/01.1-03-SUMMARY.md when done
</output>
