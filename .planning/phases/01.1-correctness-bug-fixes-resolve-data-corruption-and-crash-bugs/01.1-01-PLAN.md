---
phase: 01.1-correctness-bug-fixes-resolve-data-corruption-and-crash-bugs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - odoo_fast_report_mapper/_connection.py
  - tests/test_connection.py
autonomous: true
requirements:
  - BUG-01

must_haves:
  truths:
    - "add_field_to_dictionary() returns data_dictionary unchanged (does not crash) when IR_MODEL.search() returns an empty list"
    - "A warning is logged with the model name when the guard fires"
    - "IR_FIELDS.search() is never called when the model guard fires"
    - "A regression test in TestAddFieldToDictionary covers the empty-search case and passes"
  artifacts:
    - path: "odoo_fast_report_mapper/_connection.py"
      provides: "empty-model guard in add_field_to_dictionary"
      contains: "if not model_id"
    - path: "tests/test_connection.py"
      provides: "regression test for BUG-01"
      contains: "test_empty_model_search_skips_and_returns_dict"
  key_links:
    - from: "add_field_to_dictionary"
      to: "logger.warning"
      via: "guard branch at _connection.py:664"
      pattern: "if not model_id"
---

<objective>
Fix BUG-01 (CR-02): add_field_to_dictionary() crashes with IndexError when IR_MODEL.search() returns an empty list because model_name is not found in Odoo. The fix adds a guard that logs a warning and returns data_dictionary early, preventing the crash and keeping the collect run alive.

Purpose: A missing model (e.g., after an Odoo module was uninstalled) must not abort the entire collect-run mid-iteration and leave data_dictionary in a corrupt partial state.
Output: Guarded _connection.py + regression test in test_connection.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/BASELINE-REVIEW.md
@.planning/phases/01.1-correctness-bug-fixes-resolve-data-corruption-and-crash-bugs/01.1-CONTEXT.md
@.planning/phases/01.1-correctness-bug-fixes-resolve-data-corruption-and-crash-bugs/01.1-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Guard add_field_to_dictionary against empty IR_MODEL.search() result</name>
  <files>odoo_fast_report_mapper/_connection.py</files>
  <read_first>
    - odoo_fast_report_mapper/_connection.py (lines 660-680 for add_field_to_dictionary body; line 28 for existing logger binding)
    - .planning/BASELINE-REVIEW.md (CR-02, lines 97-117 — exact file:line, current code, failure mode)
    - .planning/phases/01.1-correctness-bug-fixes-resolve-data-corruption-and-crash-bugs/01.1-PATTERNS.md (BUG-01 section — fix pattern and analog guard at _connection.py:187-189)
  </read_first>
  <behavior>
    - When IR_MODEL.search() returns [] for model_name, add_field_to_dictionary returns data_dictionary unchanged (no IndexError)
    - logger.warning is called with message containing the model_name and "not found — skipping dependency collection"
    - IR_FIELDS.search() is never called when model_id is empty
    - When IR_MODEL.search() returns a non-empty list, behavior is unchanged (field_id lookup proceeds normally)
  </behavior>
  <action>
    In add_field_to_dictionary (_connection.py), after the line that calls IR_MODEL.search([("model", "=", model_name)]) and assigns the result to model_id, insert a guard block: if not model_id, call logger.warning(f"Model '{model_name}' not found — skipping dependency collection") and return data_dictionary immediately. The guard must sit between the IR_MODEL.search() call and the IR_FIELDS.search() call that accesses model_id[0]. Use the module-level logger binding already defined at _connection.py:28 (get_logger(__name__)). Do not add any CLI output — per D-03, logging only. No other changes to the function.
  </action>
  <verify>
    <automated>cd /Users/picard/gitbase/PyPi-Projects/odoo-fast-report-mapper && python -m pytest tests/test_connection.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>_connection.py contains "if not model_id:" guard followed by logger.warning and "return data_dictionary" before IR_FIELDS.search(); all pre-existing test_connection.py tests still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add regression test for empty-model guard in TestAddFieldToDictionary</name>
  <files>tests/test_connection.py</files>
  <read_first>
    - tests/test_connection.py (lines 22-67 for _make_connection/_setup_env helpers; lines 484-542 for TestCheckDependencies class structure as analog)
    - .planning/phases/01.1-correctness-bug-fixes-resolve-data-corruption-and-crash-bugs/01.1-PATTERNS.md (BUG-01 section — TestAddFieldToDictionary regression test pattern with mock_ir_model.search.return_value = [])
    - tests/conftest.py (to see available fixtures)
  </read_first>
  <behavior>
    - test_empty_model_search_skips_and_returns_dict: mocks ir.model search to return [], calls add_field_to_dictionary with model_name="missing.model", asserts result == {} (input dict unchanged), asserts ir.model.fields search was never called
    - Test class is named TestAddFieldToDictionary
    - Test uses _make_connection() and _setup_env() helpers from the same file
  </behavior>
  <action>
    Add a new test class TestAddFieldToDictionary to tests/test_connection.py. Place it after the existing TestCheckDependencies class. The class contains one test method: test_empty_model_search_skips_and_returns_dict. The test: (1) creates conn = _make_connection(), (2) creates mock_ir_model = MagicMock() with mock_ir_model.search.return_value = [], (3) creates mock_ir_fields = MagicMock(), (4) calls _setup_env(conn, {"ir.model": mock_ir_model, "ir.model.fields": mock_ir_fields}), (5) calls result = conn.add_field_to_dictionary({}, report_id=1, model_name="missing.model", field_name="id", company_id=False), (6) asserts result == {}, (7) asserts mock_ir_fields.search.assert_not_called(). Import MagicMock at the top of the file if not already imported (check existing imports first). Match the existing test class/method naming and indentation conventions in the file.
  </action>
  <verify>
    <automated>cd /Users/picard/gitbase/PyPi-Projects/odoo-fast-report-mapper && python -m pytest tests/test_connection.py::TestAddFieldToDictionary::test_empty_model_search_skips_and_returns_dict -v 2>&1 | tail -10</automated>
  </verify>
  <done>pytest tests/test_connection.py::TestAddFieldToDictionary::test_empty_model_search_skips_and_returns_dict exits 0; full test_connection.py suite still passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OdooRPC response → add_field_to_dictionary | IR_MODEL.search() result may be empty (model not found in remote Odoo) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.1-01 | Denial of Service | add_field_to_dictionary collect loop | mitigate | BUG-01 fix: guard prevents IndexError crash that aborts collect mid-run and leaves data_dictionary corrupt |
| T-01.1-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
pytest tests/test_connection.py -v exits 0
grep -n "if not model_id" odoo_fast_report_mapper/_connection.py shows the guard line
grep -n "logger.warning" odoo_fast_report_mapper/_connection.py shows warning call in add_field_to_dictionary
pytest tests/test_connection.py::TestAddFieldToDictionary -v exits 0
</verification>

<success_criteria>
- add_field_to_dictionary guards IR_MODEL.search() result before indexing: "if not model_id:" present in _connection.py
- When model not found: logger.warning called with model name, data_dictionary returned unchanged, IR_FIELDS.search not called
- Regression test TestAddFieldToDictionary::test_empty_model_search_skips_and_returns_dict passes
- All pre-existing tests/test_connection.py tests still pass
</success_criteria>

<output>
Create .planning/phases/01.1-correctness-bug-fixes-resolve-data-corruption-and-crash-bugs/01.1-01-SUMMARY.md when done
</output>
