---
phase: 02-type-safety
plan: 03
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - odoo_fast_report_mapper/_logging.py
  - pyproject.toml
autonomous: true
requirements:
  - TYPE-01
  - TYPE-02

must_haves:
  truths:
    - "_logging.py has 0 mypy --strict errors"
    - "The [[tool.mypy.overrides]] block for odoo_fast_report_mapper._logging is removed from pyproject.toml"
    - "uv run mypy odoo_fast_report_mapper/ exits 0 (4 modules still gated)"
    - "uv run pytest exits 0 (347 tests pass)"
    - "LoggerManager has class-level annotations for _initialized: bool and _loggers: dict[str, logging.Logger]"
  artifacts:
    - path: "odoo_fast_report_mapper/_logging.py"
      provides: "Fully annotated logging module with singleton class annotations"
      contains: "_initialized: bool"
  key_links:
    - from: "LoggerManager class"
      to: "_initialized attr"
      via: "class-level annotation resolves has-type errors (RESEARCH.md §Pattern 5)"
      pattern: "_initialized: bool"
---

<objective>
Annotate `_logging.py` (19 mypy --strict errors: 8 `no-untyped-def`, 2 `has-type`, 1 `assignment`,
2 `no-any-return`, and 6 other) and remove its `[[tool.mypy.overrides]] ignore_errors = true` block.

The singleton pattern (`LoggerManager.__new__`) requires class-level annotations for `_initialized`
and `_loggers` — annotate only, do not refactor (per CONTEXT.md fragile areas rule).

Purpose: Largest category by "other" errors in the ramp sequence.
Wave 2 (parallel with 02-02) because both depend only on 02-01, no file overlap.

Output: Fully annotated _logging.py; override block removed.
</objective>

<execution_context>
@/Users/picard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/picard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-type-safety/02-CONTEXT.md
@.planning/phases/02-type-safety/02-RESEARCH.md
@.planning/phases/02-type-safety/02-PATTERNS.md
@.planning/phases/02-type-safety/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Annotate _logging.py — singleton class attrs, method signatures, formatter widening</name>
  <files>odoo_fast_report_mapper/_logging.py, pyproject.toml</files>
  <read_first>
    - odoo_fast_report_mapper/_logging.py (full current file — all 291 LOC before touching)
    - .planning/phases/02-type-safety/02-PATTERNS.md §`_logging.py` (exact class-level fix and method table)
    - .planning/phases/02-type-safety/02-RESEARCH.md §Pattern 5 (has-type singleton fix)
    - .planning/phases/02-type-safety/02-RESEARCH.md §Pattern 6 (assignment formatter widening)
    - .planning/phases/02-type-safety/02-RESEARCH.md §Fragile Areas §1 (LoggerManager._loggers — annotate only)
    - pyproject.toml (find the _logging override block to remove after all edits)
  </read_first>
  <behavior>
    - Class-level annotations added to LoggerManager: `_instance: LoggerManager | None = None` (already present per PATTERNS.md), `_initialized: bool` (new — no default, set in __new__/__init__), `_loggers: dict[str, logging.Logger]` (replaces bare `dict` annotation if present)
    - `__new__` gets return annotation: `def __new__(cls) -> LoggerManager:`
    - `ColoredFormatter.format` gets `def format(self, record: logging.LogRecord) -> str:`
    - All other bare methods get param + return annotations following the established `setup_logger` style (lines 106–115 of _logging.py)
    - `formatter` variable widened: `formatter: logging.Formatter` declared before the conditional that assigns either `ColoredFormatter(...)` or `logging.Formatter(...)`
    - `get_logger()` / methods returning from `_loggers.get(name)` return type set to `logging.Logger | None` or guarded to `logging.Logger` — check actual usage in the file
    - `from __future__ import annotations` already present (Plan 02-01); verify, do not duplicate
    - `from typing import Any` added if needed for any remaining `Any` usage
    - Under `from __future__ import annotations`, `LoggerManager | None` does not need quotes — use it directly
    - No refactoring: `_loggers` dict remains mutable at class level; `__new__` singleton pattern stays identical
    - After annotation: `uv run mypy odoo_fast_report_mapper/_logging.py --strict` → 0 errors
  </behavior>
  <action>
    Read _logging.py in full. Identify all 19 errors by running:
    `uv run mypy odoo_fast_report_mapper/_logging.py --strict 2>&1 | grep "error:"`
    before making any changes. This gives the exact line numbers for each error.

    Apply fixes in this order (reduces cascades):
    1. Add class-level annotations `_initialized: bool` and `_loggers: dict[str, logging.Logger]`
       to `LoggerManager`. These are class-body annotations without defaults — Python allows this
       for attrs set in __init__/__new__. (Fragile-area rule: annotate only, do not restructure.)
    2. Add `-> LoggerManager:` return type to `__new__(cls)`.
    3. For the `formatter` assignment pattern: add `formatter: logging.Formatter` declaration
       before the if/else conditional (per RESEARCH.md §Pattern 6).
    4. Annotate `ColoredFormatter.format(self, record: logging.LogRecord) -> str:`.
    5. Annotate all remaining untyped methods. Follow `setup_logger` signature style for param
       names and types. For methods returning a logger by name, narrow to
       `logging.Logger` (with guard) or `logging.Logger | None` depending on actual logic.
    6. Add `from typing import Any` if any method needs it (check for bare `Any` usages after step 5).

    After all annotations applied, run:
    `uv run mypy odoo_fast_report_mapper/_logging.py --strict`
    Confirm 0 errors before proceeding.

    Then remove the `[[tool.mypy.overrides]]` block for `odoo_fast_report_mapper._logging`
    from pyproject.toml. Run full package check and pytest.

    Commit message: `[CHG] type-safety(02-03): annotate _logging.py, remove override block`
  </action>
  <verify>
    <automated>
      uv run mypy odoo_fast_report_mapper/_logging.py --strict 2>&1 | grep "error:" | wc -l | grep -q "^0$" && echo "PASS: _logging 0 errors" || echo "FAIL"
      uv run mypy odoo_fast_report_mapper/ 2>&1 | grep "error:" | wc -l | grep -q "^0$" && echo "PASS: package 0 errors" || echo "FAIL"
      grep "odoo_fast_report_mapper._logging" pyproject.toml | grep "ignore_errors" && echo "FAIL: override block still present" || echo "PASS: override removed"
      grep "_initialized: bool" odoo_fast_report_mapper/_logging.py | grep -q "bool" && echo "PASS: _initialized annotated" || echo "FAIL"
      uv run pytest --tb=short -q 2>&1 | tail -3
    </automated>
  </verify>
  <done>
    `_logging.py` has 0 mypy --strict errors; class-level annotations for `_initialized: bool`
    and `_loggers: dict[str, logging.Logger]` are present; `[[tool.mypy.overrides]]` block for
    `_logging` removed from pyproject.toml; `uv run mypy odoo_fast_report_mapper/` exits 0;
    347 tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None introduced | Annotation-only changes to _logging.py |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-SC | Tampering | No package installs in this plan | accept | No new dependencies. ASVS L1 not applicable to annotation-only work. |

**Security assessment:** This plan annotates `_logging.py` and removes one pyproject.toml
override block. The `LoggerManager` singleton is annotated but not refactored — runtime behavior
is identical. The `formatter` variable widening is a type-level declaration with no runtime effect.
No new I/O, no new trust boundary, no new external inputs. No in-scope threats.
</threat_model>

<verification>
- `uv run mypy odoo_fast_report_mapper/_logging.py --strict` → 0 errors
- `uv run mypy odoo_fast_report_mapper/` → 0 errors (4 modules gated)
- `grep "_logging" pyproject.toml | grep "ignore_errors"` → no output
- `grep "_initialized: bool" odoo_fast_report_mapper/_logging.py` → found
- `uv run pytest` → 347 passed
</verification>

<success_criteria>
1. _logging.py: all 19 errors resolved — 0 mypy --strict errors
2. LoggerManager class has `_initialized: bool` and `_loggers: dict[str, logging.Logger]` class-level annotations
3. `__new__` has return type `-> LoggerManager:`
4. formatter widened to `logging.Formatter` before conditional assignment
5. No `[[tool.mypy.overrides]]` block for `_logging` in pyproject.toml
6. `uv run mypy odoo_fast_report_mapper/` → 0 errors (4 modules still gated)
7. `uv run pytest` → 347 passed
</success_criteria>

<output>
Create `.planning/phases/02-type-safety/02-03-SUMMARY.md` when done.
</output>
