---
phase: 02-type-safety
plan: 08
type: execute
wave: 6
depends_on:
  - "02-07"
files_modified:
  - odoo_fast_report_mapper/_cli.py
  - pyproject.toml
  - .github/workflows/test.yml
autonomous: true
requirements:
  - TYPE-01
  - TYPE-02
  - TYPE-03

must_haves:
  truths:
    - "_cli.py has 0 mypy --strict errors"
    - "All [[tool.mypy.overrides]] ignore_errors blocks are removed from pyproject.toml"
    - "uv run mypy odoo_fast_report_mapper/ exits 0 with zero override exemptions for internal modules"
    - "pyproject.toml [tool.mypy] has strict = true — no flag overrides needed for CI command"
    - "uv run pytest exits 0 (347 tests pass)"
    - "CI test.yml mypy step has continue-on-error removed or set to false"
    - "Entry points odoo-fast-report-mapper and odoo-fr-mapper resolve without import error"
  artifacts:
    - path: "odoo_fast_report_mapper/_cli.py"
      provides: "Fully annotated CLI entry point"
      contains: "def print_banner() -> None"
    - path: "pyproject.toml"
      provides: "Final mypy strict config with zero internal module overrides"
      contains: "strict = true"
  key_links:
    - from: "pyproject.toml [tool.mypy]"
      to: "CI test.yml mypy step"
      via: "strict = true in config means no --strict flag needed in CI command"
      pattern: "strict = true"
    - from: "_cli.py entry points"
      to: "pyproject.toml [project.scripts]"
      via: "start_odoo_fast_report_mapper must remain importable"
      pattern: "start_odoo_fast_report_mapper"
---

<objective>
Annotate `_cli.py` (10 errors: 3 `no-untyped-def` + 6 `no-untyped-call` cascade + 1 other),
remove all remaining `[[tool.mypy.overrides]] ignore_errors = true` blocks from pyproject.toml,
and remove `continue-on-error: true` from the CI mypy step — establishing the final zero-error
strict-mode state for Phase 2.

The 6 `no-untyped-call` cascade errors in `_cli.py` disappear automatically once all callee
modules are clean (Plans 02-02 through 02-07). Only 3 genuine `no-untyped-def` and 1 other
error require annotation work in this plan.

Wave 6 — final plan; must run after all other annotation plans are complete.

Output: Zero-override strict mypy config; _cli.py clean; CI enforcement enabled.
</objective>

<execution_context>
@/Users/picard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/picard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-type-safety/02-CONTEXT.md
@.planning/phases/02-type-safety/02-RESEARCH.md
@.planning/phases/02-type-safety/02-PATTERNS.md
@.planning/phases/02-type-safety/02-07-SUMMARY.md
</context>

<interfaces>
Entry points registered in pyproject.toml [project.scripts]:
  odoo-fast-report-mapper = "odoo_fast_report_mapper._cli:start_odoo_fast_report_mapper"
  odoo-fr-mapper = "odoo_fast_report_mapper._cli:start_odoo_fast_report_mapper"

These must remain importable after annotation. The function `start_odoo_fast_report_mapper`
is the Click command decorated function — its return type is `None`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Annotate _cli.py — print_banner, init_callback, main command signature</name>
  <files>odoo_fast_report_mapper/_cli.py</files>
  <read_first>
    - odoo_fast_report_mapper/_cli.py (full current file — all 341 LOC before touching)
    - .planning/phases/02-type-safety/02-PATTERNS.md §`_cli.py` (Click callback signature, print_banner fix)
    - .planning/phases/02-type-safety/02-RESEARCH.md §Error Code Breakdown (no-untyped-call: cascade resolution)
    - .planning/phases/02-type-safety/02-RESEARCH.md §Pitfall 1 (cascade disappears when callees annotated)
  </read_first>
  <behavior>
    - `print_banner() -> None:` (bare function, no params)
    - `init_callback(ctx: click.Context, param: click.Parameter, value: bool) -> None:` (Click callback signature)
    - `start_odoo_fast_report_mapper` Click command decorated function: `-> None` (Click wraps it; annotation is on the Python function)
    - Any other bare functions in _cli.py get annotated per PATTERNS.md
    - `from __future__ import annotations` already present (Plan 02-01); verify, do not duplicate
    - `import click` already present; `click.Context`, `click.Parameter` available from click package
    - The 6 `no-untyped-call` errors were cascade from other modules — confirm they are gone after Plans 02-02..02-07
    - After annotation: `uv run mypy odoo_fast_report_mapper/_cli.py --strict` (with override removed) → 0 errors
  </behavior>
  <action>
    Read _cli.py in full. Run `uv run mypy odoo_fast_report_mapper/_cli.py --strict 2>&1` with the
    _cli override temporarily disabled (or add `--ignore-missing-imports` temporarily — do not commit
    that; use it only to see the error list). The cascade `no-untyped-call` errors should already
    be gone since callees are clean.

    Annotate:
    1. `print_banner()` → `def print_banner() -> None:`
    2. `init_callback(ctx, param, value)` → `def init_callback(ctx: click.Context, param: click.Parameter, value: bool) -> None:`
    3. The main Click command function → `-> None`
    4. Any other bare functions found in the file

    For Click-decorated functions: the decorator `@click.command()` / `@click.option()` wraps the
    function; mypy understands Click decorators when click.pyi stubs are available. `click>=8.1.3`
    ships its own `py.typed` + stubs — no extra install needed.

    Do NOT annotate `@click.option()` parameters themselves (those are Click-managed); only annotate
    the Python function signatures.

    After annotation: run `uv run mypy odoo_fast_report_mapper/_cli.py --strict` → 0 errors.

    Commit message: `[CHG] type-safety(02-08): annotate _cli.py`
  </action>
  <verify>
    <automated>
      uv run mypy odoo_fast_report_mapper/_cli.py --strict 2>&1 | grep "error:" | wc -l | grep -q "^0$" && echo "PASS: _cli 0 errors" || echo "FAIL"
      grep "def print_banner() -> None" odoo_fast_report_mapper/_cli.py | grep -q "None" && echo "PASS: print_banner annotated" || echo "FAIL"
    </automated>
  </verify>
  <done>
    `_cli.py` annotated — `print_banner() -> None`, `init_callback` with Click types, main command
    with `-> None`; `uv run mypy odoo_fast_report_mapper/_cli.py --strict` → 0 errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove all remaining override blocks, remove CI continue-on-error, final verification</name>
  <files>pyproject.toml, .github/workflows/test.yml</files>
  <read_first>
    - pyproject.toml (find all remaining [[tool.mypy.overrides]] ignore_errors blocks for internal modules)
    - .github/workflows/test.yml (find the mypy step with continue-on-error: true)
    - .planning/phases/02-type-safety/02-RESEARCH.md §Open Questions (CI workflow question — recommendation to remove continue-on-error in Plan 02-08)
    - .planning/phases/02-type-safety/02-CONTEXT.md §Integration Points (CI enforcement boundary: Phase 2 ensures config is correct)
  </read_first>
  <action>
    STEP A — Remove remaining override blocks from pyproject.toml:
    The only override block that should remain after Task 1 is:
      `[[tool.mypy.overrides]] module = "odoorpc_toolbox.*" follow_untyped_imports = true`
    ALL other `[[tool.mypy.overrides]]` blocks with `ignore_errors = true` for internal modules
    must be removed. At this point in the ramp, all have been removed by previous plans except
    `_cli`. After Task 1, the `_cli` block is the last one — remove it now.

    Verify by reading pyproject.toml: the only mypy override block remaining should be for
    `odoorpc_toolbox.*`.

    STEP B — Remove continue-on-error from CI mypy step:
    In `.github/workflows/test.yml`, find the step that runs mypy. Remove `continue-on-error: true`
    or change it to `continue-on-error: false`. The mypy command is already corrected in Plan 02-01
    (removed odoo_report_helper/ from command). This makes mypy a blocking check in CI.

    NOTE: Per CONTEXT.md §Integration Points — "The actual CI YAML change is Phase 4 CI-02, not
    this phase — Phase 2 only ensures the config is correct." However, RESEARCH.md §Open Questions
    recommends removing `continue-on-error: true` in Plan 02-08 because TYPE-03 requires CI
    enforcement. This is the final plan — removing it here is the correct interpretation.

    STEP C — Final zero-error verification:
    Run `uv run mypy odoo_fast_report_mapper/` → must exit 0 with NO internal override blocks.
    Run `uv run pytest --tb=short -q` → 347 pass.
    Run entry-point import check: `python -c "from odoo_fast_report_mapper._cli import start_odoo_fast_report_mapper; print('PASS')"`.

    Commit message: `[CHG] type-safety(02-08): remove all override blocks, enable CI mypy enforcement — Phase 2 complete`
  </action>
  <verify>
    <automated>
      uv run mypy odoo_fast_report_mapper/ 2>&1 | grep "error:" | wc -l | grep -q "^0$" && echo "PASS: 0 errors, zero overrides" || echo "FAIL"
      grep "ignore_errors" pyproject.toml | grep -v "odoorpc_toolbox" && echo "FAIL: internal ignore_errors blocks still present" || echo "PASS: all internal overrides removed"
      grep "odoorpc_toolbox" pyproject.toml | grep -q "follow_untyped_imports" && echo "PASS: odoorpc_toolbox override retained" || echo "FAIL"
      grep "strict = true" pyproject.toml | grep -q "true" && echo "PASS: strict=true in pyproject.toml" || echo "FAIL"
      cat .github/workflows/test.yml | grep -A3 "mypy" | grep "continue-on-error" | grep -q "true" && echo "FAIL: continue-on-error still true" || echo "PASS: continue-on-error removed/false"
      python -c "from odoo_fast_report_mapper._cli import start_odoo_fast_report_mapper; print('PASS: entry point importable')"
      uv run pytest --tb=short -q 2>&1 | tail -3
    </automated>
  </verify>
  <done>
    All internal `ignore_errors = true` override blocks removed from pyproject.toml; only the
    `odoorpc_toolbox.*` `follow_untyped_imports` override remains; `continue-on-error: true`
    removed from CI mypy step; `uv run mypy odoo_fast_report_mapper/` exits 0; both entry points
    importable; 347 tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI mypy enforcement | Mypy is now a blocking CI check; failing annotations block merges |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08-SC | Tampering | No package installs in this plan | accept | No new dependencies. |
| T-02-08-01 | Tampering | CI mypy now blocking | mitigate | Removing `continue-on-error: true` makes mypy a hard gate in CI. This prevents future type regressions from merging silently. Enforcement is the intended security posture per TYPE-03. |

**Security assessment:** This plan removes pyproject.toml override blocks and makes CI mypy
enforcement mandatory. The _cli.py annotations cover Click callback types — no new I/O or trust
boundary. The Click framework handles all user input parsing; annotations document the expected
types, they do not add validation. Enabling CI blocking for mypy is a quality improvement with
no in-scope security threats. No in-scope high-severity threats.
</threat_model>

<verification>
Phase 2 success criteria (all must be TRUE):

1. `uv run mypy odoo_fast_report_mapper/` → 0 errors
2. `grep "strict = true" pyproject.toml` → found (TYPE-03)
3. `grep "ignore_errors = true" pyproject.toml` → no output (all internal overrides removed)
4. `grep "follow_untyped_imports = true" pyproject.toml` → found (odoorpc_toolbox.* only, D-03)
5. `python -c "from odoo_fast_report_mapper._odoo_types import IrModelRecord"` → no error
6. `python -c "from odoo_fast_report_mapper._cli import start_odoo_fast_report_mapper"` → no error
7. `uv run pytest` → 347 passed
8. `cat .github/workflows/test.yml | grep -A3 mypy` → no `continue-on-error: true`
9. `grep "mypy>=1.18" pyproject.toml` → found (pin updated in Plan 02-01)
</verification>

<success_criteria>
1. _cli.py: 0 mypy --strict errors — `print_banner`, `init_callback`, main command annotated
2. pyproject.toml: zero `[[tool.mypy.overrides]] ignore_errors = true` blocks for internal modules
3. pyproject.toml [tool.mypy]: only `strict = true` and `python_version = "3.12"` (plus the odoorpc_toolbox override)
4. CI test.yml: `continue-on-error: true` removed from mypy step
5. `uv run mypy odoo_fast_report_mapper/` → 0 errors (TYPE-01 satisfied)
6. All public functions and methods have complete annotations (TYPE-02 satisfied)
7. `strict = true` in pyproject.toml — CI uses no override flags (TYPE-03 satisfied)
8. `uv run pytest` → 347 passed
9. Entry points `odoo-fast-report-mapper` and `odoo-fr-mapper` resolve without error
</success_criteria>

<output>
Create `.planning/phases/02-type-safety/02-08-SUMMARY.md` when done.
</output>
