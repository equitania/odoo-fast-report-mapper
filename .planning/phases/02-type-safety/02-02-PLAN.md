---
phase: 02-type-safety
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - odoo_fast_report_mapper/_lang_utils.py
  - pyproject.toml
autonomous: true
requirements:
  - TYPE-01
  - TYPE-02

must_haves:
  truths:
    - "_lang_utils.py has 0 mypy --strict errors"
    - "The [[tool.mypy.overrides]] block for odoo_fast_report_mapper._lang_utils is removed from pyproject.toml"
    - "uv run mypy odoo_fast_report_mapper/ exits 0 (_lang_utils is no longer gated)"
    - "uv run pytest exits 0 (347 tests pass)"
  artifacts:
    - path: "odoo_fast_report_mapper/_lang_utils.py"
      provides: "Fully annotated lang utilities"
      contains: "def normalize_name_dict"
  key_links:
    - from: "_lang_utils.py"
      to: "pyproject.toml [[tool.mypy.overrides]]"
      via: "override block for _lang_utils removed when module is clean"
      pattern: "_lang_utils"
---

<objective>
Annotate `_lang_utils.py` (7 mypy --strict errors: 4 `type-arg`, 1 `no-untyped-def`,
2 `no-any-return`) and remove its `[[tool.mypy.overrides]] ignore_errors = true` block.

Purpose: Second ramp step — smallest module with actual annotation work (the two trivial modules
were cleared in Plan 02-01). After this plan, _lang_utils is no longer gated; remaining gated
modules are cleared by Plans 02-03 through 02-08.

Wave 2 (parallel with 02-03) because both depend only on 02-01, no file overlap.

Output: Fully annotated _lang_utils.py; pyproject.toml override block for _lang_utils removed.
</objective>

<execution_context>
@/Users/picard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/picard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-type-safety/02-CONTEXT.md
@.planning/phases/02-type-safety/02-RESEARCH.md
@.planning/phases/02-type-safety/02-PATTERNS.md
@.planning/phases/02-type-safety/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Annotate _lang_utils.py and remove its override block</name>
  <files>odoo_fast_report_mapper/_lang_utils.py, pyproject.toml</files>
  <read_first>
    - odoo_fast_report_mapper/_lang_utils.py (full current file — read all 120 LOC before touching)
    - .planning/phases/02-type-safety/02-PATTERNS.md §`_lang_utils.py` (exact per-function fix table)
    - .planning/phases/02-type-safety/02-RESEARCH.md §Error Code Breakdown (type-arg, no-any-return patterns)
    - .planning/phases/02-type-safety/02-PATTERNS.md §Shared Patterns (dict[str,Any] Tier-2 justification)
    - pyproject.toml (find the _lang_utils override block to remove)
  </read_first>
  <behavior>
    - `normalize_name_dict(name_dict: dict) -> dict` → `normalize_name_dict(name_dict: dict[str, str]) -> dict[str, str]`
    - `get_primary_lang(name_dict: dict, ...) -> str` → `get_primary_lang(name_dict: dict[str, str], preferred_lang: str = "de_DE") -> str`; no-any-return resolved because iter(dict[str,str]) yields str
    - `build_name_search_domain(name_dict: dict) -> list` → `build_name_search_domain(name_dict: dict[str, str]) -> list[Any]`; add inline comment `# Any: Odoo domain tuples contain mixed types (str, str, str)`
    - `resolve_attachment_value(attachment, company_lang, fallback_lang=None)` → typed as `(attachment: str | dict[str, str], company_lang: str, fallback_lang: str | None = None) -> str`
    - `from __future__ import annotations` is already present (added in Plan 02-01); verify, do not duplicate
    - Add `from typing import Any` to imports if not already present
    - After annotation: `uv run mypy odoo_fast_report_mapper/_lang_utils.py --strict` → 0 errors
  </behavior>
  <action>
    Read _lang_utils.py in full. Apply the four annotation fixes listed in the behavior block above.
    All four functions are at lines 31, 49, 70, 95 per 02-PATTERNS.md — confirm line numbers
    against actual file before editing (post-Phase-1.1 bug fixes may have shifted lines).

    For `build_name_search_domain`: the return type is `list[Any]` (Tier 2 — Odoo domain tuples
    are heterogeneous `[field, operator, value]`). The inline comment `# Any: Odoo domain tuples
    contain mixed types (str, str, str)` satisfies D-07 for non-pre-justified Any.

    For `get_primary_lang`: after narrowing `name_dict` to `dict[str, str]`, `next(iter(name_dict))`
    returns `str` — no cast needed, no-any-return disappears automatically.

    Do NOT alter any logic, guard conditions, or runtime behavior. Annotations only.

    After all annotations applied, open pyproject.toml and remove the entire
    `[[tool.mypy.overrides]]` block for `odoo_fast_report_mapper._lang_utils` (the block with
    `ignore_errors = true`). Do not disturb any other override blocks.

    Run `uv run mypy odoo_fast_report_mapper/_lang_utils.py` to confirm 0 errors before committing.
    Run `uv run mypy odoo_fast_report_mapper/` to confirm total is still 0 (other modules still gated).
    Run `uv run pytest --tb=short -q` to confirm 347 tests pass.

    Commit message: `[CHG] type-safety(02-02): annotate _lang_utils.py, remove override block`
  </action>
  <verify>
    <automated>
      uv run mypy odoo_fast_report_mapper/_lang_utils.py --strict 2>&1 | grep "error:" | wc -l | grep -q "^0$" && echo "PASS: _lang_utils 0 errors" || echo "FAIL"
      uv run mypy odoo_fast_report_mapper/ 2>&1 | grep "error:" | wc -l | grep -q "^0$" && echo "PASS: package 0 errors" || echo "FAIL"
      grep "odoo_fast_report_mapper._lang_utils" pyproject.toml && echo "FAIL: override block still present" || echo "PASS: override removed"
      uv run pytest --tb=short -q 2>&1 | tail -3
    </automated>
  </verify>
  <done>
    `_lang_utils.py` has 0 mypy --strict errors; its `[[tool.mypy.overrides]]` block is removed
    from pyproject.toml; `uv run mypy odoo_fast_report_mapper/` exits 0; 347 tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None introduced | Annotation-only changes to _lang_utils.py |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-SC | Tampering | No package installs in this plan | accept | No new dependencies; annotation-only. ASVS L1 not applicable. |

**Security assessment:** This plan adds type annotations to `_lang_utils.py` and removes one
pyproject.toml override block. No new I/O, no new external inputs, no new trust boundary. No
runtime behavior changes — annotations are erased at runtime by Python. No in-scope threats.
</threat_model>

<verification>
- `uv run mypy odoo_fast_report_mapper/_lang_utils.py --strict` → 0 errors
- `uv run mypy odoo_fast_report_mapper/` → 0 errors
- `grep "_lang_utils" pyproject.toml | grep "ignore_errors"` → no output
- `uv run pytest` → 347 passed
</verification>

<success_criteria>
1. _lang_utils.py: all 4 bare-dict params annotated (`dict[str, str]` or `str | dict[str, str]`)
2. _lang_utils.py: return types on all 4 functions (`dict[str, str]`, `str`, `list[Any]`, `str`)
3. No `[[tool.mypy.overrides]]` block for `_lang_utils` in pyproject.toml
4. `uv run mypy odoo_fast_report_mapper/` → 0 errors (_lang_utils no longer gated)
5. `uv run pytest` → 347 passed
</success_criteria>

<output>
Create `.planning/phases/02-type-safety/02-02-SUMMARY.md` when done.
</output>
