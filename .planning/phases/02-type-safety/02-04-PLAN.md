---
phase: 02-type-safety
plan: 04
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
  - "02-03"
files_modified:
  - odoo_fast_report_mapper/_report.py
  - pyproject.toml
autonomous: true
requirements:
  - TYPE-01
  - TYPE-02

must_haves:
  truths:
    - "_report.py has 0 mypy --strict errors"
    - "The [[tool.mypy.overrides]] block for odoo_fast_report_mapper._report is removed from pyproject.toml"
    - "uv run mypy odoo_fast_report_mapper/ exits 0 (3 modules still gated: _utils, _connection, _cli)"
    - "uv run pytest exits 0 (347 tests pass)"
    - "__init__ has complete parameter annotations including company_id: int | Literal[False]"
  artifacts:
    - path: "odoo_fast_report_mapper/_report.py"
      provides: "Fully annotated Report model class"
      contains: "company_id: int | Literal[False]"
  key_links:
    - from: "_report.py __init__"
      to: "_connection.py"
      via: "Report instances created by OdooConnection.map_reports; signatures must be compatible"
      pattern: "Report("
---

<objective>
Annotate `_report.py` (10 mypy --strict errors: 6 `no-untyped-def`, 2 `var-annotated`, 2 `type-arg`)
and remove its `[[tool.mypy.overrides]] ignore_errors = true` block.

Wave 3 because _report.py is downstream of _lang_utils.py (uses `resolve_attachment_value`
from lang_utils) and should be cleanly annotated with fully-typed callee context.

Output: Fully annotated _report.py with Literal[False] for Odoo convention patterns.
</objective>

<execution_context>
@/Users/picard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/picard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-type-safety/02-CONTEXT.md
@.planning/phases/02-type-safety/02-RESEARCH.md
@.planning/phases/02-type-safety/02-PATTERNS.md
@.planning/phases/02-type-safety/02-02-SUMMARY.md
@.planning/phases/02-type-safety/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Annotate _report.py — __init__ params, var-annotated, method return types</name>
  <files>odoo_fast_report_mapper/_report.py, pyproject.toml</files>
  <read_first>
    - odoo_fast_report_mapper/_report.py (full current file — all 147 LOC before touching)
    - .planning/phases/02-type-safety/02-PATTERNS.md §`_report.py` (full __init__ signature and method table)
    - .planning/phases/02-type-safety/02-RESEARCH.md §Error Code Breakdown (var-annotated fix pattern)
    - .planning/phases/02-type-safety/02-RESEARCH.md §Pattern 3 (var-annotated: bare {} literal)
    - .planning/phases/02-type-safety/02-PATTERNS.md §Shared Patterns (X | Literal[False] Odoo sentinel)
    - pyproject.toml (find the _report override block to remove)
  </read_first>
  <behavior>
    - `__init__` bare params annotated: `company_id: int | Literal[False]`, `eq_export_type: str = "pdf"`, `print_report_name: str | dict[str, str] = "Report"`, `attachment: str | dict[str, str] = "Report.pdf"`, `eq_ignore_images: bool = True`, `eq_handling_html_fields: str = "standard"`, `multi: bool = False`, `attachment_use: bool = False`, `eq_print_button: bool = False`, `dependencies: list[str] | Literal[False] = False`, `model_fields: dict[str, list[str]] | None = None`, `calculated_fields: dict[str, Any] | None = None`, `eq_multiprint: str = "standard"`. `__init__` return `-> None`.
    - `self._data_dictionary: dict[str, Any] = {}` (var-annotated fix; `# Any: mixed Odoo field values` inline comment)
    - `self_ensure(self) -> None:`
    - `ensure_data_for_yaml(self) -> dict[str, Any]:`
    - `add_fields(self, field_dict: dict[str, list[str]]) -> None:` (fixes bare `dict`)
    - `add_calculated_fields(self, field_dict: dict[str, Any]) -> None:`
    - `add_dependencies(self, dependency_list: list[str]) -> None:`
    - `from __future__ import annotations` already present (Plan 02-01); verify, do not duplicate
    - `from typing import Any, Literal` added to imports (Literal needed for `Literal[False]`)
    - After annotation: `uv run mypy odoo_fast_report_mapper/_report.py --strict` → 0 errors
  </behavior>
  <action>
    Read _report.py in full. Run `uv run mypy odoo_fast_report_mapper/_report.py --strict 2>&1`
    before any edits to get exact line numbers for all 10 errors.

    Apply annotations in this order (prevents cascade confusion):
    1. Add `from typing import Any, Literal` to imports if not present.
    2. Annotate `__init__` — all bare params. Per 02-PATTERNS.md §_report.py, the established
       pattern is already partially set (`entry_name: dict[str, str]`, `report_name: str` are typed);
       extend it for the remaining bare params. `company_id` uses `int | Literal[False]` (Odoo
       company_id is int in multi-company mode, False in single-company mode).
       `dependencies` uses `list[str] | Literal[False] = False` (False = no dependencies specified).
    3. Fix the var-annotated: find the bare `{}` assignment in `__init__` body (likely `self._data_dictionary = {}`);
       add inline annotation `self._data_dictionary: dict[str, Any] = {}`.
    4. Annotate the 5 remaining methods: `self_ensure`, `ensure_data_for_yaml`, `add_fields`,
       `add_calculated_fields`, `add_dependencies`. Follow the signature patterns in the behavior block.

    Do NOT alter any logic, guard conditions (e.g. the `entry_name` isinstance+truthy guard from
    Phase 1.1 BUG-06), or runtime behavior. Annotations only.

    After annotation, verify:
    `uv run mypy odoo_fast_report_mapper/_report.py --strict` → 0 errors

    Remove the `[[tool.mypy.overrides]]` block for `odoo_fast_report_mapper._report` from pyproject.toml.
    Run full package check: `uv run mypy odoo_fast_report_mapper/` → 0 errors (3 modules still gated).
    Run: `uv run pytest --tb=short -q` → 347 pass.

    Commit message: `[CHG] type-safety(02-04): annotate _report.py, remove override block`
  </action>
  <verify>
    <automated>
      uv run mypy odoo_fast_report_mapper/_report.py --strict 2>&1 | grep "error:" | wc -l | grep -q "^0$" && echo "PASS: _report 0 errors" || echo "FAIL"
      uv run mypy odoo_fast_report_mapper/ 2>&1 | grep "error:" | wc -l | grep -q "^0$" && echo "PASS: package 0 errors" || echo "FAIL"
      grep "odoo_fast_report_mapper._report" pyproject.toml | grep "ignore_errors" && echo "FAIL: override still present" || echo "PASS: override removed"
      grep "Literal\[False\]" odoo_fast_report_mapper/_report.py | grep -q "company_id" && echo "PASS: Literal[False] in company_id" || echo "FAIL"
      uv run pytest --tb=short -q 2>&1 | tail -3
    </automated>
  </verify>
  <done>
    `_report.py` has 0 mypy --strict errors; `__init__` is fully annotated including
    `company_id: int | Literal[False]`; `_data_dictionary` has inline type annotation;
    all 5 methods annotated; override block removed from pyproject.toml;
    `uv run mypy odoo_fast_report_mapper/` exits 0; 347 tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None introduced | Annotation-only changes to _report.py |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-SC | Tampering | No package installs in this plan | accept | No new dependencies. The `Literal[False]` annotation for company_id precisely models Odoo's existing data contract — no new behavior, no new attack surface. |

**Security assessment:** This plan annotates `_report.py`. The `company_id: int | Literal[False]`
annotation correctly models the existing Odoo RPC data contract — it does not change any validation
logic (BUG-06 guard from Phase 1.1 remains intact). No new I/O, no new trust boundary.
No in-scope threats.
</threat_model>

<verification>
- `uv run mypy odoo_fast_report_mapper/_report.py --strict` → 0 errors
- `uv run mypy odoo_fast_report_mapper/` → 0 errors (3 modules gated: _utils, _connection, _cli)
- `grep "_report" pyproject.toml | grep "ignore_errors"` → no output
- `grep "Literal\[False\]" odoo_fast_report_mapper/_report.py` → found (company_id)
- `uv run pytest` → 347 passed
</verification>

<success_criteria>
1. _report.py: all 10 errors resolved — 0 mypy --strict errors
2. `__init__` fully annotated with `company_id: int | Literal[False]` and all other bare params
3. `_data_dictionary: dict[str, Any] = {}` inline annotation present
4. All 5 methods annotated with concrete return types
5. No `[[tool.mypy.overrides]]` block for `_report` in pyproject.toml
6. `uv run mypy odoo_fast_report_mapper/` → 0 errors (3 modules still gated)
7. `uv run pytest` → 347 passed
</success_criteria>

<output>
Create `.planning/phases/02-type-safety/02-04-SUMMARY.md` when done.
</output>
