---
phase: 01-package-consolidation
plan: 05
type: execute
wave: 5
depends_on:
  - "01-04"
files_modified:
  - odoo_fast_report_mapper/__init__.py
  - odoo_fast_report_mapper/odoo_fast_report_mapper.py
  - odoo_fast_report_mapper/_cli.py
  - pyproject.toml
  - tests/conftest.py
  - tests/test_connection.py
  - tests/test_report.py
  - tests/test_utils.py
  - tests/test_exceptions.py
  - tests/test_lang_utils.py
  - tests/test_logging.py
  - tests/test_progress.py
  - tests/test_cli.py
autonomous: true
requirements:
  - CONS-01
  - CONS-02
  - CONS-03
  - CONS-04
  - DEAD-01
  - DEAD-02
  - DEAD-03

must_haves:
  truths:
    - "import odoo_report_helper raises ModuleNotFoundError — the package directory is gone"
    - "from odoo_fast_report_mapper import OdooConnection resolves without error"
    - "from odoo_fast_report_mapper._progress import ProgressBar raises ImportError"
    - "pytest tests/test_connection.py passes standalone — no circular import"
    - "All remaining tests pass (~348 — 369 minus 21 deleted dead-API tests)"
    - "Both CLI entry points still work: odoo-fast-report-mapper --help and odoo-fr-mapper --help exit 0"
    - "pyproject.toml contains no references to odoo_report_helper"
    - "Coverage loss is only for dead-code lines (DEAD-01/02/03) and odoo_report_helper modules"
  artifacts:
    - path: "odoo_fast_report_mapper/__init__.py"
      provides: "OdooConnection, Report, create_connection_from_env, OdooConnectionError, PathDoesNotExistError re-exports"
    - path: "odoo_fast_report_mapper/_cli.py"
      provides: "start_odoo_fast_report_mapper CLI entry point (renamed from odoo_fast_report_mapper.py)"
    - path: "tests/test_connection.py"
      provides: "Merged test file for OdooConnection — runs standalone"
    - path: ".planning/phases/01-package-consolidation/coverage-post.txt"
      provides: "Post-Phase-1 coverage for diff against baseline"
  key_links:
    - from: "tests/test_connection.py"
      to: "odoo_fast_report_mapper._connection"
      via: "from odoo_fast_report_mapper._connection import OdooConnection"
      pattern: "from odoo_fast_report_mapper._connection import OdooConnection"
    - from: "tests/conftest.py"
      to: "odoo_fast_report_mapper._utils.ODOO"
      via: "patch('odoo_fast_report_mapper._utils.ODOO')"
      pattern: "odoo_fast_report_mapper\\._utils\\.ODOO"
    - from: "pyproject.toml"
      to: "odoo_fast_report_mapper._cli"
      via: "[project.scripts] entries"
      pattern: "odoo_fast_report_mapper\\._cli"
---

<objective>
The final Wave: switch all import paths, merge test files, delete old source files and the odoo_report_helper/ package, update pyproject.toml, rename the CLI module, and verify all Phase 1 success criteria.

Purpose: This is the "go live" commit that makes the consolidation observable from outside the package. Everything before this wave was additive (new files alongside old). This wave is the atomic switch.

Output: Single-package codebase. Old structures deleted. ~348 tests green. All 5 success criteria from ROADMAP.md pass.
</objective>

<execution_context>
@/Users/picard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/picard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-package-consolidation/01-CONTEXT.md
@.planning/phases/01-package-consolidation/01-RESEARCH.md
@.planning/phases/01-package-consolidation/01-PATTERNS.md
@.planning/phases/01-package-consolidation/01-01-SUMMARY.md
@.planning/phases/01-package-consolidation/01-04-SUMMARY.md
</context>

<interfaces>
Target __init__.py content (D-04 per PATTERNS.md):
  from .__version__ import (__author__, __author_email__, __copyright__,
    __description__, __license__, __title__, __url__, __version__, __version_info__)
  from ._connection import OdooConnection
  from ._exceptions import OdooConnectionError, PathDoesNotExistError, PathDoesNotExitError
  from ._report import Report
  from ._utils import create_connection_from_env
  __all__ = ["OdooConnection", "Report", "create_connection_from_env",
             "OdooConnectionError", "PathDoesNotExistError", "PathDoesNotExitError",
             "__version__", "__version_info__", "__title__", "__description__",
             "__author__", "__author_email__", "__url__", "__license__", "__copyright__"]

CLI rename decision: odoo_fast_report_mapper.py → _cli.py (D-03 discretion, included in this plan).
  pyproject.toml [project.scripts] must be updated to reference _cli:
    odoo-fast-report-mapper = "odoo_fast_report_mapper._cli:start_odoo_fast_report_mapper"
    odoo-fr-mapper = "odoo_fast_report_mapper._cli:start_odoo_fast_report_mapper"

CLI internal import updates (in _cli.py / formerly odoo_fast_report_mapper.py):
  from . import eq_utils        → from . import _utils as _utils  (or: from . import _utils)
  from .logging_config import   → from ._logging import
  eq_utils.*                    → _utils.*  (all 5 call sites per PATTERNS.md)

conftest.py critical mock patch path (R-02 — highest risk):
  Old: patch("odoo_report_helper.utils.ODOO")
  New: patch("odoo_fast_report_mapper._utils.ODOO")

test_connection.py merge:
  Primary source: test_eq_odoo_connection.py
  Absorb unique base tests from: test_odoo_connection.py
  Import updates: EqOdooConnection → OdooConnection; from _connection import
  Patch target: "odoo_report_helper.odoo_connection.utils.prepare_connection" → "odoo_fast_report_mapper._utils.prepare_connection"
  _make_connection helper: update patch target and class name

test_progress.py cut:
  Delete TestProgressBar (lines 27-113), TestCreateProgressBar (152-188), TestReportProgress (195-249)
  Update import: from odoo_fast_report_mapper._progress import progress_bar
  Update patch: "odoo_fast_report_mapper.progress.sys.stdout" → "odoo_fast_report_mapper._progress.sys.stdout"
  Keep TestProgressBarFunction verbatim

pyproject.toml changes (3 sections):
  [tool.setuptools.packages.find] include: remove "odoo_report_helper*"
  [tool.coverage.run] source: remove "odoo_report_helper"
  [tool.ruff.lint.isort] known-first-party: remove "odoo_report_helper"
  [project.scripts]: update to ._cli (if CLI renamed)

Files to DELETE after tests are green:
  odoo_report_helper/__init__.py
  odoo_report_helper/odoo_connection.py
  odoo_report_helper/report.py
  odoo_report_helper/utils.py
  odoo_report_helper/exceptions.py
  odoo_fast_report_mapper/eq_odoo_connection.py
  odoo_fast_report_mapper/eq_report.py
  odoo_fast_report_mapper/eq_utils.py
  odoo_fast_report_mapper/lang_utils.py
  odoo_fast_report_mapper/logging_config.py
  odoo_fast_report_mapper/progress.py
  odoo_fast_report_mapper/odoo_fast_report_mapper.py (after rename to _cli.py)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite __init__.py and update conftest.py mock patch path</name>
  <files>odoo_fast_report_mapper/__init__.py, tests/conftest.py</files>

  <read_first>
    - odoo_fast_report_mapper/__init__.py (current content — full read before rewriting)
    - tests/conftest.py (full file — find all patch() calls referencing odoo_report_helper; line 188 is the critical one per RESEARCH.md)
  </read_first>

  <action>
    Rewrite odoo_fast_report_mapper/__init__.py to the D-04 target pattern:
      - Keep all __version__ imports from .__version__ (unchanged)
      - Add: from ._connection import OdooConnection
      - Add: from ._exceptions import OdooConnectionError, PathDoesNotExistError, PathDoesNotExitError
      - Add: from ._report import Report
      - Add: from ._utils import create_connection_from_env
      - Define __all__ with all public names (see interfaces block above)
      - Remove all old eq_* imports

    Update tests/conftest.py:
      - Find every patch() call referencing "odoo_report_helper.utils.ODOO"
      - Replace with: "odoo_fast_report_mapper._utils.ODOO"
      - Find every patch() call referencing "odoo_report_helper.odoo_connection.utils.prepare_connection"
      - Replace with: "odoo_fast_report_mapper._utils.prepare_connection"
      - Check for any other odoo_report_helper references in conftest.py and update them
      - All other fixture content (sample data, tmp dirs, env files) is unchanged

    After both edits, run: uv run pytest -q --tb=short
    If tests fail at this point, the conftest.py patch paths are the most likely cause. Debug before proceeding.
  </action>

  <verify>
    <automated>python -c "from odoo_fast_report_mapper import OdooConnection, Report, create_connection_from_env, OdooConnectionError; print('OK')"</automated>
    Expect: exits 0
    <automated>grep -c "odoo_report_helper" odoo_fast_report_mapper/__init__.py</automated>
    Expect: 0
    <automated>grep -c "odoo_report_helper" tests/conftest.py</automated>
    Expect: 0
    <automated>grep -c "odoo_fast_report_mapper._utils.ODOO" tests/conftest.py</automated>
    Expect: >= 1 (the critical mock_odoorpc fixture patch path)
    <automated>uv run pytest -q --tb=short 2>&1 | tail -5</automated>
    Expect: tests still pass (old modules still present; conftest.py now patches the new path which will resolve once _utils.py ODOO import is wired)
  </verify>

  <done>
    - __init__.py re-exports OdooConnection, Report, create_connection_from_env, all three exception classes
    - conftest.py has zero odoo_report_helper references
    - conftest.py patches "odoo_fast_report_mapper._utils.ODOO" (not the old path)
    - python -c "from odoo_fast_report_mapper import OdooConnection" exits 0
  </done>
</task>

<task type="auto">
  <name>Task 2: Rename CLI module and update pyproject.toml</name>
  <files>odoo_fast_report_mapper/_cli.py, pyproject.toml</files>

  <read_first>
    - odoo_fast_report_mapper/odoo_fast_report_mapper.py (full file — read before rename to understand all eq_utils.* call sites: lines 48, 145, 262, 297, 299 per PATTERNS.md)
    - pyproject.toml (current content — full read before edits)
  </read_first>

  <action>
    Step 1 — Rename/copy CLI module:
      Copy odoo_fast_report_mapper/odoo_fast_report_mapper.py to odoo_fast_report_mapper/_cli.py
      Update _cli.py imports:
        - "from . import eq_utils" → "from . import _utils" (or "from . import _utils as _utils")
        - "from .logging_config import get_logger, setup_logging" → "from ._logging import get_logger, setup_logging"
      Update all eq_utils.* call sites in the function body:
        - eq_utils.generate_env_template(...) → _utils.generate_env_template(...)
        - eq_utils.create_connection_from_env(...) → _utils.create_connection_from_env(...)
        - eq_utils.list_yaml_reports(...) → _utils.list_yaml_reports(...)
        - eq_utils.build_reports_from_yaml_objects(...) → _utils.build_reports_from_yaml_objects(...)
        - eq_utils.collect_all_reports(...) → _utils.collect_all_reports(...)
      The function name start_odoo_fast_report_mapper must NOT be changed.
      The old odoo_fast_report_mapper.py is NOT deleted yet (done in Task 4).

    Step 2 — Update pyproject.toml (3 sections):
      [project.scripts] (lines 43-45):
        odoo-fast-report-mapper = "odoo_fast_report_mapper._cli:start_odoo_fast_report_mapper"
        odoo-fr-mapper = "odoo_fast_report_mapper._cli:start_odoo_fast_report_mapper"

      [tool.setuptools.packages.find] (line 68):
        include = ["odoo_fast_report_mapper*"]
        (remove "odoo_report_helper*")

      [tool.coverage.run] (line 100):
        source = ["odoo_fast_report_mapper"]
        (remove "odoo_report_helper")

      [tool.ruff.lint.isort] (line 82):
        known-first-party = ["odoo_fast_report_mapper"]
        (remove "odoo_report_helper")

    Reinstall the package to pick up the entry-point change:
      uv pip install -e .

    Then verify both CLI commands:
      odoo-fast-report-mapper --help
      odoo-fr-mapper --help
  </action>

  <verify>
    <automated>python -c "from odoo_fast_report_mapper._cli import start_odoo_fast_report_mapper; print('OK')"</automated>
    Expect: exits 0
    <automated>odoo-fast-report-mapper --help 2>&1 | head -3</automated>
    Expect: exits 0, shows usage
    <automated>odoo-fr-mapper --help 2>&1 | head -3</automated>
    Expect: exits 0, shows usage
    <automated>grep -c "odoo_report_helper" pyproject.toml</automated>
    Expect: 0
    <automated>grep -c "_cli" pyproject.toml</automated>
    Expect: 2 (both entry points updated)
    <automated>uv run pytest -q --tb=short 2>&1 | tail -3</automated>
    Expect: tests still pass
  </verify>

  <done>
    - odoo_fast_report_mapper/_cli.py exists with updated imports and all eq_utils.* → _utils.* replacements
    - pyproject.toml [project.scripts] references _cli:start_odoo_fast_report_mapper for both entries
    - pyproject.toml has zero odoo_report_helper references
    - Both CLI commands resolve (uv pip install -e . applied)
    - Test suite still green
  </done>
</task>

<task type="auto">
  <name>Task 3: Merge test files (9 test file operations)</name>
  <files>
    tests/test_connection.py,
    tests/test_report.py,
    tests/test_utils.py,
    tests/test_exceptions.py,
    tests/test_lang_utils.py,
    tests/test_logging.py,
    tests/test_progress.py,
    tests/test_cli.py
  </files>

  <read_first>
    - tests/test_eq_odoo_connection.py (primary source for test_connection.py merge)
    - tests/test_odoo_connection.py (base tests to absorb into test_connection.py; identify unique tests vs duplicate coverage)
    - tests/test_eq_report.py (primary source for test_report.py merge)
    - tests/test_report.py (CURRENT — base tests to absorb; overwrite with merged version)
    - tests/test_eq_utils.py (primary source for test_utils.py merge)
    - tests/test_helper_utils.py (base tests to absorb into test_utils.py)
    - tests/test_exceptions.py (update import path only)
    - tests/test_lang_utils.py (update import path only)
    - tests/test_logging.py (update import path only)
    - tests/test_progress.py (cut dead-API tests; keep TestProgressBarFunction)
    - tests/test_cli.py (update import path if CLI renamed)
  </read_first>

  <action>
    Process each test file in order. After all merges, run the full suite once and confirm ~348 passing.

    test_connection.py (MERGE — replaces/creates):
      - Start with test_eq_odoo_connection.py content as the primary source
      - Import update: "from odoo_fast_report_mapper.eq_odoo_connection import EqOdooConnection" → "from odoo_fast_report_mapper._connection import OdooConnection"
      - Update _make_connection helper: patch target "odoo_report_helper.odoo_connection.utils.prepare_connection" → "odoo_fast_report_mapper._utils.prepare_connection"; OdooConnection(...) constructor call
      - Absorb unique tests from test_odoo_connection.py: read each test class/function, include only those that test behavior not already covered by the Eq-version tests. For base-only methods with no Eq override (__repr__, __str__, login, _get_fast_report_ids, check_module), include base tests if they exist and cover unique paths.
      - Update any remaining EqOdooConnection → OdooConnection references

    test_report.py (MERGE — overwrite existing):
      - Start with test_eq_report.py content as the primary source
      - Import update: "from odoo_fast_report_mapper.eq_report import EqReport" → "from odoo_fast_report_mapper._report import Report"
      - Update all EqReport(...) → Report(...)
      - Absorb unique tests from current test_report.py (base tests for add_fields, add_calculated_fields, add_dependencies if present)

    test_utils.py (MERGE — replaces/creates):
      - Start with test_eq_utils.py content as the primary source
      - Import update: all "from odoo_fast_report_mapper.eq_utils import ..." → "from odoo_fast_report_mapper._utils import ..."
      - Import update: "import odoo_report_helper.utils as utils" → "from odoo_fast_report_mapper._utils import prepare_connection, self_clean, parse_yaml_folder"
      - Absorb unique tests from test_helper_utils.py for prepare_connection, self_clean, parse_yaml_folder

    test_exceptions.py (UPDATE — import path only):
      - "from odoo_report_helper.exceptions import ..." → "from odoo_fast_report_mapper import OdooConnectionError, PathDoesNotExistError, PathDoesNotExitError"

    test_lang_utils.py (UPDATE — import path only):
      - "from odoo_fast_report_mapper.lang_utils import ..." → "from odoo_fast_report_mapper._lang_utils import ..."

    test_logging.py (UPDATE — import path only):
      - "from odoo_fast_report_mapper.logging_config import ..." → "from odoo_fast_report_mapper._logging import ..."

    test_progress.py (CUT — remove dead API tests):
      - Delete TestProgressBar class (lines 27-113): 11 test functions
      - Delete TestCreateProgressBar class (lines 152-188): 4 test functions
      - Delete TestReportProgress class (lines 195-249): 6 test functions
      - Update import: "from odoo_fast_report_mapper.progress import progress_bar" → "from odoo_fast_report_mapper._progress import progress_bar"
      - Update any patch call: "odoo_fast_report_mapper.progress.sys.stdout" → "odoo_fast_report_mapper._progress.sys.stdout"
      - Keep TestProgressBarFunction intact (4 tests)
      - Total file: ~30 LOC (per D-12)

    test_cli.py (UPDATE — import path only):
      - Update import: "from odoo_fast_report_mapper.odoo_fast_report_mapper import ..." → "from odoo_fast_report_mapper._cli import ..."
      - Any patch target referencing "odoo_fast_report_mapper.odoo_fast_report_mapper.*" → "odoo_fast_report_mapper._cli.*"

    After all 9 files are updated:
      uv run pytest tests/test_connection.py -v (isolation test — must pass standalone)
      uv run pytest -q --tb=short (full suite count)
  </action>

  <verify>
    <automated>uv run pytest tests/test_connection.py -v 2>&1 | tail -5</automated>
    Expect: all tests in test_connection.py pass in isolation (Success Criterion #1)
    <automated>uv run pytest -q --tb=short 2>&1 | tail -5</automated>
    Expect: ~348 passed (369 minus 21 deleted dead-API tests); 0 failures
    <automated>grep -rn "odoo_report_helper\|eq_odoo_connection\|eq_report\|eq_utils\|logging_config\|lang_utils" tests/ --include="*.py" | grep -v "test_odoo_connection.py\|test_eq_" | wc -l</automated>
    Expect: 0 (no old import paths in updated test files; old test files still exist but are no longer collected)
    Note: test_odoo_connection.py and test_eq_* files still exist at this point but tests/conftest.py now uses new paths
  </verify>

  <done>
    - tests/test_connection.py passes standalone: uv run pytest tests/test_connection.py -v exits 0
    - Full suite: ~348 tests pass (exactly 369 minus 21)
    - TestProgressBar, TestCreateProgressBar, TestReportProgress are absent from test_progress.py
    - test_progress.py is ~30 LOC with only TestProgressBarFunction
    - No odoo_report_helper import references in the 8 updated test files
  </done>
</task>

<task type="auto">
  <name>Task 4: Delete old source files and odoo_report_helper/, then final verification</name>
  <files>
    (deletes: odoo_report_helper/, odoo_fast_report_mapper/eq_odoo_connection.py,
     odoo_fast_report_mapper/eq_report.py, odoo_fast_report_mapper/eq_utils.py,
     odoo_fast_report_mapper/lang_utils.py, odoo_fast_report_mapper/logging_config.py,
     odoo_fast_report_mapper/progress.py, odoo_fast_report_mapper/odoo_fast_report_mapper.py)
    .planning/phases/01-package-consolidation/coverage-post.txt
    .planning/phases/01-package-consolidation/coverage-post.json
  </files>

  <read_first>
    - tests/test_connection.py (just merged — confirm it does NOT import from old eq_* paths before deleting)
    - tests/test_utils.py (confirm it does NOT import from odoo_report_helper or eq_utils before deleting)
    - tests/conftest.py (confirm zero odoo_report_helper references before deleting)
  </read_first>

  <action>
    PRECONDITION: uv run pytest -q --tb=short must exit 0 before proceeding with deletions.
    If any test fails, stop and fix before deleting files.

    Step 1 — Delete old source files (git rm so they are tracked):
      git rm odoo_report_helper/__init__.py
      git rm odoo_report_helper/odoo_connection.py
      git rm odoo_report_helper/report.py
      git rm odoo_report_helper/utils.py
      git rm odoo_report_helper/exceptions.py
      git rm odoo_fast_report_mapper/eq_odoo_connection.py
      git rm odoo_fast_report_mapper/eq_report.py
      git rm odoo_fast_report_mapper/eq_utils.py
      git rm odoo_fast_report_mapper/lang_utils.py
      git rm odoo_fast_report_mapper/logging_config.py
      git rm odoo_fast_report_mapper/progress.py
      git rm odoo_fast_report_mapper/odoo_fast_report_mapper.py

    Also delete old test files (they have been superseded by merged versions):
      git rm tests/test_odoo_connection.py
      git rm tests/test_eq_odoo_connection.py
      git rm tests/test_eq_report.py
      git rm tests/test_helper_utils.py
      git rm tests/test_eq_utils.py

    Step 2 — Run full test suite to confirm deletions did not break anything:
      uv run pytest -q --tb=short
    Expect: ~348 tests pass.

    Step 3 — Run ruff check:
      uv run ruff check .
      uv run ruff format --check .
    Fix any import-order or unused-import issues introduced by the merge.

    Step 4 — Run uv build to confirm pyproject.toml change is correct:
      uv build
    Expect: wheel + sdist produced without errors.

    Step 5 — Run success criteria checks:
      python -c "import odoo_report_helper" 2>&1 | grep -c "ModuleNotFoundError"
      # Expect: 1 (Success Criterion #2)

      python -c "from odoo_fast_report_mapper import OdooConnection; print('PASS')"
      # Expect: exits 0, prints PASS (Success Criterion #3)

      python -c "from odoo_fast_report_mapper._progress import ProgressBar" 2>&1 | grep -c "ImportError\|cannot import"
      # Expect: 1 (Success Criterion #4)

      uv run pytest tests/test_connection.py -v 2>&1 | tail -3
      # Expect: all pass, no import errors (Success Criterion #1)

    Step 6 — Generate post-Phase-1 coverage and diff:
      uv run pytest --cov --cov-report=term-missing --cov-report=json -q 2>&1 | tee .planning/phases/01-package-consolidation/coverage-post.txt
      cp coverage.json .planning/phases/01-package-consolidation/coverage-post.json

    Coverage diff analysis:
      Compare coverage-baseline.txt vs coverage-post.txt.
      Acceptable coverage drops (per D-13):
        - odoo_report_helper/* modules: 0% expected (package deleted)
        - odoo_fast_report_mapper/progress.py: 0% expected (file deleted; ProgressBar/create_progress_bar/ReportProgress removed)
      Unacceptable coverage drops (must investigate):
        - odoo_fast_report_mapper/_connection.py lower than eq_odoo_connection.py was
        - odoo_fast_report_mapper/_report.py lower than eq_report.py was
        - odoo_fast_report_mapper/_utils.py lower than eq_utils.py was
      The fail_under = 60 threshold must still pass.

    Step 7 — Commit everything:
      git add -A
      git commit -m "[CHG] refactor(01): switch imports to private submodules, merge tests, delete odoo_report_helper/ and old eq_* files — CONS-01/02/03/04 DEAD-01/02/03"
  </action>

  <verify>
    <automated>python -c "import odoo_report_helper" 2>&1 | grep -c "ModuleNotFoundError"</automated>
    Expect: 1 — SC-2 (odoo_report_helper is gone)
    <automated>python -c "from odoo_fast_report_mapper import OdooConnection; print('PASS')"</automated>
    Expect: exits 0 — SC-3
    <automated>python -c "from odoo_fast_report_mapper._progress import ProgressBar" 2>&1 | grep -c "ImportError\|cannot import"</automated>
    Expect: 1 — SC-4
    <automated>uv run pytest tests/test_connection.py -v --tb=short 2>&1 | tail -3</automated>
    Expect: all tests pass in isolation — SC-1
    <automated>uv run pytest -q --tb=short 2>&1 | tail -3</automated>
    Expect: ~348 passed — SC-5
    <automated>odoo-fast-report-mapper --help 2>&1 | head -1</automated>
    Expect: Usage line (exits 0)
    <automated>odoo-fr-mapper --help 2>&1 | head -1</automated>
    Expect: Usage line (exits 0)
    <automated>grep -c "odoo_report_helper" pyproject.toml</automated>
    Expect: 0
    <automated>ls odoo_report_helper/ 2>&1 | grep -c "No such file"</automated>
    Expect: 1 (directory gone)
    <automated>uv run ruff check . 2>&1 | tail -3</automated>
    Expect: exits 0 (no linting errors)
    <automated>uv build 2>&1 | tail -5</automated>
    Expect: Successfully built wheel and sdist
    <automated>uv run pytest --cov --cov-report=term-missing -q 2>&1 | grep "TOTAL\|fail_under"</automated>
    Expect: Total coverage >= 60%
  </verify>

  <done>
    All 5 Phase 1 success criteria pass:
    SC-1: uv run pytest tests/test_connection.py exits 0 standalone
    SC-2: python -c "import odoo_report_helper" raises ModuleNotFoundError
    SC-3: python -c "from odoo_fast_report_mapper import OdooConnection" exits 0
    SC-4: python -c "from odoo_fast_report_mapper._progress import ProgressBar" raises ImportError
    SC-5: uv run pytest -q exits 0 with ~348 tests passing

    Additional:
    - odoo_report_helper/ directory no longer exists
    - pyproject.toml zero odoo_report_helper references
    - Both CLI entry points work
    - ruff check passes
    - uv build produces wheel + sdist
    - Coverage baseline diff shows loss only for deleted dead-code modules
    - Git commit created with all changes staged
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixtures → production code | conftest.py patch paths must reference exact import locations of ODOO and prepare_connection |
| CLI entry → package | pyproject.toml scripts reference must match actual module._function path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | conftest.py mock_odoorpc patch path | mitigate | Explicit verify step: grep -c "odoo_fast_report_mapper._utils.ODOO" tests/conftest.py must return >= 1 |
| T-05-02 | Denial of Service | uv build after pyproject.toml change | mitigate | Run uv build as explicit verification step before commit |
| T-05-03 | Elevation of Privilege | git rm on old test files | accept | git rm is tracked; recoverable via git checkout if incorrect file removed |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | uv pip install -e . is reinstall of current project, not new dependency |
</threat_model>

<verification>
After all tasks complete, run in order:

1. python -c "import odoo_report_helper" — must raise ModuleNotFoundError (SC-2)
2. python -c "from odoo_fast_report_mapper import OdooConnection; print('PASS')" — must print PASS (SC-3)
3. python -c "from odoo_fast_report_mapper._progress import ProgressBar" — must raise ImportError (SC-4)
4. uv run pytest tests/test_connection.py -v — must pass standalone (SC-1)
5. uv run pytest -q — must show ~348 passed (SC-5)
6. odoo-fast-report-mapper --help — must exit 0
7. odoo-fr-mapper --help — must exit 0
8. grep -c "odoo_report_helper" pyproject.toml — must output 0
9. uv run ruff check . — must exit 0
10. uv build — must produce wheel + sdist
11. Coverage total >= 60% (fail_under passes)
12. Coverage diff: loss only for odoo_report_helper/* and progress.py dead symbols
</verification>

<success_criteria>
All 5 ROADMAP.md Success Criteria for Phase 1 pass. The Phase 1 consolidation is complete and the codebase is a single package.
</success_criteria>

<output>
Create .planning/phases/01-package-consolidation/01-05-SUMMARY.md when done
</output>
