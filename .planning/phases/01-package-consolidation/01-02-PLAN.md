---
phase: 01-package-consolidation
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - odoo_fast_report_mapper/_exceptions.py
  - odoo_fast_report_mapper/_lang_utils.py
  - odoo_fast_report_mapper/_logging.py
  - odoo_fast_report_mapper/_yaml_dumper.py
  - odoo_fast_report_mapper/_progress.py
autonomous: true
requirements:
  - CONS-01
  - CONS-03
  - DEAD-01
  - DEAD-02
  - DEAD-03

must_haves:
  truths:
    - "Five new private submodule files exist in odoo_fast_report_mapper/"
    - "Old source files (lang_utils.py, logging_config.py, progress.py) are still present — no deletions yet"
    - "Tests still pass against old import paths (nothing has been switched yet)"
    - "_progress.py contains only progress_bar() — ProgressBar, create_progress_bar, ReportProgress are absent"
  artifacts:
    - path: "odoo_fast_report_mapper/_exceptions.py"
      provides: "OdooConnectionError, PathDoesNotExistError, PathDoesNotExitError"
    - path: "odoo_fast_report_mapper/_lang_utils.py"
      provides: "LEGACY_LANG_MAP, normalize_language_code, normalize_name_dict, get_primary_lang, build_name_search_domain, resolve_attachment_value"
    - path: "odoo_fast_report_mapper/_logging.py"
      provides: "LogColors, ColoredFormatter, LoggerManager, get_logger, setup_logging, set_log_level, get_log_file_path, enable_debug_logging, enable_verbose_logging, enable_quiet_logging, _manager"
    - path: "odoo_fast_report_mapper/_yaml_dumper.py"
      provides: "YAMLDumper(yaml.Dumper)"
    - path: "odoo_fast_report_mapper/_progress.py"
      provides: "progress_bar() context manager (38 LOC) — dead APIs absent"
  key_links:
    - from: "odoo_fast_report_mapper/_progress.py"
      to: "odoo_fast_report_mapper/_connection.py"
      via: "from ._progress import progress_bar (created in plan 03)"
      pattern: "from \\._progress import progress_bar"
---

<objective>
Create five new private submodule files in odoo_fast_report_mapper/. Three are exact renames (copy content verbatim), one is an extraction (YAMLDumper from eq_odoo_connection.py), one is a partial copy with dead-code deletion (_progress.py).

Purpose: These are the leaf utilities with no intra-package dependencies that introduce changes. Creating them first, while leaving the old files in place, ensures the existing test suite remains green throughout this plan. The old files are NOT deleted here.

Output: Five new _*.py files. Old files unchanged. Test suite still green.
</objective>

<execution_context>
@/Users/picard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/picard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-package-consolidation/01-CONTEXT.md
@.planning/phases/01-package-consolidation/01-RESEARCH.md
@.planning/phases/01-package-consolidation/01-PATTERNS.md
</context>

<interfaces>
From odoo_fast_report_mapper/eq_odoo_connection.py lines 20-24 (YAMLDumper to extract):
  class YAMLDumper(yaml.Dumper):
      def increase_indent(self, flow=False, indentless=False):
          return super().increase_indent(flow, False)
  # NOTE: super() here calls yaml.Dumper — NOT eliminated by the merge

From odoo_fast_report_mapper/progress.py (surviving section lines 98-135):
  def progress_bar(iterable, desc, unit, total, disable, colour) -> Iterable[Any]:
      # wraps tqdm — full function body copied verbatim

Dead symbols to NOT include in _progress.py:
  - ProgressBar class (lines 17-95)
  - create_progress_bar() factory (lines 137-159)
  - ReportProgress class (lines 162-208)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create _exceptions.py, _lang_utils.py, _logging.py (exact copies)</name>
  <files>
    odoo_fast_report_mapper/_exceptions.py,
    odoo_fast_report_mapper/_lang_utils.py,
    odoo_fast_report_mapper/_logging.py
  </files>

  <read_first>
    - odoo_report_helper/exceptions.py (source for _exceptions.py — copy verbatim)
    - odoo_fast_report_mapper/lang_utils.py (source for _lang_utils.py — copy verbatim)
    - odoo_fast_report_mapper/logging_config.py (source for _logging.py — copy verbatim)
  </read_first>

  <action>
    Create three new files by copying source content verbatim. No logic changes.

    _exceptions.py:
      - Copy entire content of odoo_report_helper/exceptions.py
      - Three symbols: OdooConnectionError, PathDoesNotExistError, PathDoesNotExitError (alias)
      - Copyright header must be present

    _lang_utils.py:
      - Copy entire content of odoo_fast_report_mapper/lang_utils.py
      - Six symbols: LEGACY_LANG_MAP, normalize_language_code, normalize_name_dict, get_primary_lang, build_name_search_domain, resolve_attachment_value
      - Content is identical; only the filename changes

    _logging.py:
      - Copy entire content of odoo_fast_report_mapper/logging_config.py
      - All symbols: LogColors, ColoredFormatter, LoggerManager, module-level functions, _manager
      - The LoggerManager singleton internals are NOT changed (fragile area — deferred per CONTEXT.md)
      - Content is identical; only the filename changes

    Do NOT delete or modify the original source files (odoo_report_helper/exceptions.py, lang_utils.py, logging_config.py). Both old and new files coexist after this task.
  </action>

  <verify>
    <automated>python -c "from odoo_fast_report_mapper._exceptions import OdooConnectionError, PathDoesNotExistError, PathDoesNotExitError; print('OK')"</automated>
    <automated>python -c "from odoo_fast_report_mapper._lang_utils import build_name_search_domain, normalize_language_code; print('OK')"</automated>
    <automated>python -c "from odoo_fast_report_mapper._logging import get_logger, LoggerManager; print('OK')"</automated>
    <automated>uv run pytest -q --tb=short 2>&1 | tail -3</automated>
    Expect: all tests still pass (old import paths still work; no imports switched)
  </verify>

  <done>
    - odoo_fast_report_mapper/_exceptions.py exists with OdooConnectionError, PathDoesNotExistError, PathDoesNotExitError
    - odoo_fast_report_mapper/_lang_utils.py exists with all 6 symbols from lang_utils.py
    - odoo_fast_report_mapper/_logging.py exists with all symbols from logging_config.py
    - All three new files are importable without error
    - Existing test suite still passes (old files unchanged)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create _yaml_dumper.py (extracted) and _progress.py (partial copy, dead APIs removed)</name>
  <files>
    odoo_fast_report_mapper/_yaml_dumper.py,
    odoo_fast_report_mapper/_progress.py
  </files>

  <read_first>
    - odoo_fast_report_mapper/eq_odoo_connection.py lines 1-30 (YAMLDumper class at lines 20-24 — extract only this class)
    - odoo_fast_report_mapper/progress.py (full file — identify surviving section lines 98-135; confirm dead sections lines 17-95, 137-159, 162-208)
  </read_first>

  <action>
    Create _yaml_dumper.py:
      - Add copyright header (# Copyright 2014-now Equitania Software GmbH - Pforzheim - Germany / # License AGPL-3.0 or later)
      - Add module docstring: """Custom YAML dumper with consistent indentation formatting."""
      - Add: import yaml
      - Copy the YAMLDumper class verbatim from eq_odoo_connection.py lines 20-24
      - The super().increase_indent(flow, False) call MUST be preserved — it calls yaml.Dumper, not an intra-package base
      - Do NOT include any other content from eq_odoo_connection.py
      - MyDumper.py does NOT exist in the codebase (verified in RESEARCH.md) — there is nothing to rename

    Create _progress.py:
      - Add copyright header
      - Add module docstring: """Progress bar context manager for long-running operations. Internal use only."""
      - Copy imports from progress.py lines 1-15: sys, Iterable, Any, tqdm — omit any imports used only by deleted classes (e.g., if Any is only for ProgressBar, keep it only if progress_bar uses it)
      - Copy the progress_bar() function verbatim from progress.py lines 98-135 (the contextmanager function)
      - Do NOT include: ProgressBar class (lines 17-95), create_progress_bar factory (lines 137-159), ReportProgress class (lines 162-208)
      - The old progress.py is NOT deleted or modified

    Behavior-freeze guard: progress_bar() body is copied character-for-character. No "improvements".
  </action>

  <verify>
    <automated>python -c "from odoo_fast_report_mapper._yaml_dumper import YAMLDumper; import yaml; d = YAMLDumper; print('OK')"</automated>
    <automated>python -c "from odoo_fast_report_mapper._progress import progress_bar; print('OK')"</automated>
    <automated>python -c "from odoo_fast_report_mapper._progress import ProgressBar" 2>&1 | grep -c "ImportError\|cannot import"</automated>
    Expect: 1 (ProgressBar must NOT be importable from _progress)
    <automated>python -c "from odoo_fast_report_mapper._progress import ReportProgress" 2>&1 | grep -c "ImportError\|cannot import"</automated>
    Expect: 1
    <automated>python -c "from odoo_fast_report_mapper._progress import create_progress_bar" 2>&1 | grep -c "ImportError\|cannot import"</automated>
    Expect: 1
    <automated>uv run pytest -q --tb=short 2>&1 | tail -3</automated>
    Expect: all tests still pass (old progress.py still in place; no imports switched)
  </verify>

  <done>
    - odoo_fast_report_mapper/_yaml_dumper.py exists with YAMLDumper(yaml.Dumper) and super().increase_indent() preserved
    - odoo_fast_report_mapper/_progress.py exists with only progress_bar() — no ProgressBar, no create_progress_bar, no ReportProgress
    - Both files importable without error
    - python -c "from odoo_fast_report_mapper._progress import ProgressBar" raises ImportError
    - Full test suite still green (old progress.py unchanged)
  </done>
</task>

<task type="auto">
  <name>Task 3: Commit Wave 2 leaf utilities</name>
  <files>
    odoo_fast_report_mapper/_exceptions.py,
    odoo_fast_report_mapper/_lang_utils.py,
    odoo_fast_report_mapper/_logging.py,
    odoo_fast_report_mapper/_yaml_dumper.py,
    odoo_fast_report_mapper/_progress.py
  </files>

  <read_first>
    None — verify all five new files exist before staging.
  </read_first>

  <action>
    Stage only the five new files and commit. Do not stage any old/modified files.

    git add odoo_fast_report_mapper/_exceptions.py
    git add odoo_fast_report_mapper/_lang_utils.py
    git add odoo_fast_report_mapper/_logging.py
    git add odoo_fast_report_mapper/_yaml_dumper.py
    git add odoo_fast_report_mapper/_progress.py
    git commit -m "[ADD] refactor(01): create private submodules — exceptions, lang_utils, logging, yaml_dumper, progress (DEAD-01/02/03)"
  </action>

  <verify>
    <automated>git log --oneline -1</automated>
    Expect: commit message starts with "[ADD] refactor(01):"
    <automated>git status</automated>
    Expect: working tree clean (no unstaged or uncommitted changes)
  </verify>

  <done>
    - All five _*.py files committed
    - git status is clean
    - Test suite still green (not run again here; covered in prior tasks)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source file → new file | File content is copied verbatim — no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | _logging.py LoggerManager | accept | Singleton state deferred per CONTEXT.md — copying verbatim preserves existing behavior |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
After all tasks complete:
- Five new files exist in odoo_fast_report_mapper/: _exceptions.py, _lang_utils.py, _logging.py, _yaml_dumper.py, _progress.py
- uv run pytest -q exits 0 (old files still present; no import paths changed)
- python -c "from odoo_fast_report_mapper._progress import ProgressBar" raises ImportError
- git status is clean
</verification>

<success_criteria>
- All five private submodule leaf files created and importable
- Dead progress API (ProgressBar, create_progress_bar, ReportProgress) absent from _progress.py
- Full test suite still green (old files coexist)
- Wave 2 committed
</success_criteria>

<output>
Create .planning/phases/01-package-consolidation/01-02-SUMMARY.md when done
</output>
