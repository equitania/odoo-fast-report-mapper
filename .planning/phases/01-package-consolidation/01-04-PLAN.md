---
phase: 01-package-consolidation
plan: 04
title: "Wave 4 — Merge Report into _report.py (standalone, no super())"
status: pending
type: execute
wave: 4
depends_on:
  - "01-03"
files_modified:
  - odoo_fast_report_mapper/_report.py
autonomous: true
requirements:
  - CONS-01
  - CONS-03

must_haves:
  truths:
    - "odoo_fast_report_mapper/_report.py exists with class Report (not EqReport)"
    - "_report.py has no parent class (EqReport(Report) hierarchy is gone)"
    - "_report.py includes add_fields, add_calculated_fields, add_dependencies from the base class"
    - "add_calculated_fields body is verbatim (BUG-02 preserved for Phase 1.1)"
    - "Full test suite still passes (old eq_report.py still in place)"
  artifacts:
    - path: "odoo_fast_report_mapper/_report.py"
      provides: "class Report — merged from EqReport + base Report"
      contains: "class Report:"
  key_links:
    - from: "odoo_fast_report_mapper/_report.py"
      to: "odoo_fast_report_mapper/_utils.py"
      via: "from ._utils import self_clean"
      pattern: "from \\._utils import self_clean"
    - from: "odoo_fast_report_mapper/_report.py"
      to: "odoo_fast_report_mapper/_lang_utils.py"
      via: "from ._lang_utils import get_primary_lang"
      pattern: "from \\._lang_utils import get_primary_lang"
---

<objective>
Create _report.py — the merged Report class. This is Wave 4 (sequential after Wave 3) because _connection.py imports Report and _utils.py references Report. Creating _report.py after _connection.py / _utils.py avoids any import ordering concern.

Purpose: Completes the creation of all new private submodule production files. After this plan, every new _*.py file exists and is importable.

Output: _report.py created. Old eq_report.py still in place. Test suite green.
</objective>

<execution_context>
@/Users/picard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/picard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-package-consolidation/01-CONTEXT.md
@.planning/phases/01-package-consolidation/01-RESEARCH.md
@.planning/phases/01-package-consolidation/01-PATTERNS.md
</context>

<interfaces>
From odoo_fast_report_mapper/eq_report.py (primary source — Eq-version is canonical):
  class EqReport(Report):  → becomes class Report: (no parent)
  __init__ does NOT call super().__init__() — this is intentional (anti-pattern noted in ARCHITECTURE.md)
  __init__ sets all attributes directly (eq_report.py lines 10-51)
  self_ensure(self) — Eq-canonical, copy verbatim
  ensure_data_for_yaml(self) — Eq-canonical, copy verbatim

From odoo_report_helper/report.py (base methods with no Eq override — INLINE into merged Report):
  add_fields(self, field_dict: dict)  (lines 53-65)
  add_calculated_fields(self, field_dict)  (lines 67-79) — VERBATIM including BUG-02
  add_dependencies(self, dependency_list: list)  (lines 81-87)

  Note: add_fields and add_calculated_fields call self_clean — currently via "utils.self_clean()".
  After merge: from ._utils import self_clean (direct call)

Target imports for _report.py:
  from ._lang_utils import get_primary_lang
  from ._utils import self_clean

Cross-package imports to REMOVE from eq_report.py:
  from odoo_report_helper.report import Report   → DELETE (no parent class)
  from .lang_utils import get_primary_lang        → from ._lang_utils import get_primary_lang
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create _report.py (merged Report class)</name>
  <files>odoo_fast_report_mapper/_report.py</files>

  <read_first>
    - odoo_fast_report_mapper/eq_report.py (full file — primary source for __init__, self_ensure, ensure_data_for_yaml)
    - odoo_report_helper/report.py (full file — source for add_fields, add_calculated_fields, add_dependencies)
  </read_first>

  <action>
    Create odoo_fast_report_mapper/_report.py as the merged standalone Report class.

    Structure:
    1. Copyright header (# Copyright 2014-now Equitania Software GmbH - Pforzheim - Germany / # License AGPL-3.0 or later)
    2. Module docstring: """Report data container for FastReport YAML-to-Odoo mapping."""
    3. Imports:
       from ._lang_utils import get_primary_lang
       from ._utils import self_clean
    4. class Report:  (no parent — standalone class)
    5. __init__ — copy from eq_report.py verbatim (the Eq-version). Do NOT add super().__init__() — EqReport intentionally never called it. Add a comment: "# Note: this class has no base class after consolidation. No super().__init__() is needed."
    6. self_ensure — copy from eq_report.py verbatim
    7. ensure_data_for_yaml — copy from eq_report.py verbatim
    8. add_fields — copy from odoo_report_helper/report.py lines 53-65 verbatim.
       Update: calls to utils.self_clean(...) → self_clean(...) (local import)
    9. add_calculated_fields — copy from odoo_report_helper/report.py lines 67-79 VERBATIM.
       BUG-02 body (list(dict.fromkeys(value)) iterates only keys) must be preserved exactly.
       Update: calls to utils.self_clean(...) → self_clean(...)
    10. add_dependencies — copy from odoo_report_helper/report.py lines 81-87 verbatim.

    Replace only:
       - "from odoo_report_helper.report import Report" → DELETE
       - "class EqReport(Report)" → "class Report:"
       - "from .lang_utils import get_primary_lang" → "from ._lang_utils import get_primary_lang"
       - "utils.self_clean(...)" → "self_clean(...)"

    Do NOT delete eq_report.py or odoo_report_helper/report.py.
  </action>

  <verify>
    <automated>python -c "from odoo_fast_report_mapper._report import Report; print(Report)"</automated>
    Expect: exits 0, prints class reference
    <automated>grep -c "EqReport\|odoo_report_helper" odoo_fast_report_mapper/_report.py</automated>
    Expect: 0
    <automated>grep -c "class Report:" odoo_fast_report_mapper/_report.py</automated>
    Expect: 1
    <automated>grep -c "super().__init__" odoo_fast_report_mapper/_report.py</automated>
    Expect: 0 (EqReport never called super; merged class preserves this — intentional design)
    <automated>grep -c "def add_fields\|def add_calculated_fields\|def add_dependencies" odoo_fast_report_mapper/_report.py</automated>
    Expect: 3 (all three base-only methods must be present)
    <automated>uv run pytest -q --tb=short 2>&1 | tail -3</automated>
    Expect: all tests still pass
  </verify>

  <done>
    - odoo_fast_report_mapper/_report.py exists with standalone class Report
    - No EqReport, odoo_report_helper, or super().__init__() references
    - add_fields, add_calculated_fields, add_dependencies present (with verbatim BUG-02 body)
    - python -c "from odoo_fast_report_mapper._report import Report" exits 0
    - Full test suite still green
  </done>
</task>

<task type="auto">
  <name>Task 2: Commit Wave 4 merged report class</name>
  <files>odoo_fast_report_mapper/_report.py</files>

  <read_first>
    None — verify file exists before committing.
  </read_first>

  <action>
    Stage only _report.py and commit.

    git add odoo_fast_report_mapper/_report.py
    git commit -m "[ADD] refactor(01): create merged _report.py (Report class) — CONS-01/03"
  </action>

  <verify>
    <automated>git log --oneline -1</automated>
    Expect: commit message starts with "[ADD] refactor(01):"
    <automated>git status</automated>
    Expect: working tree clean
  </verify>

  <done>
    - _report.py committed
    - git status clean
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eq_report.py + odoo_report_helper/report.py → _report.py | Method body transcription — no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | add_calculated_fields BUG-02 | accept | Verbatim copy preserves existing (buggy) behavior; Phase 1.1 adds the fix |
| T-04-02 | Tampering | Report.__init__ no super() | accept | Intentional design — documented with comment in code |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
After both tasks complete:
- python -c "from odoo_fast_report_mapper._report import Report" exits 0
- grep -c "EqReport\|odoo_report_helper" odoo_fast_report_mapper/_report.py outputs 0
- grep -c "def add_fields\|def add_calculated_fields\|def add_dependencies" odoo_fast_report_mapper/_report.py outputs 3
- uv run pytest -q exits 0 (all tests green — old eq_report.py still present)
- git status clean
</verification>

<success_criteria>
- _report.py: merged Report class, standalone (no parent), all methods from both Eq and base versions present
- add_calculated_fields verbatim including BUG-02 body
- Test suite still green with old files coexisting
</success_criteria>

<output>
Create .planning/phases/01-package-consolidation/01-04-SUMMARY.md when done
</output>
