---
phase: 01-package-consolidation
plan: 03
title: "Wave 3 — Merge OdooConnection + Create _utils.py (breaks circular import)"
status: pending
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - odoo_fast_report_mapper/_connection.py
  - odoo_fast_report_mapper/_utils.py
autonomous: true
requirements:
  - CONS-01
  - CONS-02
  - CONS-03
  - CONS-04

must_haves:
  truths:
    - "odoo_fast_report_mapper/_connection.py exists with class OdooConnection (not EqOdooConnection)"
    - "The merged constructor has no super() call — all attributes set explicitly"
    - "odoo_fast_report_mapper/_utils.py exists with prepare_connection, self_clean, parse_yaml_folder and all factory functions"
    - "_connection.py imports only from relative _* submodules — no odoo_report_helper references"
    - "The circular import is eliminated — _connection.py has no upward cross-package dependency"
    - "Full test suite still passes (old eq_odoo_connection.py and eq_utils.py still in place)"
  artifacts:
    - path: "odoo_fast_report_mapper/_connection.py"
      provides: "class OdooConnection — merged from EqOdooConnection + base OdooConnection"
      contains: "class OdooConnection"
    - path: "odoo_fast_report_mapper/_utils.py"
      provides: "create_connection_from_env, prepare_connection, self_clean, parse_yaml_folder, all eq_utils functions"
  key_links:
    - from: "odoo_fast_report_mapper/_connection.py"
      to: "odoo_fast_report_mapper/_utils.py"
      via: "from ._utils import prepare_connection"
      pattern: "from \\._utils import prepare_connection"
    - from: "odoo_fast_report_mapper/_connection.py"
      to: "odoo_fast_report_mapper/_lang_utils.py"
      via: "from ._lang_utils import build_name_search_domain, get_primary_lang, resolve_attachment_value"
      pattern: "from \\._lang_utils import"
---

<objective>
Create the two most complex new files: _connection.py (merged OdooConnection class) and _utils.py (merged factory + helpers). These are Wave 3 because _connection.py imports from _utils.py, so _utils.py conceptually comes first within this plan.

Purpose: Eliminates the circular import (CONS-02) and the override-smell (CONS-03). After this plan, there is a fully working merged class but tests still run against the old eq_odoo_connection.py / eq_utils.py paths. Import switching happens in plan 05.

Output: _connection.py and _utils.py created. All old files remain. Test suite still green.
</objective>

<execution_context>
@/Users/picard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/picard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-package-consolidation/01-CONTEXT.md
@.planning/phases/01-package-consolidation/01-RESEARCH.md
@.planning/phases/01-package-consolidation/01-PATTERNS.md
</context>

<interfaces>
From odoo_report_helper/odoo_connection.py (base class — methods to INLINE into merged OdooConnection):
  __init__(url, port, username, password, database)   → merged into new explicit constructor
  __repr__  → copy verbatim
  __str__   → copy verbatim
  login()   → copy verbatim (replace exceptions.OdooConnectionError → OdooConnectionError)
  _get_fast_report_ids()  → copy verbatim
  check_module()  → copy verbatim
  map_reports()   → DELETE (EqOdooConnection overrides completely)
  set_calculated_fields() → DELETE (EqOdooConnection overrides completely)
  _search_report()        → DELETE (EqOdooConnection overrides completely)
  check_dependencies()    → DELETE (EqOdooConnection overrides completely)

From odoo_fast_report_mapper/eq_odoo_connection.py (Eq class — canonical for overridden methods):
  Class: EqOdooConnection(OdooConnection) → becomes OdooConnection (no parent)
  __init__(language, collect_yaml, disable_qweb, workflow, url, port, *args, **kwargs):
    currently calls super().__init__(url, port, *args, **kwargs) → REMOVE super() call
    *args passes username, password, database positionally to base — make them explicit params
  YAMLDumper: EXTRACT to _yaml_dumper.py (already done in plan 02)
  _search_report_v13: copy VERBATIM (fragile — zero edits per R-05)

Merged __init__ signature (per PATTERNS.md):
  def __init__(self, language, collect_yaml, disable_qweb, workflow, url, port,
               username, password, database, auth_method: str = "password")
  Sets: self.url, self.port, self.username, self.password, self.database,
        self.version = "", self.language, self.collect_yaml, self.disable_qweb,
        self.workflow, self.auth_method
  Then: self.connection = prepare_connection(url, port)  [wrapped in URLError → OdooConnectionError]

Target imports for _connection.py:
  import os, urllib.error
  from datetime import datetime
  from random import choice
  import yaml
  from odoorpc_toolbox import RPCError
  from ._exceptions import OdooConnectionError
  from ._lang_utils import build_name_search_domain, get_primary_lang, resolve_attachment_value
  from ._logging import get_logger
  from ._progress import progress_bar
  from ._utils import prepare_connection
  from ._yaml_dumper import YAMLDumper

From odoo_report_helper/utils.py (helper functions to ABSORB into _utils.py):
  prepare_connection(url, port)
  fire_all_functions(function_list)
  self_clean(input_dictionary) → copy VERBATIM (BUG-02 body — do NOT fix)
  parse_yaml(yaml_file)
  parse_yaml_folder_with_filenames(path)
  parse_yaml_folder(path)

From odoo_fast_report_mapper/eq_utils.py (primary source for _utils.py — 376 lines):
  All factory functions survive: create_connection_from_env, collect_all_reports,
  build_reports_from_yaml_objects, create_report_object_from_yaml_object,
  create_odoo_connection_from_yaml_object, list_yaml_reports, generate_env_template,
  convert_all_yaml_objects, collect_all_connections
  Constant: ENV_TEMPLATE

  create_connection_from_env return type: (OdooConnection, dotenv_path) — not EqOdooConnection
  Instantiation inside: OdooConnection(...) not eq_odoo_connection.EqOdooConnection(...)

Target imports for _utils.py (replacing cross-package imports):
  Old: import odoo_report_helper.exceptions as exceptions  → from ._exceptions import OdooConnectionError, PathDoesNotExistError
  Old: import odoo_report_helper.utils as utils            → inline helper functions directly
  Old: from . import eq_odoo_connection, eq_report         → from ._connection import OdooConnection; from ._report import Report
  Old: from .lang_utils import ...                         → from ._lang_utils import ...
  Old: from .logging_config import get_logger              → from ._logging import get_logger
  Note: _report.py is created in plan 04. _utils.py may import from ._report — if that causes
        an import error at this point, use a TYPE_CHECKING guard or defer the Report import.
        Safer: structure _utils.py to do "from ._report import Report" only where needed,
        or use a lazy import inside the factory function. Check if eq_utils.py imports eq_report
        at module level or inside a function.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create _utils.py (merged factory + helpers)</name>
  <files>odoo_fast_report_mapper/_utils.py</files>

  <read_first>
    - odoo_report_helper/utils.py (full file — absorb all 6 helper functions verbatim)
    - odoo_fast_report_mapper/eq_utils.py (full file — primary source; note import of eq_report at top vs inside function)
    - odoo_fast_report_mapper/eq_report.py lines 1-10 (check if EqReport is a module-level import in eq_utils.py or a function-level import — determines whether _report import in _utils.py is safe before _report.py exists)
  </read_first>

  <action>
    Create odoo_fast_report_mapper/_utils.py by merging odoo_report_helper/utils.py helpers into eq_utils.py as the primary structure.

    File structure:
    1. Copyright header + module docstring
    2. Standard library imports (os, pathlib, urllib, logging, etc.)
    3. Third-party imports (yaml, dotenv, odoorpc_toolbox.ODOO)
    4. Relative imports:
       - from ._exceptions import OdooConnectionError, PathDoesNotExistError
       - from ._lang_utils import normalize_language_code, normalize_name_dict
       - from ._logging import get_logger
       - If eq_utils.py imports eq_report at module level: use TYPE_CHECKING guard or lazy import inside factory function to avoid circular dependency before _report.py exists. Per RESEARCH.md, _utils.py must be importable for _connection.py to import prepare_connection — check eq_utils.py line-by-line.

    5. Helper functions from odoo_report_helper/utils.py (verbatim, behavior-freeze):
       - prepare_connection(url, port)
       - fire_all_functions(function_list)
       - self_clean(input_dictionary) — VERBATIM including BUG-02 body (list(dict.fromkeys(value)))
       - parse_yaml(yaml_file)
       - parse_yaml_folder_with_filenames(path)
       - parse_yaml_folder(path)

    6. All factory and utility functions from eq_utils.py:
       - ENV_TEMPLATE constant
       - generate_env_template, list_yaml_reports, convert_all_yaml_objects
       - create_report_object_from_yaml_object, create_odoo_connection_from_yaml_object
       - build_reports_from_yaml_objects, collect_all_reports, collect_all_connections
       - create_connection_from_env → return type tuple[OdooConnection, str]; instantiation uses OdooConnection(...)

    Replace all references:
       - exceptions.OdooConnectionError → OdooConnectionError
       - exceptions.PathDoesNotExistError → PathDoesNotExistError
       - utils.parse_yaml_folder(...) → parse_yaml_folder(...) (now local function)
       - utils.prepare_connection(...) → prepare_connection(...) (now local function)
       - utils.self_clean(...) → self_clean(...) (now local function)
       - eq_odoo_connection.EqOdooConnection(...) → OdooConnection(...)
       - lang_utils.normalize_language_code → from ._lang_utils import ... (already done in imports)
       - from .logging_config import → from ._logging import

    Do NOT delete eq_utils.py or odoo_report_helper/utils.py.
  </action>

  <verify>
    <automated>python -c "from odoo_fast_report_mapper._utils import prepare_connection, self_clean, parse_yaml_folder, create_connection_from_env; print('OK')"</automated>
    <automated>grep -c "odoo_report_helper" odoo_fast_report_mapper/_utils.py</automated>
    Expect: 0 (no odoo_report_helper references in the new file)
    <automated>grep -c "EqOdooConnection" odoo_fast_report_mapper/_utils.py</automated>
    Expect: 0
    <automated>uv run pytest -q --tb=short 2>&1 | tail -3</automated>
    Expect: all tests still pass (eq_utils.py still present; nothing switched yet)
  </verify>

  <done>
    - odoo_fast_report_mapper/_utils.py exists and imports without error
    - prepare_connection, self_clean, parse_yaml_folder importable from _utils
    - create_connection_from_env importable from _utils
    - No odoo_report_helper or EqOdooConnection references in the new file
    - Test suite still green
  </done>
</task>

<task type="auto">
  <name>Task 2: Create _connection.py (merged OdooConnection class)</name>
  <files>odoo_fast_report_mapper/_connection.py</files>

  <read_first>
    - odoo_fast_report_mapper/eq_odoo_connection.py (full file — primary source; 805 lines)
    - odoo_report_helper/odoo_connection.py (full file — base methods to inline: __repr__, __str__, login, _get_fast_report_ids, check_module)
    - odoo_fast_report_mapper/_utils.py (just created — confirm prepare_connection is available)
  </read_first>

  <action>
    Create odoo_fast_report_mapper/_connection.py as the merged, standalone OdooConnection class.

    Structure:
    1. Copyright header + module docstring
    2. Imports (per PATTERNS.md):
       import os
       import urllib.error
       from datetime import datetime
       from random import choice
       import yaml
       from odoorpc_toolbox import RPCError
       from ._exceptions import OdooConnectionError
       from ._lang_utils import build_name_search_domain, get_primary_lang, resolve_attachment_value
       from ._logging import get_logger
       from ._progress import progress_bar
       from ._utils import prepare_connection
       from ._yaml_dumper import YAMLDumper

    3. logger = get_logger(__name__)

    4. class OdooConnection:  (NOT EqOdooConnection, NOT EqOdooConnection(OdooConnection))

    5. Merged __init__ — explicit parameters, no super():
       def __init__(self, language, collect_yaml, disable_qweb, workflow, url, port,
                    username, password, database, auth_method: str = "password"):
         self.url = url
         self.port = port
         self.username = username
         self.password = password
         self.database = database
         self.version = ""
         self.language = language
         self.collect_yaml = collect_yaml
         self.disable_qweb = disable_qweb
         self.workflow = workflow
         self.auth_method = auth_method
         try:
             self.connection = prepare_connection(url, port)
         except urllib.error.URLError as ex:
             self.password = None
             raise OdooConnectionError(
                 "ERROR: Please check your parameters and your connection" + " " + str(ex)
             ) from ex

    6. Inline from base (verbatim, only updating exception class reference):
       __repr__, __str__, login (replace exceptions.OdooConnectionError → OdooConnectionError),
       _get_fast_report_ids, check_module

    7. All Eq-canonical methods from eq_odoo_connection.py (verbatim, behavior-freeze):
       map_reports, set_calculated_fields, _search_report, check_dependencies,
       authenticate, _is_odoo_version_at_least, and all other Eq methods

    8. _search_report_v13 — copy VERBATIM, zero edits (R-05 fragile method)

    9. add_field_to_dictionary — copy VERBATIM including unguarded model_id[0] (BUG-01, fixed in Phase 1.1)

    Critical import updates inside method bodies:
       - Any reference to eq_report.EqReport(...)  → import Report from ._report (lazy or at top)
       - If _report.py does not yet exist, use: from importlib import import_module or TYPE_CHECKING guard
       - Safest: check if _connection.py references EqReport inside methods. If so, add at top: from ._report import Report (will be created in plan 04; circular check: _report imports only from ._lang_utils and ._utils, not from ._connection — so no circular import)

    Remove entirely:
       - from odoo_report_helper.odoo_connection import OdooConnection
       - class EqOdooConnection(OdooConnection):
       - YAMLDumper class definition (extracted to _yaml_dumper.py in plan 02)
       - from . import eq_report (replace with from ._report import Report)
       - super().__init__(url, port, *args, **kwargs) call
       - Any reference to lang_utils.* (use ._lang_utils.* instead — already in imports)

    Do NOT delete eq_odoo_connection.py.
  </action>

  <verify>
    <automated>python -c "from odoo_fast_report_mapper._connection import OdooConnection; print(OdooConnection)"</automated>
    Expect: exits 0, prints class reference
    <automated>grep -c "EqOdooConnection\|odoo_report_helper" odoo_fast_report_mapper/_connection.py</automated>
    Expect: 0
    <automated>grep -c "class OdooConnection:" odoo_fast_report_mapper/_connection.py</automated>
    Expect: 1
    <automated>grep -c "super().__init__" odoo_fast_report_mapper/_connection.py</automated>
    Expect: 0 (the merged constructor has no super() call; YAMLDumper's super().increase_indent is in _yaml_dumper.py not here)
    <automated>uv run pytest -q --tb=short 2>&1 | tail -3</automated>
    Expect: all tests still pass
  </verify>

  <done>
    - odoo_fast_report_mapper/_connection.py exists with class OdooConnection (no inheritance from OdooConnection base)
    - Constructor takes (language, collect_yaml, disable_qweb, workflow, url, port, username, password, database, auth_method="password")
    - No references to EqOdooConnection, odoo_report_helper, or super().__init__ in the file
    - _search_report_v13 present verbatim
    - add_field_to_dictionary present verbatim (unguarded model_id[0] preserved for Phase 1.1)
    - python -c "from odoo_fast_report_mapper._connection import OdooConnection" exits 0
    - Full test suite still green
  </done>
</task>

<task type="auto">
  <name>Task 3: Commit Wave 3 merged connection and utils</name>
  <files>odoo_fast_report_mapper/_connection.py, odoo_fast_report_mapper/_utils.py</files>

  <read_first>
    None — verify files exist before committing.
  </read_first>

  <action>
    Stage only _connection.py and _utils.py and commit.

    git add odoo_fast_report_mapper/_connection.py
    git add odoo_fast_report_mapper/_utils.py
    git commit -m "[ADD] refactor(01): create merged _connection.py (OdooConnection) and _utils.py — CONS-01/02/03/04"
  </action>

  <verify>
    <automated>git log --oneline -1</automated>
    Expect: commit message starts with "[ADD] refactor(01):"
    <automated>git status</automated>
    Expect: working tree clean
  </verify>

  <done>
    - Both new files committed
    - git status clean
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eq_odoo_connection.py → _connection.py | Method body transcription — no external input |
| eq_utils.py + odoo_report_helper/utils.py → _utils.py | Merge — no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | _connection.py constructor | mitigate | Explicit parameter names in merged __init__ prevent silent positional-arg reordering (R-01 from RESEARCH.md) |
| T-03-02 | Tampering | BUG-01 in add_field_to_dictionary | accept | Verbatim copy preserves behavior; Phase 1.1 adds the guard |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
After all tasks complete:
- python -c "from odoo_fast_report_mapper._connection import OdooConnection; print('OK')" exits 0
- python -c "from odoo_fast_report_mapper._utils import create_connection_from_env, prepare_connection; print('OK')" exits 0
- grep -c "odoo_report_helper" odoo_fast_report_mapper/_connection.py outputs 0
- grep -c "odoo_report_helper" odoo_fast_report_mapper/_utils.py outputs 0
- uv run pytest -q exits 0 (all tests green — old files still present)
- git status clean
</verification>

<success_criteria>
- _connection.py: merged OdooConnection, no base-class inheritance, no super().__init__, all Eq-canonical methods present
- _utils.py: all helper functions from odoo_report_helper/utils.py plus all factory functions from eq_utils.py; no cross-package imports
- No circular import possible (_connection.py → _utils.py → no reverse dependency)
- Full test suite still green (old files coexist)
</success_criteria>

<output>
Create .planning/phases/01-package-consolidation/01-03-SUMMARY.md when done
</output>
