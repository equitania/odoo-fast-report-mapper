---
phase: 01-package-consolidation
plan: 01
title: "Wave 1 — Coverage Baseline + Pre-flight Green Test Run"
status: pending
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-package-consolidation/coverage-baseline.txt
  - .planning/phases/01-package-consolidation/coverage-baseline.json
autonomous: true
requirements:
  - CONS-01
  - CONS-02
  - CONS-03
  - CONS-04
  - DEAD-01
  - DEAD-02
  - DEAD-03

must_haves:
  truths:
    - "The pre-Phase-1 test suite runs clean (green) and the exact test count is recorded"
    - "Coverage baseline (term-missing + JSON) is committed to the planning directory before any source file is touched"
    - "The exact number of tests (369 or otherwise) is known so the post-Phase-1 delta can be verified"
  artifacts:
    - path: ".planning/phases/01-package-consolidation/coverage-baseline.txt"
      provides: "Human-readable coverage report for diffing after Phase 1"
    - path: ".planning/phases/01-package-consolidation/coverage-baseline.json"
      provides: "Machine-readable coverage JSON for automated diff in plan 05"
  key_links:
    - from: ".planning/phases/01-package-consolidation/coverage-baseline.json"
      to: ".planning/phases/01-package-consolidation/coverage-post.json"
      via: "diff in plan 05"
      pattern: "coverage-post\\.json"
---

<objective>
Record a coverage baseline and verify the full test suite is green before any source file changes.

Purpose: D-13 requires a before/after coverage diff. This plan creates the "before" side and confirms the exact test count so the post-Phase-1 drop of exactly 21 tests (from the dead progress API) can be verified in plan 05.

Output: Two baseline files committed to .planning/phases/01-package-consolidation/. No production code changes.
</objective>

<execution_context>
@/Users/picard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/picard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-package-consolidation/01-CONTEXT.md
@.planning/phases/01-package-consolidation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify pre-flight green and record exact test count</name>
  <files>.planning/phases/01-package-consolidation/coverage-baseline.txt</files>

  <read_first>
    - pyproject.toml (to confirm pytest and coverage settings — fail_under = 60, source list)
    - .planning/codebase/TESTING.md (369-test baseline claim; verify count matches)
  </read_first>

  <action>
    Run the full test suite with coverage and save the output.

    Command to run:
      uv run pytest --cov --cov-report=term-missing --cov-report=json -q 2>&1 | tee .planning/phases/01-package-consolidation/coverage-baseline.txt

    Then run the test count check:
      uv run pytest --collect-only -q 2>&1 | tail -5

    Record the exact collected-test count from the tail output. If the count differs from 369, note it explicitly in the baseline file as a comment on the first line (e.g., "# Actual pre-Phase-1 count: 367").

    The coverage run also produces coverage.json in the working directory. Copy it:
      cp coverage.json .planning/phases/01-package-consolidation/coverage-baseline.json

    Do NOT modify any source file in this task. If pytest fails, stop and report the failure before creating any plans.
  </action>

  <verify>
    <automated>uv run pytest -q --tb=short 2>&1 | tail -3</automated>
    Expect: "passed" with zero failures/errors. If failures exist, do not proceed.
  </verify>

  <done>
    - uv run pytest exits 0 (all tests pass, coverage >= fail_under = 60)
    - .planning/phases/01-package-consolidation/coverage-baseline.txt exists and contains line counts and coverage percentages for both odoo_fast_report_mapper and odoo_report_helper modules
    - .planning/phases/01-package-consolidation/coverage-baseline.json exists and is valid JSON
    - Exact test count is known and noted (expected ~369)
  </done>
</task>

<task type="auto">
  <name>Task 2: Commit coverage baseline to git</name>
  <files>.planning/phases/01-package-consolidation/coverage-baseline.txt, .planning/phases/01-package-consolidation/coverage-baseline.json</files>

  <read_first>
    - .planning/phases/01-package-consolidation/coverage-baseline.txt (just created — verify it has content before committing)
  </read_first>

  <action>
    Stage and commit the two baseline files. No other files should be staged.

    git add .planning/phases/01-package-consolidation/coverage-baseline.txt
    git add .planning/phases/01-package-consolidation/coverage-baseline.json
    git commit -m "[ADD] docs(01): record coverage baseline before Phase 1 consolidation (D-13)"

    The commit message uses the [ADD] prefix per CLAUDE.md convention.
  </action>

  <verify>
    <automated>git log --oneline -1</automated>
    Expect: commit message starts with "[ADD] docs(01):"
    <automated>ls -la .planning/phases/01-package-consolidation/coverage-baseline.*</automated>
    Expect: both files present
  </verify>

  <done>
    - git commit exits 0
    - git log shows the baseline commit as HEAD
    - Both coverage-baseline.txt and coverage-baseline.json are tracked in git
    - Working tree is clean (git status shows nothing to commit)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner → filesystem | pytest writes coverage.json; tee writes baseline.txt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | coverage-baseline.json | accept | No secrets in coverage data; file is in .planning/ (not shipped in wheel) |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
After both tasks complete:
- git status is clean
- uv run pytest -q exits 0
- .planning/phases/01-package-consolidation/coverage-baseline.txt is non-empty
- .planning/phases/01-package-consolidation/coverage-baseline.json is valid JSON with a "totals" key
</verification>

<success_criteria>
- Full test suite green before any Phase 1 code changes begin
- Coverage baseline committed and available for diff in plan 05
- Exact test count documented
</success_criteria>

<output>
Create .planning/phases/01-package-consolidation/01-01-SUMMARY.md when done
</output>
