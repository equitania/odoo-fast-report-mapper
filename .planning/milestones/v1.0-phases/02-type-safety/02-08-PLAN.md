---
phase: 02-type-safety
plan: 08
type: execute
wave: 6
depends_on:
  - "02-07"
files_modified:
  - odoo_fast_report_mapper/_cli.py
  - pyproject.toml
autonomous: true
requirements:
  - TYPE-01
  - TYPE-02
  - TYPE-03

must_haves:
  truths:
    - "_cli.py has 0 mypy --strict errors"
    - "All [[tool.mypy.overrides]] ignore_errors blocks are removed from pyproject.toml"
    - "uv run mypy odoo_fast_report_mapper/ exits 0 with zero override exemptions for internal modules"
    - "pyproject.toml [tool.mypy] has strict = true — uv run mypy odoo_fast_report_mapper/ (no flags) exits 0"
    - "uv run pytest exits 0 (347 tests pass)"
    - "Entry points odoo-fast-report-mapper and odoo-fr-mapper resolve without import error"
  artifacts:
    - path: "odoo_fast_report_mapper/_cli.py"
      provides: "Fully annotated CLI entry point"
      contains: "def print_banner() -> None"
    - path: "pyproject.toml"
      provides: "Final mypy strict config with zero internal module overrides"
      contains: "strict = true"
  key_links:
    - from: "pyproject.toml [tool.mypy]"
      to: "uv run mypy odoo_fast_report_mapper/"
      via: "strict = true in config means no --strict flag needed in local command"
      pattern: "strict = true"
    - from: "_cli.py entry points"
      to: "pyproject.toml [project.scripts]"
      via: "start_odoo_fast_report_mapper must remain importable"
      pattern: "start_odoo_fast_report_mapper"
---

<objective>
Annotate `_cli.py` (10 errors: 3 `no-untyped-def` + 6 `no-untyped-call` cascade + 1 other)
AND remove all remaining `[[tool.mypy.overrides]] ignore_errors = true` blocks from pyproject.toml
in a single atomic pass — establishing the final zero-error strict-mode state for Phase 2.

The 6 `no-untyped-call` cascade errors in `_cli.py` disappear automatically once all callee
modules are clean (Plans 02-02 through 02-07). Only 3 genuine `no-untyped-def` and 1 other
error require annotation work.

The Captain's release workflow is local (`uv run mypy odoo_fast_report_mapper/` + `uv run pytest`,
then `uv publish`). Phase 2 makes NO changes to `.github/workflows/test.yml`. CI enforcement
work is deferred to Phase 4. TYPE-03 is satisfied purely by `strict = true` living in
pyproject.toml — `uv run mypy odoo_fast_report_mapper/` (no flags) exits 0 because the config
is correct.

Wave 6 — final plan; must run after all other annotation plans are complete.

Output: Zero-override strict mypy config; _cli.py clean; Phase 2 complete.
</objective>

<execution_context>
@/Users/picard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/picard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-type-safety/02-CONTEXT.md
@.planning/phases/02-type-safety/02-RESEARCH.md
@.planning/phases/02-type-safety/02-PATTERNS.md
@.planning/phases/02-type-safety/02-07-SUMMARY.md
</context>

<interfaces>
Entry points registered in pyproject.toml [project.scripts]:
  odoo-fast-report-mapper = "odoo_fast_report_mapper._cli:start_odoo_fast_report_mapper"
  odoo-fr-mapper = "odoo_fast_report_mapper._cli:start_odoo_fast_report_mapper"

These must remain importable after annotation. The function `start_odoo_fast_report_mapper`
is the Click command decorated function — its return type is `None`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Annotate _cli.py AND remove all remaining override blocks — atomic final pass</name>
  <files>odoo_fast_report_mapper/_cli.py, pyproject.toml</files>
  <read_first>
    - odoo_fast_report_mapper/_cli.py (full current file — all 341 LOC before touching)
    - .planning/phases/02-type-safety/02-PATTERNS.md §`_cli.py` (Click callback signature, print_banner fix)
    - .planning/phases/02-type-safety/02-RESEARCH.md §Error Code Breakdown (no-untyped-call: cascade resolution)
    - .planning/phases/02-type-safety/02-RESEARCH.md §Pitfall 1 (cascade disappears when callees annotated)
    - pyproject.toml (find all remaining [[tool.mypy.overrides]] ignore_errors blocks for internal modules)
  </read_first>
  <behavior>
    - `print_banner() -> None:` (bare function, no params)
    - `init_callback(ctx: click.Context, param: click.Parameter, value: bool) -> None:` (Click callback signature)
    - `start_odoo_fast_report_mapper` Click command decorated function: `-> None`
    - Any other bare functions in _cli.py get annotated per PATTERNS.md
    - `from __future__ import annotations` already present (Plan 02-01); verify, do not duplicate
    - `import click` already present; `click.Context`, `click.Parameter` available from click package
    - The 6 `no-untyped-call` errors are cascade from other modules — confirm they are gone since Plans 02-02..02-07 cleared all callees
    - After annotation + override removal: `uv run mypy odoo_fast_report_mapper/` (config-driven, no flags) → 0 errors with zero internal ignore_errors blocks
  </behavior>
  <action>
    PART A — Annotate _cli.py:

    Read _cli.py in full. Run `uv run mypy odoo_fast_report_mapper/_cli.py --strict 2>&1` before
    making any changes to see the current error list. The cascade `no-untyped-call` errors should
    already be gone since all callee modules are now clean.

    Annotate:
    1. `print_banner()` → `def print_banner() -> None:`
    2. `init_callback(ctx, param, value)` → `def init_callback(ctx: click.Context, param: click.Parameter, value: bool) -> None:`
    3. The main Click command function → `-> None`
    4. Any other bare functions found in the file

    For Click-decorated functions: the decorator `@click.command()` / `@click.option()` wraps the
    function; mypy understands Click decorators when click.pyi stubs are available. `click>=8.1.3`
    ships its own `py.typed` + stubs — no extra install needed.

    Do NOT annotate `@click.option()` parameters themselves (those are Click-managed); only annotate
    the Python function signatures.

    After annotation, verify isolation: `uv run mypy odoo_fast_report_mapper/_cli.py --strict` → 0 errors.

    PART B — Remove the _cli override block and verify zero internal overrides remain:

    Open pyproject.toml. Remove the `[[tool.mypy.overrides]]` block for
    `odoo_fast_report_mapper._cli` (the last remaining `ignore_errors = true` internal block).

    After removal, the only `[[tool.mypy.overrides]]` block that should remain is:
      `[[tool.mypy.overrides]] module = "odoorpc_toolbox.*" follow_untyped_imports = true`

    Verify by reading pyproject.toml: count `ignore_errors` occurrences — must be 0 for internal modules.

    PART C — Final zero-error verification (config-driven, no flags):

    Run `uv run mypy odoo_fast_report_mapper/` (no --strict flag; strict = true is read from
    pyproject.toml). This is the authoritative check — no flags means the config alone drives the result.
    Must exit 0. If it does not, the override removal is incomplete or an annotation was missed.

    Run `uv run pytest --tb=short -q` → 347 pass.
    Run entry-point import check: `python -c "from odoo_fast_report_mapper._cli import start_odoo_fast_report_mapper; print('PASS')"`.

    NOTE: Do NOT touch `.github/workflows/test.yml`. CI workflow changes are deferred to Phase 4
    per the Captain's decision (local release workflow: `uv run mypy` + `uv run pytest` + `uv publish`).

    Commit message: `[CHG] type-safety(02-08): annotate _cli.py, remove all override blocks — Phase 2 complete`
  </action>
  <verify>
    <automated>
      uv run mypy odoo_fast_report_mapper/_cli.py --strict 2>&1 | grep "error:" | wc -l | grep -q "^0$" && echo "PASS: _cli 0 errors" || echo "FAIL"
      grep "def print_banner() -> None" odoo_fast_report_mapper/_cli.py | grep -q "None" && echo "PASS: print_banner annotated" || echo "FAIL"
      uv run mypy odoo_fast_report_mapper/ 2>&1 | grep "error:" | wc -l | grep -q "^0$" && echo "PASS: full package 0 errors (no flags)" || echo "FAIL"
      grep -v "^#" pyproject.toml | grep "ignore_errors" && echo "FAIL: internal ignore_errors blocks still present" || echo "PASS: all internal overrides removed"
      grep "odoorpc_toolbox" pyproject.toml | grep -q "follow_untyped_imports" && echo "PASS: odoorpc_toolbox override retained" || echo "FAIL"
      grep "strict = true" pyproject.toml | grep -q "true" && echo "PASS: strict=true in pyproject.toml" || echo "FAIL"
      python -c "from odoo_fast_report_mapper._cli import start_odoo_fast_report_mapper; print('PASS: entry point importable')"
      uv run pytest --tb=short -q 2>&1 | tail -3
    </automated>
  </verify>
  <done>
    `_cli.py` annotated — `print_banner() -> None`, `init_callback` with Click types, main command
    with `-> None`; all internal `ignore_errors = true` override blocks removed from pyproject.toml;
    only the `odoorpc_toolbox.*` `follow_untyped_imports` override remains; `uv run mypy
    odoo_fast_report_mapper/` (no flags, config-driven) exits 0; both entry points importable;
    347 tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None introduced | Annotation-only changes to _cli.py and pyproject.toml override removal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08-SC | Tampering | No package installs in this plan | accept | No new dependencies. |

**Security assessment:** This plan annotates `_cli.py` and removes the last pyproject.toml
override blocks. The Click callback annotations cover framework-managed types — no new I/O or
trust boundary is introduced. All user input parsing remains handled by Click. The _cli.py
annotations document the expected types; they do not add runtime validation. No in-scope threats.
</threat_model>

<verification>
Phase 2 success criteria (all must be TRUE — local commands only):

1. `uv run mypy odoo_fast_report_mapper/` → 0 errors (TYPE-01 satisfied)
2. `grep "strict = true" pyproject.toml` → found (TYPE-03 satisfied — config-correctness)
3. `grep -v "^#" pyproject.toml | grep "ignore_errors"` → no output (all internal overrides removed)
4. `grep "follow_untyped_imports = true" pyproject.toml` → found (odoorpc_toolbox.* only, D-03)
5. `python -c "from odoo_fast_report_mapper._odoo_types import IrModelRecord"` → no error
6. `python -c "from odoo_fast_report_mapper._cli import start_odoo_fast_report_mapper"` → no error
7. `uv run pytest` → 347 passed
8. `grep "mypy>=1.18" pyproject.toml` → found (pin updated in Plan 02-01)
</verification>

<success_criteria>
1. _cli.py: 0 mypy --strict errors — `print_banner`, `init_callback`, main command annotated
2. pyproject.toml: zero `[[tool.mypy.overrides]] ignore_errors = true` blocks for internal modules
3. pyproject.toml [tool.mypy]: only `strict = true` and `python_version = "3.12"` plus the odoorpc_toolbox override
4. `uv run mypy odoo_fast_report_mapper/` (config-driven, no flags) → 0 errors (TYPE-01 satisfied)
5. All public functions and methods have complete annotations (TYPE-02 satisfied)
6. `strict = true` in pyproject.toml — `uv run mypy odoo_fast_report_mapper/` runs without flag overrides (TYPE-03 satisfied)
7. `uv run pytest` → 347 passed
8. Entry points `odoo-fast-report-mapper` and `odoo-fr-mapper` resolve without error
</success_criteria>

<output>
Create `.planning/phases/02-type-safety/02-08-SUMMARY.md` when done.
</output>
