---
phase: 02-type-safety
plan: 05
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
  - "02-03"
files_modified:
  - odoo_fast_report_mapper/_utils.py
  - pyproject.toml
autonomous: true
requirements:
  - TYPE-01
  - TYPE-02

must_haves:
  truths:
    - "_utils.py has 0 mypy --strict errors"
    - "The [[tool.mypy.overrides]] block for odoo_fast_report_mapper._utils is removed from pyproject.toml"
    - "uv run mypy odoo_fast_report_mapper/ exits 0 (_utils is no longer gated)"
    - "uv run pytest exits 0 (347 tests pass)"
    - "os.getenv() calls are guarded against None (no bare int(os.getenv(...)))"
  artifacts:
    - path: "odoo_fast_report_mapper/_utils.py"
      provides: "Fully annotated utility and factory functions"
      contains: "def collect_yaml_files"
  key_links:
    - from: "_utils.py os.getenv() sites"
      to: "arg-type error resolution"
      via: "None-guard or default argument eliminates str|None mismatch"
      pattern: "os.getenv"
---

<objective>
Annotate `_utils.py` (26 mypy --strict errors: 15 `no-untyped-def`, 6 `no-untyped-call`,
3 `type-arg`, 2 `arg-type`) and remove its `[[tool.mypy.overrides]] ignore_errors = true` block.

The 6 `no-untyped-call` errors are cascades from the same module's own untyped functions —
annotating all 15 `no-untyped-def` entries first will automatically resolve the cascade calls.
The 2 `arg-type` errors are from `os.getenv()` returning `str | None` at call sites expecting `str`.

Wave 3 (parallel with 02-04) — both depend on 02-01/02-02/02-03, no file overlap.

Output: Fully annotated _utils.py; override block removed.
</objective>

<execution_context>
@/Users/picard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/picard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-type-safety/02-CONTEXT.md
@.planning/phases/02-type-safety/02-RESEARCH.md
@.planning/phases/02-type-safety/02-PATTERNS.md
@.planning/phases/02-type-safety/02-02-SUMMARY.md
@.planning/phases/02-type-safety/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Annotate _utils.py — all function signatures, os.getenv guards, bare-dict fixes</name>
  <files>odoo_fast_report_mapper/_utils.py, pyproject.toml</files>
  <read_first>
    - odoo_fast_report_mapper/_utils.py (full current file — all 517 LOC before touching)
    - .planning/phases/02-type-safety/02-PATTERNS.md §`_utils.py` (established import pattern, os.getenv fix, type-arg table)
    - .planning/phases/02-type-safety/02-RESEARCH.md §Pattern 7 (arg-type os.getenv fix pattern)
    - .planning/phases/02-type-safety/02-RESEARCH.md §Pitfall 3 (os.getenv pitfall — replicate BUG-05 pattern)
    - .planning/phases/02-type-safety/02-RESEARCH.md §Pitfall 1 (no-untyped-call cascade — annotate all in one commit)
    - pyproject.toml (find the _utils override block to remove)
  </read_first>
  <behavior>
    - All 15 `no-untyped-def` functions get param + return type annotations in a single pass
    - YAML-parsing functions use `dict[str, Any]` return (Tier 2 — opaque YAML content):
        `collect_yaml_files(path: str) -> list[dict[str, Any]]`
        `prepare_connection(server_config: dict[str, Any]) -> dict[str, Any]`
        `generate_env_template(target_dir: str) -> str`
    - `self_clean` function: `self_clean(data: dict[str, list[str]]) -> dict[str, list[str]]`
      (IMPORTANT: the Phase 1.1 BUG-02 fix removed the self_clean call from add_calculated_fields;
      verify the function's actual param usage in the file before annotating)
    - os.getenv() `arg-type` fix (lines 470, 477 per RESEARCH.md — confirm actual lines):
        Before: `int(os.getenv("ODOO_PORT"))` → int() receives str|None
        After: use `os.getenv("ODOO_PORT", "")` with default or add explicit None check before use
        Replicate the Phase 1.1 BUG-05 pattern from prepare_connection (already in the file)
    - `from __future__ import annotations` already present (Plan 02-01); verify, do not duplicate
    - `from typing import Any` import: already present or add if needed
    - After all 15 defs annotated: the 6 `no-untyped-call` cascade errors disappear automatically
    - After annotation: `uv run mypy odoo_fast_report_mapper/_utils.py --strict` → 0 errors
  </behavior>
  <action>
    Read _utils.py in full (517 LOC). Run `uv run mypy odoo_fast_report_mapper/_utils.py --strict 2>&1`
    before any edits to get exact line numbers for all 26 errors.

    Strategy: annotate ALL functions in _utils.py in a single pass before re-checking mypy.
    Per RESEARCH.md §Pitfall 1 ("never commit a half-annotated module with the override removed"),
    complete all 15 `no-untyped-def` annotations before running the final mypy check.

    For each function in _utils.py:
    1. Identify param names and their expected types from context (call sites in _connection.py,
       _cli.py, and the function's own docstring/body).
    2. For functions returning YAML-loaded data or config dicts: use `dict[str, Any]` (Tier 2).
    3. For functions with `os.getenv()` calls passed to `int()` or other typed functions: add
       `or ""` default, or add an explicit `if env_val is None: raise ValueError(...)` guard
       replicating the BUG-05 pattern already in prepare_connection.
    4. For `self_clean`: read the actual signature usage first; annotate as `dict[str, list[str]],
       dict[str, list[str]]` only if the parameter is actually used that way (not dict[str, Any]).

    Do NOT change any logic. If a guard is needed for os.getenv, add ONLY the None check — the
    pattern is already established in prepare_connection by Phase 1.1; replicate exactly.

    After all annotations applied, verify:
    `uv run mypy odoo_fast_report_mapper/_utils.py --strict` → 0 errors

    Remove the `[[tool.mypy.overrides]]` block for `odoo_fast_report_mapper._utils` from pyproject.toml.
    Run: `uv run mypy odoo_fast_report_mapper/` → 0 errors (_utils is no longer gated).
    Run: `uv run pytest --tb=short -q` → 347 pass.

    Commit message: `[CHG] type-safety(02-05): annotate _utils.py, remove override block`
  </action>
  <verify>
    <automated>
      uv run mypy odoo_fast_report_mapper/_utils.py --strict 2>&1 | grep "error:" | wc -l | grep -q "^0$" && echo "PASS: _utils 0 errors" || echo "FAIL"
      uv run mypy odoo_fast_report_mapper/ 2>&1 | grep "error:" | wc -l | grep -q "^0$" && echo "PASS: package 0 errors" || echo "FAIL"
      grep "odoo_fast_report_mapper._utils" pyproject.toml | grep "ignore_errors" && echo "FAIL: override still present" || echo "PASS: override removed"
      grep "collect_yaml_files" odoo_fast_report_mapper/_utils.py | grep -q "list\[" && echo "PASS: return type annotated" || echo "FAIL"
      uv run pytest --tb=short -q 2>&1 | tail -3
    </automated>
  </verify>
  <done>
    `_utils.py` has 0 mypy --strict errors (all 15 `no-untyped-def` and both `arg-type` errors
    resolved; 6 `no-untyped-call` cascade errors disappeared automatically); override block
    removed from pyproject.toml; `uv run mypy odoo_fast_report_mapper/` exits 0;
    347 tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| os.getenv() None guards | Ensures environment variables are validated before use |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05-SC | Tampering | No package installs in this plan | accept | No new dependencies. |
| T-02-05-01 | Information Disclosure | os.getenv() None guard | mitigate | Adding explicit None-check or default guards for ODOO_PORT etc. prevents confusing error messages when env vars are absent; no new behavior, only clearer failure path. |

**Security assessment:** The os.getenv() None guards added by this plan are defensive improvements
— they replace silent None→int() TypeErrors with explicit ValueError or default values. This
is annotation work that fixes real type mismatches (arg-type errors); no new data flows or trust
boundaries are created. All other changes are pure annotations. No in-scope high-severity threats.
</threat_model>

<verification>
- `uv run mypy odoo_fast_report_mapper/_utils.py --strict` → 0 errors
- `uv run mypy odoo_fast_report_mapper/` → 0 errors (_utils is no longer gated)
- `grep "_utils" pyproject.toml | grep "ignore_errors"` → no output
- `uv run pytest` → 347 passed
</verification>

<success_criteria>
1. _utils.py: all 26 errors resolved — 0 mypy --strict errors
2. All 15 `no-untyped-def` functions have complete param + return type annotations
3. os.getenv() call sites guarded against None (no bare str|None passed to int())
4. No `[[tool.mypy.overrides]]` block for `_utils` in pyproject.toml
5. `uv run mypy odoo_fast_report_mapper/` → 0 errors (_utils is no longer gated)
6. `uv run pytest` → 347 passed
</success_criteria>

<output>
Create `.planning/phases/02-type-safety/02-05-SUMMARY.md` when done.
</output>
