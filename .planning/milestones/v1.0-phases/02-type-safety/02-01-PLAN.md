---
phase: 02-type-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - odoo_fast_report_mapper/_odoo_types.py
  - odoo_fast_report_mapper/_yaml_dumper.py
  - odoo_fast_report_mapper/_progress.py
  - odoo_fast_report_mapper/__init__.py
  - odoo_fast_report_mapper/__version__.py
  - odoo_fast_report_mapper/_exceptions.py
  - odoo_fast_report_mapper/_lang_utils.py
  - odoo_fast_report_mapper/_logging.py
  - odoo_fast_report_mapper/_report.py
  - odoo_fast_report_mapper/_utils.py
  - odoo_fast_report_mapper/_connection.py
  - odoo_fast_report_mapper/_cli.py
autonomous: true
requirements:
  - TYPE-01
  - TYPE-02
  - TYPE-03

must_haves:
  truths:
    - "uv run mypy odoo_fast_report_mapper/ exits 0 after this plan (all 8 pending modules have ignore_errors=true; the 3 already-clean modules are NOT gated)"
    - "pyproject.toml [tool.mypy] contains strict = true and no ignore_missing_imports"
    - "odoorpc_toolbox.* override uses follow_untyped_imports = true"
    - "mypy pin is mypy>=1.18 in [project.optional-dependencies].dev"
    - "_odoo_types.py exists with IrModelRecord, IrModelFieldsRecord, ReportAction, LanguageRecord TypedDicts"
    - "from __future__ import annotations is the first import in every _*.py module"
    - "_yaml_dumper.py and _progress.py have 0 mypy --strict errors (overrides removed for these two)"
    - "uv run pytest exits 0 (347 tests pass)"
  artifacts:
    - path: "odoo_fast_report_mapper/_odoo_types.py"
      provides: "TypedDict shapes for Odoo RPC responses"
      contains: "IrModelRecord"
    - path: "pyproject.toml"
      provides: "mypy strict config + per-module overrides"
      contains: "strict = true"
  key_links:
    - from: "pyproject.toml [tool.mypy.overrides]"
      to: "odoo_fast_report_mapper._yaml_dumper"
      via: "ignore_errors = true temporarily gates this module"
      pattern: "ignore_errors"
    - from: "_odoo_types.py"
      to: "_connection.py"
      via: "TypedDict imports used in Plans 02-06/02-07"
      pattern: "from ._odoo_types import"
---

<objective>
Enable mypy strict mode globally on day one, gate the 8 pending modules with per-module
override exemptions, add `from __future__ import annotations` project-wide, create the
`_odoo_types.py` TypedDict definitions, and clear the 2 trivial modules (_yaml_dumper, _progress).

Purpose: Establishes the scaffolding for the module-by-module annotation ramp (D-04/D-05/D-06).
After this plan, `uv run mypy odoo_fast_report_mapper/` exits 0 — not because errors are fixed,
but because each pending module has `ignore_errors = true`. Subsequent plans remove those blocks.

The Captain's release workflow is local (`uv run mypy` + `uv run pytest`, then `uv publish`).
Phase 2 makes NO changes to `.github/workflows/test.yml`. All verification criteria are local
commands only. CI/GitHub Actions changes are deferred to Phase 4.

NOTE FOR EXECUTOR: The project's `fr-mapper` SKILL.md (in `.claude/skills/`) still describes the
pre-Phase-1 two-package layout (`odoo_report_helper/`, `eq_odoo_connection.py`, `eq_report.py`).
That SKILL.md is intentionally stale — refer to the actual consolidated codebase under
`odoo_fast_report_mapper/`. The SKILL.md update is deferred to Phase 4 (DOCS-04).

Output: Updated pyproject.toml, new _odoo_types.py, __future__ annotations project-wide,
_yaml_dumper.py and _progress.py fully annotated (overrides removed for those two).
</objective>

<execution_context>
@/Users/picard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/picard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-type-safety/02-CONTEXT.md
@.planning/phases/02-type-safety/02-RESEARCH.md
@.planning/phases/02-type-safety/02-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update pyproject.toml — mypy strict config + override ramp scaffold</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml (full current state — especially [tool.mypy] and [project.optional-dependencies].dev)
    - .planning/phases/02-type-safety/02-RESEARCH.md §`pyproject.toml Changes Required` (exact target TOML blocks)
    - .planning/phases/02-type-safety/02-PATTERNS.md §`pyproject.toml` (before/after annotation)
  </read_first>
  <action>
    Replace the [tool.mypy] section in pyproject.toml with the following structure (per D-03/D-04/TYPE-03):

    1. Replace [tool.mypy] body: remove `warn_return_any`, `warn_unused_configs`, `ignore_missing_imports`;
       add `strict = true`. Keep `python_version = "3.12"`.

    2. Add [[tool.mypy.overrides]] block for `odoorpc_toolbox.*` with `follow_untyped_imports = true`
       (per D-03). This replaces the blanket `ignore_missing_imports = true`.

    3. Add one [[tool.mypy.overrides]] block per pending module with `ignore_errors = true`.
       Pending modules (per D-04 sequence, all gated initially):
         - odoo_fast_report_mapper._lang_utils    (Plan 02-02 will remove)
         - odoo_fast_report_mapper._logging       (Plan 02-03 will remove)
         - odoo_fast_report_mapper._report        (Plan 02-04 will remove)
         - odoo_fast_report_mapper._utils         (Plan 02-05 will remove)
         - odoo_fast_report_mapper._connection    (Plans 02-06/07 will remove)
         - odoo_fast_report_mapper._cli           (Plan 02-08 will remove)
       NOTE: Do NOT add ignore_errors blocks for _yaml_dumper or _progress — those are
       cleared in Task 3 of this plan, so they start unblocked.
       Do NOT add ignore_errors blocks for _exceptions, __init__, __version__ — already clean.

    4. In [project.optional-dependencies].dev, change `"mypy>=1.0"` to `"mypy>=1.18"`.
       (Required for follow_untyped_imports support — 1.19.1 is installed, pin ensures minimum.)

    5. Run `uv lock --upgrade-package mypy` to update the lockfile for the new pin. If no
       lockfile change occurs (installed version already satisfies), that is expected.

    Commit message: `[CHG] type-safety(02-01): enable mypy strict mode with per-module override ramp`
  </action>
  <verify>
    <automated>
      grep -E "^strict = true" pyproject.toml | wc -l | grep -q "^1$" && echo "PASS: strict=true found" || echo "FAIL: strict=true missing"
      grep "ignore_missing_imports" pyproject.toml && echo "FAIL: old ignore_missing_imports still present" || echo "PASS: removed"
      grep "follow_untyped_imports" pyproject.toml | grep -q "true" && echo "PASS: follow_untyped_imports set" || echo "FAIL"
      grep "mypy>=1.18" pyproject.toml | grep -q "1.18" && echo "PASS: mypy pin updated" || echo "FAIL"
      grep "ignore_errors" pyproject.toml | grep -v "#" | wc -l | grep -qE "^[4-6]$" && echo "PASS: 4-6 override blocks" || echo "FAIL: unexpected count"
    </automated>
  </verify>
  <done>
    pyproject.toml [tool.mypy] contains `strict = true`; no `ignore_missing_imports`; odoorpc_toolbox.*
    override with `follow_untyped_imports = true`; 4-6 pending module `ignore_errors = true` blocks
    (_yaml_dumper and _progress NOT gated here); mypy pin is `mypy>=1.18`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create _odoo_types.py — TypedDict shapes for Odoo RPC responses</name>
  <files>odoo_fast_report_mapper/_odoo_types.py</files>
  <read_first>
    - .planning/phases/02-type-safety/02-PATTERNS.md §`_odoo_types.py (NEW)` (full skeleton with TypedDict bodies)
    - .planning/phases/02-type-safety/02-RESEARCH.md §`_odoo_types.py — TypedDict Shapes` (shape table + field details)
    - odoo_fast_report_mapper/_exceptions.py (copyright header format to copy verbatim)
    - odoo_fast_report_mapper/_progress.py (module docstring + import style to replicate)
  </read_first>
  <action>
    Create `odoo_fast_report_mapper/_odoo_types.py` with:

    1. Copyright block (copy verbatim from _exceptions.py: "Copyright 2014-now Equitania Software GmbH...").
    2. One-line module docstring: "TypedDict type definitions for Odoo RPC response shapes. Internal use only."
    3. `from __future__ import annotations` as first import (per D-05).
    4. Imports: `from typing import Any, Literal, TypedDict`.
    5. Four TypedDict classes (per D-06 Tier 1):
       - `IrModelRecord(TypedDict, total=True)`: `id: int`, `model: str`, `name: str`
         Docstring: "ir.model record returned by Odoo RPC search_read calls."
       - `IrModelFieldsRecord(TypedDict, total=False)`: `id: int`, `name: str`, `ttype: str`,
         `modules: str | Literal[False]`
         Docstring: "ir.model.fields record. total=False: not all fields guaranteed in all Odoo versions."
         Add inline comment on modules field: "# Odoo returns False when no module owns the field"
       - `ReportAction(TypedDict, total=True)`: `id: int`, `report_type: str`, `model: str`,
         `name: str`, `ids: list[int]`
         Docstring: "ir.actions.report record returned by browse/search_read."
       - `LanguageRecord(TypedDict, total=True)`: `code: str`, `name: str`
         Docstring: "res.lang record from get_installed_languages."

    Use `X | Literal[False]` union syntax (not Optional) — matches established _progress.py style.
    No `__all__` needed — the file is private (_odoo_types.py prefix).

    Commit message: `[ADD] type-safety(02-01): add _odoo_types.py with TypedDict shapes`
  </action>
  <verify>
    <automated>
      python -c "from odoo_fast_report_mapper._odoo_types import IrModelRecord, IrModelFieldsRecord, ReportAction, LanguageRecord; print('PASS: all TypedDicts importable')"
      uv run mypy odoo_fast_report_mapper/_odoo_types.py --strict 2>&1 | grep -c "error:" | grep -q "^0$" && echo "PASS: 0 errors" || echo "FAIL: errors in _odoo_types.py"
    </automated>
  </verify>
  <done>
    _odoo_types.py exists; all four TypedDicts are importable; `uv run mypy --strict
    odoo_fast_report_mapper/_odoo_types.py` reports 0 errors.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add __future__ annotations project-wide + annotate _yaml_dumper.py and _progress.py</name>
  <files>
    odoo_fast_report_mapper/_yaml_dumper.py,
    odoo_fast_report_mapper/_progress.py,
    odoo_fast_report_mapper/__init__.py,
    odoo_fast_report_mapper/__version__.py,
    odoo_fast_report_mapper/_exceptions.py,
    odoo_fast_report_mapper/_lang_utils.py,
    odoo_fast_report_mapper/_logging.py,
    odoo_fast_report_mapper/_report.py,
    odoo_fast_report_mapper/_utils.py,
    odoo_fast_report_mapper/_connection.py,
    odoo_fast_report_mapper/_cli.py
  </files>
  <read_first>
    - .planning/phases/02-type-safety/02-PATTERNS.md §Shared Patterns (`from __future__` placement rule)
    - .planning/phases/02-type-safety/02-PATTERNS.md §`_yaml_dumper.py` (exact fix for increase_indent)
    - .planning/phases/02-type-safety/02-PATTERNS.md §`_progress.py` (exact return type fix tqdm[Any])
    - odoo_fast_report_mapper/_yaml_dumper.py (current 13-line file — read before editing)
    - odoo_fast_report_mapper/_progress.py (current file — read before editing)
  </read_first>
  <behavior>
    - After this task: `uv run mypy odoo_fast_report_mapper/_yaml_dumper.py` (without suppression) → 0 errors
    - After this task: `uv run mypy odoo_fast_report_mapper/_progress.py` (without suppression) → 0 errors
    - After this task: `uv run mypy odoo_fast_report_mapper/` → 0 errors (because all other pending modules are still gated)
    - After this task: `uv run pytest` → 347 tests pass (no runtime behavior changed)
  </behavior>
  <action>
    STEP A — Add `from __future__ import annotations` to every _*.py module:
    Insert `from __future__ import annotations` as the first import line (after the copyright
    comment block and module docstring, before all other imports) in these files:
    `_yaml_dumper.py`, `_progress.py`, `_lang_utils.py`, `_logging.py`, `_report.py`,
    `_utils.py`, `_connection.py`, `_cli.py`.
    Also add to `__init__.py`, `__version__.py`, `_exceptions.py` if not already present.
    Placement rule (per 02-PATTERNS.md §Shared Patterns): first import line after copyright+docstring.

    STEP B — Annotate _yaml_dumper.py (1 error: no-untyped-def):
    The only change beyond __future__ is annotating `increase_indent`:
    - Add param annotations: `flow: bool = False, indentless: bool = False`
    - Add return annotation: `-> None`
    (Return is None because Dumper.increase_indent mutates internal state via super().increase_indent.)
    Verify against types-PyYAML stub for yaml.Dumper.increase_indent signature.

    STEP C — Annotate _progress.py (1 error: no-any-return):
    The only change beyond __future__ is fixing the return type of `progress_bar()`:
    - Change `-> Iterable[Any]` to `-> tqdm[Any]`
    - The `from tqdm import tqdm` import is already present; no new import needed.
    - `tqdm[Any]` is the concrete generic return type (tqdm ships py.typed + stubs ≥4.66).

    NOTE: Do NOT touch `.github/workflows/test.yml`. CI workflow changes are deferred to Phase 4
    per the Captain's decision (local release workflow: `uv run mypy` + `uv run pytest` + `uv publish`).

    Commit message: `[CHG] type-safety(02-01): add __future__ annotations project-wide, clear _yaml_dumper + _progress`
  </action>
  <verify>
    <automated>
      uv run mypy odoo_fast_report_mapper/_yaml_dumper.py --strict 2>&1 | grep "error:" | wc -l | grep -q "^0$" && echo "PASS: _yaml_dumper clean" || echo "FAIL"
      uv run mypy odoo_fast_report_mapper/_progress.py --strict 2>&1 | grep "error:" | wc -l | grep -q "^0$" && echo "PASS: _progress clean" || echo "FAIL"
      uv run mypy odoo_fast_report_mapper/ 2>&1 | grep "error:" | wc -l | grep -q "^0$" && echo "PASS: full package 0 errors" || echo "FAIL: non-zero errors (check overrides are present)"
      uv run pytest --tb=short -q 2>&1 | tail -5
      grep -r "from __future__ import annotations" odoo_fast_report_mapper/ | wc -l | grep -qE "^[89]$|^1[01]$" && echo "PASS: __future__ in all modules" || echo "FAIL: missing in some modules"
    </automated>
  </verify>
  <done>
    `from __future__ import annotations` present in every _*.py module; `_yaml_dumper.py` and
    `_progress.py` have 0 mypy --strict errors (no `[[overrides]]` blocks for these two);
    `uv run mypy odoo_fast_report_mapper/` exits 0 (all other pending modules still gated);
    `uv run pytest` exits 0 (347 tests pass).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None introduced | This plan adds type annotations and mypy configuration only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-SC | Tampering | No new npm/pip/cargo installs in this plan | accept | No packages installed; mypy pin change is a version constraint update only, not a new dependency. No slopcheck required. |

**Security assessment:** This plan is annotation-only. It adds a new source file (`_odoo_types.py`)
containing only `TypedDict` class definitions — no executable logic, no I/O, no new trust boundary,
no external input handling. The pyproject.toml change tightens the mypy version pin and replaces
`ignore_missing_imports` with `follow_untyped_imports`; neither change introduces a new attack
surface. ASVS L1 controls are not applicable to this phase. No in-scope high-severity threats exist.
</threat_model>

<verification>
After all three tasks complete:
- `uv run mypy odoo_fast_report_mapper/` → 0 errors (strict mode active, 6 modules gated)
- `uv run pytest` → 347 tests pass
- `grep "strict = true" pyproject.toml` → found
- `grep "follow_untyped_imports" pyproject.toml` → found
- `grep "ignore_missing_imports" pyproject.toml` → NOT found
- `grep "mypy>=1.18" pyproject.toml` → found
- `python -c "from odoo_fast_report_mapper._odoo_types import IrModelRecord"` → no ImportError
</verification>

<success_criteria>
1. mypy strict mode enabled in pyproject.toml — `strict = true` present, no `ignore_missing_imports`
2. odoorpc_toolbox.* override uses `follow_untyped_imports = true` (per D-03)
3. 6 pending modules have `ignore_errors = true` override blocks in pyproject.toml
   (_yaml_dumper and _progress do NOT — they are already clean after this plan)
4. `_odoo_types.py` exists with all 4 TypedDicts importable
5. `from __future__ import annotations` in all `_*.py` modules
6. `uv run mypy odoo_fast_report_mapper/` exits 0
7. `uv run pytest` exits 0 (347 tests pass)
</success_criteria>

<output>
Create `.planning/phases/02-type-safety/02-01-SUMMARY.md` when done.
</output>
