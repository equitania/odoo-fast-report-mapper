---
phase: 02-type-safety
plan: 06
type: execute
wave: 4
depends_on:
  - "02-04"
  - "02-05"
files_modified:
  - odoo_fast_report_mapper/_connection.py
autonomous: true
requirements:
  - TYPE-01
  - TYPE-02

must_haves:
  truths:
    - "_connection.py Part 1 (lines 1–496: class definition + __init__ + login/auth + check methods) has all no-untyped-def errors resolved"
    - "OdooConnection.__init__ is fully annotated with concrete parameter types"
    - "check_dependencies returns tuple[bool, list[str]] (per Phase 1.1 BUG-03 fix)"
    - "uv run pytest exits 0 (347 tests pass)"
    - "The _connection override block remains in pyproject.toml (Plan 02-07 will remove it)"
  artifacts:
    - path: "odoo_fast_report_mapper/_connection.py"
      provides: "Partially annotated _connection.py — Part 1 complete"
      contains: "def __init__"
  key_links:
    - from: "_connection.py __init__"
      to: "_utils.py prepare_connection"
      via: "OdooConnection is instantiated from prepare_connection result dict"
      pattern: "OdooConnection("
---

<objective>
Annotate `_connection.py` Part 1 (lines 1–496, approximately): class definition, `__init__`,
login and authentication methods, dependency-checking methods. 64 errors total split over two plans;
this plan targets the ~30 `no-untyped-def` errors in the first half.

The `_connection` override block stays in pyproject.toml throughout this plan — it is removed only
in Plan 02-07 when the full file is clean.

Wave 4 — depends on 02-04 (_report.py typed so OdooConnection → Report imports are resolved) and
02-05 (_utils.py typed so factory functions are resolved).

Output: _connection.py Part 1 annotated; override block kept; pytest green.
</objective>

<execution_context>
@/Users/picard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/picard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-type-safety/02-CONTEXT.md
@.planning/phases/02-type-safety/02-RESEARCH.md
@.planning/phases/02-type-safety/02-PATTERNS.md
@.planning/phases/02-type-safety/02-04-SUMMARY.md
@.planning/phases/02-type-safety/02-05-SUMMARY.md
</context>

<interfaces>
From odoo_fast_report_mapper/_odoo_types.py (created in Plan 02-01):
  IrModelRecord: TypedDict — id: int, model: str, name: str
  IrModelFieldsRecord: TypedDict (total=False) — id: int, name: str, ttype: str, modules: str | Literal[False]
  ReportAction: TypedDict — id: int, report_type: str, model: str, name: str, ids: list[int]
  LanguageRecord: TypedDict — code: str, name: str

From odoo_fast_report_mapper/_report.py (annotated in Plan 02-04):
  Report.__init__(self, entry_name: dict[str, str], report_name: str, ..., company_id: int | Literal[False], ...) -> None

From odoo_fast_report_mapper/_utils.py (annotated in Plan 02-05):
  prepare_connection(server_config: dict[str, Any]) -> dict[str, Any]
  collect_yaml_files(path: str) -> list[dict[str, Any]]
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Annotate _connection.py Part 1 — class attrs, __init__, login/auth, check_dependencies</name>
  <files>odoo_fast_report_mapper/_connection.py</files>
  <read_first>
    - odoo_fast_report_mapper/_connection.py (full file — all 873 LOC; read before any edits)
    - .planning/phases/02-type-safety/02-PATTERNS.md §`_connection.py` (full __init__ target signature, established partial annotation, method return type table)
    - .planning/phases/02-type-safety/02-RESEARCH.md §Error Code Breakdown (union-attr, var-annotated, no-untyped-def patterns)
    - .planning/phases/02-type-safety/02-RESEARCH.md §Fragile Areas §2 (_search_report_v13 signature)
    - .planning/phases/02-type-safety/02-RESEARCH.md §Fragile Areas §3 (_connection.py:560-561 data_dictionary)
    - odoo_fast_report_mapper/_odoo_types.py (TypedDict names available for import)
  </read_first>
  <behavior>
    - `__init__` is fully annotated: `language: str, collect_yaml: bool, disable_qweb: bool, workflow: int, url: str, port: int, username: str, password: str, database: str, auth_method: str = "password"` → `-> None`
    - Class-level instance attribute annotations added if needed (e.g., `self.language: str` etc. inferred from __init__ assignments — mypy usually infers these from typed __init__, but explicit class-level annotations may be needed if assigned outside __init__)
    - `check_dependencies` return type: `tuple[bool, list[str]]` (per Phase 1.1 BUG-03 fix; confirm actual return type in file)
    - Login/auth methods (e.g., `login`, `prepare_connection` call, `create_connection`): `-> None` returns; `ODOO` param typed as `Any` (Tier 2 — odoorpc proxy, no typed class available)
    - `_search_report_v13` annotated (fragile area — annotate only, no refactor):
        `model_name: str, report_name: dict[str, str], IR_ACTIONS_REPORT: Any | None = None, company_id: int | None = None` → `int | Literal[False]`
        Add `# Any: odoorpc proxy object, no typed class` on the IR_ACTIONS_REPORT param
    - `from __future__ import annotations` already present (Plan 02-01); verify, do not duplicate
    - Import: `from ._odoo_types import IrModelRecord, IrModelFieldsRecord, ReportAction, LanguageRecord`
    - Import: `from typing import Any, Literal, cast`
    - Scope of THIS plan: lines 1–496 (class definition through the dependency-check methods). Lines 497–873 are Plan 02-07.
    - The `_connection` [[tool.mypy.overrides]] block stays in pyproject.toml — do NOT remove it in this plan.
    - After this plan's edits: `uv run mypy odoo_fast_report_mapper/` still exits 0 (override blocks gate the remaining errors in Part 2)
    - After this plan: `uv run pytest` → 347 pass
  </behavior>
  <action>
    Read _connection.py in full (873 LOC). Run `uv run mypy odoo_fast_report_mapper/_connection.py --strict 2>&1`
    to get all 64 error lines (override block is still present — this dry-run runs with the module
    ungated temporarily; restore the override in pyproject.toml after the dry-run if you removed it,
    OR simply use the error list from 02-RESEARCH.md which has the full breakdown).

    Identify the split point: line ~496 is the seam between setup/auth methods and mapping/collection
    methods (per RESEARCH.md recommendation). Confirm this by reading the method list up to line 496.

    PHASE 1 EDITS (this plan — Part 1, lines 1–496):

    1. Add imports at the top of the file (after existing imports):
       `from ._odoo_types import IrModelRecord, IrModelFieldsRecord, ReportAction, LanguageRecord`
       `from typing import Any, Literal, cast`
       Only add what is needed for Part 1 methods; Part 2 uses will be added in 02-07 if needed.

    2. Annotate `__init__` — all bare params using the signature in the behavior block above.

    3. Annotate `check_dependencies` return type: `tuple[bool, list[str]]` (verify BUG-03 fix is
       already in the file from Phase 1.1).

    4. Annotate login/auth methods: `login`, any `connect_to_odoo` style methods.
       For methods that take odoorpc proxy objects: type them as `Any` with `# Any: odoorpc proxy`.

    5. Annotate `_search_report_v13` (fragile — annotate only, identical to behavior block above).

    6. Annotate any other methods in lines 1–496 that have `no-untyped-def` errors.

    Do NOT touch lines 497–873 (those are Plan 02-07's scope).
    Do NOT remove the `[[tool.mypy.overrides]]` block for `_connection` from pyproject.toml.

    Run `uv run pytest --tb=short -q` → confirm 347 pass.

    Commit message: `[CHG] type-safety(02-06): annotate _connection.py Part 1 (lines 1-496)`
  </action>
  <verify>
    <automated>
      uv run mypy odoo_fast_report_mapper/ 2>&1 | grep "error:" | wc -l | grep -q "^0$" && echo "PASS: package 0 errors (override still active)" || echo "FAIL"
      grep "odoo_fast_report_mapper._connection" pyproject.toml | grep "ignore_errors" | grep -q "true" && echo "PASS: override still in place for Part 2" || echo "FAIL: override removed too early"
      grep "auth_method: str" odoo_fast_report_mapper/_connection.py | grep -q "str" && echo "PASS: __init__ annotated" || echo "FAIL"
      grep "tuple\[bool" odoo_fast_report_mapper/_connection.py | grep -q "check_dependencies\|tuple" && echo "PASS: check_dependencies return type found" || echo "FAIL"
      uv run pytest --tb=short -q 2>&1 | tail -3
    </automated>
  </verify>
  <done>
    `_connection.py` Part 1 (lines 1–496) is annotated — `__init__` fully typed, `check_dependencies`
    returns `tuple[bool, list[str]]`, `_search_report_v13` annotated per fragile-area rule;
    `_connection` override block remains in pyproject.toml (not removed);
    `uv run mypy odoo_fast_report_mapper/` exits 0; 347 tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None introduced | Annotation-only changes to _connection.py |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06-SC | Tampering | No package installs in this plan | accept | No new dependencies. |
| T-02-06-01 | Spoofing | OdooConnection.__init__ param types | accept | Annotating param types does not change runtime validation; inputs are still caller-supplied. Type annotations document the contract but do not enforce it at runtime. |

**Security assessment:** This plan annotates _connection.py Part 1. The `check_dependencies`
return type annotation of `tuple[bool, list[str]]` matches the Phase 1.1 BUG-03 fix already in
place — it is a documentation of existing behavior, not a behavior change. The `Any` annotation
for odoorpc proxy objects (IR_ACTIONS_REPORT, ODOO) is Tier 2 per D-06. No new I/O, no new trust
boundary, no new attack surface. No in-scope high-severity threats.
</threat_model>

<verification>
- `uv run mypy odoo_fast_report_mapper/` → 0 errors (override still active for _connection)
- `grep "odoo_fast_report_mapper._connection" pyproject.toml | grep "ignore_errors"` → found
- `grep "auth_method: str" odoo_fast_report_mapper/_connection.py` → found in __init__ signature
- `grep "tuple\[bool" odoo_fast_report_mapper/_connection.py` → found
- `uv run pytest` → 347 passed
</verification>

<success_criteria>
1. _connection.py lines 1–496: all `no-untyped-def` errors in that range annotated
2. `__init__` fully annotated with 10 typed params + `-> None`
3. `check_dependencies` return type is `tuple[bool, list[str]]`
4. `_search_report_v13` annotated (fragile area — annotate only, no refactor)
5. `_connection` override block still in pyproject.toml (NOT removed — Plan 02-07 does that)
6. `uv run mypy odoo_fast_report_mapper/` → 0 errors (override active)
7. `uv run pytest` → 347 passed
</success_criteria>

<output>
Create `.planning/phases/02-type-safety/02-06-SUMMARY.md` when done.
</output>
