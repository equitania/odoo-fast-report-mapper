---
phase: "04-release-preparation"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - odoo_fast_report_mapper/__version__.py
  - pyproject.toml
  - MANIFEST.in
  - IMPROVEMENT_PLAN.md
  - REVIEW.md
  - TASK_TRACKING.md
autonomous: true
requirements:
  - DOCS-03
  - GATE-05
  - CI-01

must_haves:
  truths:
    - "D-11: Package version is 1.0.0 everywhere it is referenced"
    - "D-12: Version bump happens in Wave 1 — before the Captain review checkpoint (GATE-04) in plan 04-05"
    - "D-14: MANIFEST.in ships MIGRATION.md and omits CLAUDE.md in the sdist"
    - "D-03: pyproject.toml [project.urls] contains a Migration Guide link and MIGRATION.md ships in the sdist"
    - "pyproject.toml classifiers reflect Production/Stable and Python 3.14"
    - "D-13: Legacy pre-GSD planning files (IMPROVEMENT_PLAN.md, REVIEW.md, TASK_TRACKING.md) are removed from the working tree via git rm"
    - "uv build produces a wheel and sdist without errors"
  artifacts:
    - path: "odoo_fast_report_mapper/__version__.py"
      provides: "Single-source version string"
      contains: '__version__ = "1.0.0"'
    - path: "pyproject.toml"
      provides: "Package metadata"
      contains: "Development Status :: 5 - Production/Stable"
    - path: "MANIFEST.in"
      provides: "sdist contents rule"
      contains: "include MIGRATION.md"
  key_links:
    - from: "pyproject.toml"
      to: "odoo_fast_report_mapper/__version__.py"
      via: "dynamic version attr"
      pattern: 'attr = "odoo_fast_report_mapper.__version__.__version__"'
---

<objective>
Bump the package version to 1.0.0, update packaging metadata (pyproject.toml classifiers, MANIFEST.in sdist inclusions), and remove three legacy pre-GSD planning files that are superseded by .planning/ and RELEASE_NOTES.

Purpose: Establishes the correct version identity for the v1.0 release and cleans obsolete repo-root files before documentation is written. All subsequent plans reference version 1.0.0 consistently.

Output: __version__.py at 1.0.0, pyproject.toml with Production/Stable + Python 3.14 classifier + Migration Guide URL, MANIFEST.in without CLAUDE.md + with MIGRATION.md, three git-rm'd legacy files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-release-preparation/04-CONTEXT.md
@.planning/phases/04-release-preparation/04-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Bump version to 1.0.0 and update pyproject.toml metadata</name>
  <files>odoo_fast_report_mapper/__version__.py, pyproject.toml</files>
  <read_first>
    - odoo_fast_report_mapper/__version__.py (current version string and __version_info__ tuple pattern)
    - pyproject.toml (full file: classifiers list, [project.urls] block, [tool.mypy] section)
  </read_first>
  <action>
    In __version__.py: change __version__ = "0.9.7.4" to __version__ = "1.0.0". The __version_info__ line is unchanged — tuple(int(x) for x in __version__.split(".")) continues to produce (1, 0, 0) correctly. No other changes to this file.

    In pyproject.toml, make three independent changes:
    1. Classifiers: replace "Development Status :: 4 - Beta" with "Development Status :: 5 - Production/Stable". Add "Programming Language :: Python :: 3.14" as a new entry after the existing "Programming Language :: Python :: 3.13" line (per D-07 Python 3.14 is stable).
    2. [project.urls]: add a new entry "Migration Guide" = "https://github.com/equitania/odoo-fast-report-mapper/blob/main/MIGRATION.md" after the Documentation line (per D-03).
    3. [tool.mypy] overrides: if a [[tool.mypy.overrides]] block targeting "odoo_report_helper.*" exists, remove it — that package no longer exists. The "odoorpc_toolbox.*" override (ignore_missing_imports = true) stays.

    Do NOT change the version field in pyproject.toml itself — it is set via dynamic attr from __version__.py.
  </action>
  <verify>
    <automated>cd /Users/picard/gitbase/PyPi-Projects/odoo-fast-report-mapper && uv run python -c "from odoo_fast_report_mapper import __version__; assert __version__ == '1.0.0', f'Got {__version__}'; from odoo_fast_report_mapper.__version__ import __version_info__; assert __version_info__ == (1,0,0)"</automated>
  </verify>
  <acceptance_criteria>
    - __version__.py line 12: __version__ = "1.0.0"
    - __version_info__ evaluates to (1, 0, 0) at runtime
    - pyproject.toml contains "Development Status :: 5 - Production/Stable" (not 4 - Beta)
    - pyproject.toml contains "Programming Language :: Python :: 3.14"
    - pyproject.toml [project.urls] contains "Migration Guide" key
    - No [[tool.mypy.overrides]] block for "odoo_report_helper.*" in pyproject.toml
  </acceptance_criteria>
  <done>uv run python -c "from odoo_fast_report_mapper import __version__; assert __version__ == '1.0.0'" exits 0.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fix MANIFEST.in and remove legacy planning files</name>
  <files>MANIFEST.in, IMPROVEMENT_PLAN.md, REVIEW.md, TASK_TRACKING.md</files>
  <read_first>
    - MANIFEST.in (current 5-line content: README.md, LICENSE.txt, CLAUDE.md, yaml_examples, prune)
    - IMPROVEMENT_PLAN.md (confirm file exists before git rm)
    - REVIEW.md (confirm file exists before git rm)
    - TASK_TRACKING.md (confirm file exists before git rm)
  </read_first>
  <action>
    MANIFEST.in: replace "include CLAUDE.md" (line 3) with "include MIGRATION.md" (per D-14, D-03). The rest of the file stays identical. Final 5 lines: include README.md / include LICENSE.txt / include MIGRATION.md / recursive-include yaml_examples *.yaml / prune *.egg-info.

    Legacy file removal (per D-13 — user-approved deletion via CONTEXT.md): run git rm on the three legacy pre-GSD planning files at the repo root:
      git rm IMPROVEMENT_PLAN.md REVIEW.md TASK_TRACKING.md

    This is an explicit user-approved action (CONTEXT.md D-13). The files are superseded by .planning/ and RELEASE_NOTES.md; their content is preserved in git history.

    After git rm, verify the files are staged for removal (git status shows "deleted:").

    Do NOT run uv build yet — that is a GATE-05 verification step reserved for Plan 05 so the Captain can review MIGRATION.md first.
  </action>
  <verify>
    <automated>cd /Users/picard/gitbase/PyPi-Projects/odoo-fast-report-mapper && grep "include MIGRATION.md" MANIFEST.in && grep -v "CLAUDE.md" MANIFEST.in | grep -c "include" | grep -q "2" && echo "MANIFEST OK"</automated>
  </verify>
  <acceptance_criteria>
    - MANIFEST.in line 3: "include MIGRATION.md" (not CLAUDE.md)
    - MANIFEST.in does not contain "CLAUDE.md"
    - MANIFEST.in still contains "include README.md", "include LICENSE.txt", "recursive-include yaml_examples *.yaml", "prune *.egg-info"
    - git status shows IMPROVEMENT_PLAN.md, REVIEW.md, TASK_TRACKING.md as "deleted:" (staged)
    - The three files no longer exist in the working tree
  </acceptance_criteria>
  <done>MANIFEST.in references MIGRATION.md, three legacy files are staged for deletion via git rm.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pyproject.toml → sdist | Controls which files ship to PyPI users — wrong inclusions leak internal files |
| __version__.py → published metadata | Version string is single source of truth for PyPI; wrong value mislabels the release |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | MANIFEST.in | mitigate | Remove CLAUDE.md from sdist — internal developer instructions must not ship to PyPI users (D-14) |
| T-04-02 | Tampering | pyproject.toml classifiers | accept | Classifier values are informational metadata; incorrect value causes cosmetic PyPI mislabeling, not a security issue |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan — only file edits and git rm |
</threat_model>

<verification>
1. `uv run python -c "from odoo_fast_report_mapper import __version__; assert __version__ == '1.0.0'"` exits 0
2. `grep "1.0.0" odoo_fast_report_mapper/__version__.py` returns the version line
3. `grep "Production/Stable" pyproject.toml` returns the classifier line
4. `grep "Migration Guide" pyproject.toml` returns the URL entry
5. `grep "CLAUDE.md" MANIFEST.in` returns no output
6. `grep "MIGRATION.md" MANIFEST.in` returns the include line
7. `git status` shows three legacy files as "deleted:"
</verification>

<success_criteria>
- Package version is 1.0.0 (single-sourced from __version__.py)
- pyproject.toml classifiers: Production/Stable, Python 3.14, Migration Guide URL present
- MANIFEST.in: MIGRATION.md in, CLAUDE.md out
- IMPROVEMENT_PLAN.md, REVIEW.md, TASK_TRACKING.md staged for deletion
</success_criteria>

<output>
Create `.planning/phases/04-release-preparation/04-01-SUMMARY.md` when done
</output>
