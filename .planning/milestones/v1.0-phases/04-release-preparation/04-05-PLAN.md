---
phase: "04-release-preparation"
plan: "05"
type: execute
wave: 3
depends_on:
  - "04-01"
  - "04-02"
  - "04-03"
  - "04-04"
autonomous: false
requirements:
  - GATE-01
  - GATE-02
  - GATE-03
  - GATE-04
  - GATE-05
  - DOCS-04

must_haves:
  truths:
    - "All tests pass (uv run pytest exits 0)"
    - "Ruff check and format are clean"
    - "Mypy --strict exits 0 on odoo_fast_report_mapper/"
    - "uv build produces wheel and sdist without errors"
    - "Captain has reviewed MIGRATION.md and approved it"
    - "D-10: Captain knows the required branch-protection check names for GitHub settings; uv publish stays local and Captain-only"
    - "D-06: .env.example already documents ODOO_API_KEY and its precedence over password (verified — no file change needed, see PATTERNS.md)"
  artifacts:
    - path: "dist/"
      provides: "Built wheel and sdist for v1.0.0"
      contains: "odoo_fast_report_mapper_equitania-1.0.0"
  key_links:
    - from: "dist/ artifacts"
      to: "PyPI"
      via: "uv publish (Captain only — never executed by Claude)"
      pattern: "uv publish"
---

<objective>
Run all release-quality gates (GATE-01..GATE-05): verify tests, ruff, mypy, and uv build pass cleanly; present MIGRATION.md to the Captain for review (GATE-04 manual checkpoint); document the required GitHub branch-protection check names so the Captain can configure them.

Purpose: GATE-04 requires a human review of MIGRATION.md before the release proceeds. GATE-05 requires the Captain to run uv publish. This plan verifies everything is ready and hands off to the Captain.

Output: All automated gates confirmed passing; Captain-reviewed MIGRATION.md; Captain has the information needed to configure branch protection and run uv publish.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-release-preparation/04-CONTEXT.md
@.planning/phases/04-release-preparation/04-01-SUMMARY.md
@.planning/phases/04-release-preparation/04-02-SUMMARY.md
@.planning/phases/04-release-preparation/04-03-SUMMARY.md
@.planning/phases/04-release-preparation/04-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Run all automated release-quality gates</name>
  <files></files>
  <read_first>
    - odoo_fast_report_mapper/__version__.py (confirm version is 1.0.0 before running gates)
    - MANIFEST.in (confirm MIGRATION.md is listed, CLAUDE.md is absent)
  </read_first>
  <action>
    Run each gate sequentially and report the result. All must pass before the checkpoint task.

    Gate sequence (GATE-01 through GATE-05 automated portion):

    GATE-01 (tests): uv run pytest --tb=short 2>&1 | tail -5
    Expected: "passed" with exit code 0. If any failures, stop and report.

    GATE-02 (ruff): uv run ruff check . && uv run ruff format --check .
    Expected: both commands exit 0 with no output (clean). If failures, stop and report.

    GATE-03 (mypy): uv run mypy odoo_fast_report_mapper/ --strict
    Expected: "Success: no issues found" with exit code 0. If errors, stop and report.

    GATE-05 partial (uv build): uv build
    Expected: exits 0, produces dist/odoo_fast_report_mapper_equitania-1.0.0-*.whl and dist/odoo_fast_report_mapper_equitania-1.0.0.tar.gz. Check with: ls dist/ | grep "1.0.0"

    Also verify MANIFEST.in correctness: confirm the built sdist tar.gz contains MIGRATION.md and does NOT contain CLAUDE.md:
      tar -tzf dist/odoo_fast_report_mapper_equitania-1.0.0.tar.gz | grep -E "(MIGRATION|CLAUDE)" 

    Report all gate results clearly before the checkpoint.

    Do NOT run uv publish — that is exclusively the Captain's action (GATE-05 policy, CONTEXT.md D-13).
  </action>
  <verify>
    <automated>cd /Users/picard/gitbase/PyPi-Projects/odoo-fast-report-mapper && uv run pytest --tb=short -q 2>&1 | tail -3 && uv run ruff check . && uv run ruff format --check . && uv run mypy odoo_fast_report_mapper/ --strict 2>&1 | tail -2 && uv build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - uv run pytest exits 0 (all tests pass)
    - uv run ruff check . exits 0 (no lint errors)
    - uv run ruff format --check . exits 0 (no format issues)
    - uv run mypy odoo_fast_report_mapper/ --strict exits 0 with "Success: no issues found"
    - uv build exits 0 and produces files matching dist/*1.0.0*.whl and dist/*1.0.0*.tar.gz
    - tar -tzf dist/*1.0.0*.tar.gz | grep "MIGRATION.md" returns at least one line
    - tar -tzf dist/*1.0.0*.tar.gz | grep "CLAUDE.md" returns no output
  </acceptance_criteria>
  <done>All automated quality gates pass: tests, ruff, mypy --strict, uv build with correct sdist contents.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    All automated release-quality gates passed (GATE-01 tests, GATE-02 ruff, GATE-03 mypy --strict, GATE-05 partial uv build). The built sdist contains MIGRATION.md and omits CLAUDE.md.

    MIGRATION.md is now ready for your review (GATE-04). This is the bilingual DE/EN migration guide for users upgrading from pre-1.0 versions.
  </what-built>
  <how-to-verify>
    1. Review MIGRATION.md at the repo root:
       - Does it cover all import-path changes from odoo_report_helper.* to odoo_fast_report_mapper.*?
       - Are the before/after code snippets for ProgressBar, ReportProgress, create_progress_bar correct?
       - Are the Phase 1.1 behavior changes documented (ValueError, api_key precedence)?
       - Is the German typography correct: „…" quotation pairs (not "…") in German prose?
       - Is the tqdm direct-usage example accurate?

    2. Optionally verify the built wheel:
       ls dist/   (should show 1.0.0 wheel and tar.gz)

    3. Branch protection check names for GitHub settings (Captain sets these in repo Settings → Branches → main and develop):
       Required CI check names from .github/workflows/test.yml:
         - "test (3.12)"
         - "test (3.13)"
         - "test (3.14)"
         - "perf"
         - "build"
       These are the names GitHub will show in the branch protection rule "Require status checks to pass before merging" list.

    4. When ready to publish: run "uv publish" from your Mac locally (never automated in CI per GATE-05 policy).
  </how-to-verify>
  <resume-signal>Type "approved" if MIGRATION.md is correct and all gates passed. Describe any issues to fix otherwise.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dist/ wheel/sdist → PyPI | The built artifacts are the final published package — sdist contents and MANIFEST.in correctness matter here |
| Captain-only publish gate | uv publish must never be automated — any CI change that adds uv publish would be a policy violation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09 | Information Disclosure | sdist contents | mitigate | Verify CLAUDE.md absent from sdist via tar -tzf check in Task 1 (D-14) |
| T-04-10 | Tampering | uv publish automation | mitigate | Task 1 explicitly does NOT run uv publish; checkpoint reminds Captain of the manual-only policy |
| T-04-11 | Information Disclosure | branch protection check names | accept | Check names are informational (they match workflow job names); publishing them in the plan is intentional to help Captain configure GitHub settings |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new package installs; uv build only uses already-installed packages |
</threat_model>

<verification>
1. All automated gates exit 0 (reported in Task 1 output)
2. `ls dist/ | grep "1.0.0"` returns wheel and tar.gz entries
3. `tar -tzf dist/*1.0.0*.tar.gz | grep "MIGRATION.md"` returns at least one hit
4. `tar -tzf dist/*1.0.0*.tar.gz | grep "CLAUDE.md"` returns no output
5. Captain types "approved" at the checkpoint
</verification>

<success_criteria>
- GATE-01: all tests pass
- GATE-02: ruff clean
- GATE-03: mypy --strict exits 0
- GATE-04: MIGRATION.md reviewed and approved by Captain
- GATE-05 partial: uv build succeeds, sdist has correct contents; Captain runs uv publish manually
- Captain has the GitHub branch-protection check names to configure repo settings
</success_criteria>

<output>
Create `.planning/phases/04-release-preparation/04-05-SUMMARY.md` when done
</output>
