---
phase: 03-performance
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - odoo_fast_report_mapper/_connection.py
  - tests/test_benchmark_rpc.py
autonomous: true
requirements:
  - PERF-02
  - PERF-03
  - PERF-04

must_haves:
  truths:
    - "add_field_to_dictionary no longer accesses self.connection — no IR_MODEL.search or IR_FIELDS.search inside the method"
    - "collect_report_entries resolves field_object.modules in the outer loop and passes modules to add_field_to_dictionary"
    - "RPC_CEILING is set to 0 (inner search calls after fix) in test_benchmark_rpc.py"
    - "test_collect_rpc_call_count asserts mock_ir_model.search.call_count == 0 and mock_ir_fields inner search count == 0"
    - "The pre-fix documentation test is deleted; the regression test that enforces zero inner searches replaces it"
    - "All existing TestAddFieldToDictionary tests still pass with the updated signature"
    - "mypy --strict odoo_fast_report_mapper/ reports 0 errors after signature change"
  artifacts:
    - path: "odoo_fast_report_mapper/_connection.py"
      provides: "Fixed add_field_to_dictionary with modules parameter; fixed collect_report_entries with modules extraction"
      contains: "modules: list[str] | None = None"
    - path: "tests/test_benchmark_rpc.py"
      provides: "Regression test asserting 0 inner IR_MODEL/IR_FIELDS search calls"
      contains: "RPC_CEILING: int = 0"
  key_links:
    - from: "tests/test_benchmark_rpc.py::test_collect_rpc_call_count"
      to: "odoo_fast_report_mapper/_connection.py::add_field_to_dictionary"
      via: "MagicMock call_count assertion on mock_ir_model.search"
      pattern: "mock_ir_model\\.search\\.call_count == 0"
    - from: "odoo_fast_report_mapper/_connection.py::collect_report_entries"
      to: "odoo_fast_report_mapper/_connection.py::add_field_to_dictionary"
      via: "modules parameter passed from field_object.modules extraction"
      pattern: "add_field_to_dictionary.*modules"
---

<objective>
Apply the performance fix: eliminate the 2 extra RPC calls per field from
add_field_to_dictionary by moving dependency resolution into the collect_report_entries
outer loop (where field_object.modules is already available on the browsed object).
Lock the fix with an asserting regression test (RPC_CEILING = 0).

Purpose: PERF-02 — zero extra RPC calls per field. PERF-03 — CI-enforced regression test.
PERF-04 — RPC_CEILING constant set to the measured after-fix value (0 inner search calls).

Output: Modified _connection.py (add_field_to_dictionary gains optional modules parameter,
collect_report_entries extracts modules before calling it); updated test_benchmark_rpc.py
(RPC_CEILING = 0, asserting test replaces the documentation test).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-performance/03-RESEARCH.md
@.planning/phases/03-performance/03-PATTERNS.md
@.planning/phases/03-performance/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix add_field_to_dictionary — add modules param, delete 2 search calls, update collect_report_entries call-site</name>
  <files>odoo_fast_report_mapper/_connection.py</files>

  <read_first>
    - odoo_fast_report_mapper/_connection.py lines 627–731 (collect_report_entries outer loop lines 627–667 and full add_field_to_dictionary lines 688–731 — both must be edited)
    - .planning/phases/03-performance/03-PATTERNS.md (new signature, deletion block, replacement block, modules extraction to insert, call-site update)
    - .planning/phases/03-performance/03-RESEARCH.md §Code Examples and §Common Pitfalls (Pitfall 1: signature change breaks mypy; Pitfall 3: field_object.modules can be False — use `or ""` guard)
  </read_first>

  <behavior>
    - add_field_to_dictionary gains a new trailing optional parameter: modules: list[str] | None = None (after company_id: int | Literal[False]).
    - Inside add_field_to_dictionary, the entire "Collect dependencies" block (lines 708–731) is replaced by: modules_dependencies = modules or []. Then apply the existing dedup logic using modules_dependencies directly (no self.connection access at all).
    - In collect_report_entries, after line 636 (field_name = field_object.name), add two lines: raw_modules = field_object.modules or "" and modules = [m for m in raw_modules.replace(" ", "").split(",") if m].
    - The add_field_to_dictionary call at lines 661–667 gains modules as the final positional argument.
    - No other changes to _connection.py. map_reports and _map_report_fields are NOT touched (they contain legitimate RPC calls per RESEARCH.md anti-patterns).
  </behavior>

  <action>
    Edit odoo_fast_report_mapper/_connection.py in two locations:

    LOCATION 1 — collect_report_entries, outer for-loop body (current lines 627–667):
    After `field_name = field_object.name` (line 636), insert:
      raw_modules = field_object.modules or ""
      modules = [m for m in raw_modules.replace(" ", "").split(",") if m]
    This is the exact guard from RESEARCH.md §Code Examples (post-BUG-08/BUG-09 pattern, verbatim copy).

    Update the add_field_to_dictionary call (current lines 661–667) to pass modules as sixth positional argument after report_company_id.

    LOCATION 2 — add_field_to_dictionary method signature and body (current lines 688–731):
    a) Add `modules: list[str] | None = None,` as the sixth parameter after `company_id: int | Literal[False],`. Add inline comment: `# resolved by caller from field_object.modules; None means no dependency info`.
    b) Delete the entire "Collect dependencies" block starting at the comment `# Collect dependencies` (current line 707) through the final `return data_dictionary` at line 731. This removes: IR_FIELDS, IR_MODEL, model_id, field_id, field_obj, raw_modules, modules_dependencies assignments and all conditional logic that calls self.connection.
    c) Replace with:
       modules_dependencies = modules or []
       if not modules_dependencies:
           return data_dictionary
       if "dependencies" in data_dictionary[report_id]:
           data_dictionary[report_id]["dependencies"].extend(modules_dependencies)
           data_dictionary[report_id]["dependencies"] = list(set(data_dictionary[report_id]["dependencies"]))
       else:
           data_dictionary[report_id]["dependencies"] = list(modules_dependencies)
       return data_dictionary

    Verify after editing: `uv run mypy odoo_fast_report_mapper/` must report 0 errors. The new parameter uses `list[str] | None` union syntax already established by `company_id: int | Literal[False]` on the previous line.

    Note: Existing TestAddFieldToDictionary tests (_setup_field_env, test_adds_field_to_new_entry, etc.) call add_field_to_dictionary without the modules argument — this is valid since the parameter defaults to None. Those tests will continue to pass because `modules or []` gives `[]` when None is passed, which is the same as the current "empty modules" behavior when IR_FIELDS.search returns an empty list.
  </action>

  <verify>
    <automated>cd /Users/picard/gitbase/PyPi-Projects/odoo-fast-report-mapper && uv run pytest tests/test_connection.py -q -m "not integration" 2>&amp;1 | tail -10 && uv run mypy odoo_fast_report_mapper/ 2>&amp;1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - add_field_to_dictionary signature contains `modules: list[str] | None = None` as sixth parameter (grep -n "modules" odoo_fast_report_mapper/_connection.py confirms presence).
    - `self.connection.env["ir.model"]` and `self.connection.env["ir.model.fields"]` do NOT appear inside the add_field_to_dictionary method body (grep confirms absence from that method's scope).
    - `field_object.modules or ""` appears in collect_report_entries loop body (grep confirms).
    - The add_field_to_dictionary call in collect_report_entries passes modules as the sixth argument.
    - All 347+ tests in tests/test_connection.py pass: uv run pytest tests/test_connection.py -q -m "not integration" exits 0.
    - uv run mypy odoo_fast_report_mapper/ reports "Success: no issues found" (0 errors).
  </acceptance_criteria>

  <done>
    _connection.py is modified: add_field_to_dictionary takes modules param, has zero self.connection access, and collect_report_entries extracts and passes modules. All existing tests pass. mypy 0 errors.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update test_benchmark_rpc.py — set RPC_CEILING=0, replace doc-test with asserting regression test</name>
  <files>tests/test_benchmark_rpc.py</files>

  <read_first>
    - tests/test_benchmark_rpc.py (current state from plan 03-01 — read entire file before editing)
    - .planning/phases/03-performance/03-RESEARCH.md §Pattern 1 (Mock-Counter assertion pattern), §Pitfall 2 (measuring the wrong search calls), §Open Questions Q1 (ceiling value = outer legitimate searches only after fix)
    - .planning/phases/03-performance/03-PATTERNS.md (Mock-Counter assertion pattern, ir.module.module must-not-be-queried guard, RPC_CEILING placement)
  </read_first>

  <behavior>
    - RPC_CEILING: int changes from -1 to 0. Comment updated to: "# After fix: add_field_to_dictionary makes 0 IR_MODEL.search + 0 IR_FIELDS.search calls per field. Outer legitimate calls (IR_ACTIONS_REPORT.search + IR_MODEL_FIELDS.search outer) are not counted here."
    - test_add_field_to_dictionary_calls_ir_model_search_currently is DELETED (it documented the BEFORE state; the fix has been applied).
    - test_rpc_baseline_call_count is RENAMED to test_collect_rpc_call_count and upgraded: the two print() lines are replaced with assertions. Specifically: assert mock_ir_model.search.call_count == 0 (IR_MODEL is only used inside add_field_to_dictionary; after fix it is never called). Assertion message: "add_field_to_dictionary must not call ir.model.search after fix — got {n} calls".
    - A second assertion added: the ir.model.fields mock's .search call count equals exactly the number of outer top-level field ID searches (1 per company, i.e. 1 for the single-company test fixture). Because after the fix, no inner IR_FIELDS.search calls happen, the total ir.model.fields.search count must equal 1.
    - test_add_field_to_dictionary_zero_rpc_calls (new test): calls add_field_to_dictionary directly with modules=["sale", "account"] and asserts the result dict contains the correct dependencies — verifying the new parameter is used correctly. Also confirms no env access occurred (mock_ir_model.search.call_count == 0, mock_ir_fields.search.call_count == 0 after setting up the env with those mocks).
  </behavior>

  <action>
    Edit tests/test_benchmark_rpc.py:

    1. Change RPC_CEILING: int = -1 to RPC_CEILING: int = 0 and update the comment per behavior above.

    2. Delete test_add_field_to_dictionary_calls_ir_model_search_currently entirely (this test documented the pre-fix state; it will now FAIL because add_field_to_dictionary no longer calls those search methods).

    3. Rename test_rpc_baseline_call_count to test_collect_rpc_call_count. Remove the two print() lines.
       Ensure the ir.module.module guard from plan 03-01 remains wired in the env_map setup: the MagicMock with side_effect=AssertionError("ir.module.module must not be queried during collect") must stay in place — per PATTERNS.md Shared Patterns. If it was not present in the 03-01 output (check the actual test file), add it now.
       Replace the print() lines with:
       assert mock_ir_model.search.call_count == 0, (
           f"add_field_to_dictionary must not call ir.model.search after fix — "
           f"got {mock_ir_model.search.call_count} calls (RPC_CEILING={RPC_CEILING})"
       )
       assert mock_ir_model_fields.search.call_count == 1, (
           f"ir.model.fields.search must be called exactly once (outer field ID fetch) — "
           f"got {mock_ir_model_fields.search.call_count}"
       )
       The second assertion is valid because after the fix, the only ir.model.fields.search call is the single outer all_report_field_ids search in collect_report_entries (1 call for the 1 company in the fixture).

    4. Add test_add_field_to_dictionary_zero_rpc_calls (new test, no tmp_path needed):
       - conn = _make_connection()
       - Wire mock_ir_model and mock_ir_fields via _setup_env (so they'd be available IF add_field_to_dictionary tried to use self.connection).
       - Call conn.add_field_to_dictionary({}, 100, "sale.order", "amount_total", False, modules=["sale", "account"]).
       - Assert result == {100: {"sale.order": ["amount_total"], "dependencies": ["sale", "account"]}} (or set-equal for dependencies since set() reorders).
       - Assert mock_ir_model.search.call_count == 0 with message "add_field_to_dictionary must not call ir.model.search — modules param eliminates this RPC call".
       - Assert mock_ir_fields.search.call_count == 0 with message "add_field_to_dictionary must not call ir.model.fields.search — modules param eliminates this RPC call".
       - Docstring: "PERF-02/PERF-03: After fix, add_field_to_dictionary accepts modules param and performs 0 RPC calls."

    Keep all other test functions from plan 03-01 unchanged (test infrastructure helpers).
  </action>

  <verify>
    <automated>cd /Users/picard/gitbase/PyPi-Projects/odoo-fast-report-mapper && uv run pytest tests/test_benchmark_rpc.py -v 2>&amp;1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - RPC_CEILING: int = 0 is in the file (grep -n "RPC_CEILING" tests/test_benchmark_rpc.py shows "= 0").
    - test_collect_rpc_call_count asserts mock_ir_model.search.call_count == 0 (grep -n "call_count == 0" tests/test_benchmark_rpc.py confirms).
    - test_add_field_to_dictionary_zero_rpc_calls exists and passes.
    - test_add_field_to_dictionary_calls_ir_model_search_currently does NOT appear in the file (grep confirms absence).
    - All tests in tests/test_benchmark_rpc.py pass: uv run pytest tests/test_benchmark_rpc.py -v exits 0.
    - Full suite passes: uv run pytest tests/ -q -m "not integration" exits 0.
    - mypy still 0 errors: uv run mypy odoo_fast_report_mapper/ reports "Success: no issues found".
  </acceptance_criteria>

  <done>
    test_benchmark_rpc.py has RPC_CEILING=0, an asserting regression test for the 10×50 collect run (0 inner IR_MODEL.search calls), a direct add_field_to_dictionary test confirming 0 RPC calls with modules param, and no pre-fix documentation test. All tests pass. mypy 0 errors.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| collect_report_entries → add_field_to_dictionary | Internal method call; modules data flows from already-browsed Odoo field object (server-side data already trusted at browse time) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | Information Disclosure | modules parameter passed from field_object.modules | accept | modules string originates from ir.model.fields.modules (trusted Odoo server data, already in scope at browse point); no new external input surface; the `or ""` guard already applied per BUG-08/BUG-09 fix |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed; all dependencies already present (pytest, pytest-mock, mypy — verified in RESEARCH.md Standard Stack) |

**Security note:** This plan is a pure internal refactor — no new user-facing inputs, no authentication changes, no new file I/O, no new endpoints. The modules data was already flowing through the code via a different path (2 extra RPC calls); the fix moves the source of that data from a redundant re-query to the already-browsed field object. ASVS L1 compliance is unaffected. No threat disposition changes from accept.
</threat_model>

<verification>
## Phase Gate (Wave 2 / Phase Complete)

```bash
cd /Users/picard/gitbase/PyPi-Projects/odoo-fast-report-mapper

# Regression test passes with asserting ceiling
uv run pytest tests/test_benchmark_rpc.py -v

# Full suite green (all 347+ tests)
uv run pytest tests/ -q -m "not integration"

# mypy strict clean
uv run mypy odoo_fast_report_mapper/

# RPC_CEILING is 0
grep -n "RPC_CEILING" tests/test_benchmark_rpc.py

# add_field_to_dictionary has no self.connection access
grep -n "self\.connection" odoo_fast_report_mapper/_connection.py | grep -A5 -B5 "add_field"

# modules param present in signature
grep -n "modules" odoo_fast_report_mapper/_connection.py | head -10
```
</verification>

<success_criteria>
Phase 3 is complete when ALL of these are true:

1. add_field_to_dictionary contains no IR_MODEL.search or IR_FIELDS.search calls.
2. collect_report_entries passes modules (extracted from field_object.modules or "") to add_field_to_dictionary.
3. test_collect_rpc_call_count passes with assert mock_ir_model.search.call_count == 0.
4. test_add_field_to_dictionary_zero_rpc_calls passes.
5. RPC_CEILING = 0 in test_benchmark_rpc.py.
6. All 347+ tests pass.
7. mypy odoo_fast_report_mapper/ reports 0 errors.
</success_criteria>

<output>
Create `.planning/phases/03-performance/03-02-SUMMARY.md` when done.
</output>
