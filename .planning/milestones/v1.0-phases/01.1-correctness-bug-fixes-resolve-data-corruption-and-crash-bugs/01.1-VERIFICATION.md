---
phase: 01.1-correctness-bug-fixes-resolve-data-corruption-and-crash-bugs
verified: 2026-05-29T00:00:00Z
status: passed
score: 8/8 must-haves verified
overrides_applied: 0
---

# Phase 1.1: Correctness Bug Fixes — Verification Report

**Phase Goal:** The correctness bugs surfaced by the baseline review are fixed in the consolidated codebase — no unguarded crashes, no silent data corruption, no bypassed dependency checks, and connection/config parsing fails loudly instead of silently mis-handling input. Each fix is locked in by a regression test. All existing tests still pass after the fixes.
**Verified:** 2026-05-29
**Status:** PASSED
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | BUG-01: `add_field_to_dictionary()` guards `IR_MODEL.search()` before indexing `model_id[0]` | VERIFIED | `_connection.py:665`: `if not model_id:` guard + `logger.warning` + `return data_dictionary` before `IR_FIELDS.search` |
| 2 | BUG-02: `self_clean()` no longer destroys calculated-field parameter lists | VERIFIED | `_report.py:141-144`: `add_calculated_fields` assigns directly to `self._calculated_fields[field_name] = content` with no `self_clean` call; comment explains intent |
| 3 | BUG-03: `check_dependencies` return contract is explicit (`-> tuple[bool, list[str]]`) and a missing dependency actually blocks `map_reports()` | VERIFIED | `_connection.py:203`: `def check_dependencies(self, dependencies) -> tuple[bool, list[str]]:`; `_connection.py:289`: caller unpacks correctly; regression test `test_map_reports_skips_report_when_dependency_missing` confirms `ir.actions.report.create` not called when dep missing |
| 4 | BUG-04: `build_name_search_domain({})` raises `ValueError` (no unbounded search) | VERIFIED | `_lang_utils.py:82-83`: `if not name_dict: raise ValueError("Cannot build name search domain from empty name_dict")` as first guard in function |
| 5 | BUG-05: `prepare_connection` strips scheme AND path (`https://host/web` → `host`) | VERIFIED | `_utils.py:95-100`: uses `parsed.hostname` from `urlparse` result; the old `url.replace("https:", "")` + while-loop pattern is absent; test `test_url_with_path_component_strips_path` confirms `host.example.com` (not `host.example.com/web`) reaches `ODOO()` |
| 6 | BUG-06: `Report.__init__` `entry_name` guards against non-dict (TypeError) | VERIFIED | `_report.py:14`: annotation `entry_name: dict[str, str]`; `_report.py:32-35`: `if not isinstance(entry_name, dict) or not entry_name: raise TypeError(...)` as first body statement |
| 7 | BUG-07: `create_odoo_connection_from_yaml_object` honors `api_key` auth | VERIFIED | `_utils.py:282-299`: `if server.get("api_key"):` block sets `credential = server["api_key"]` and `auth_method = "api_key"`; `auth_method=auth_method` passed as keyword arg to `OdooConnection()` |
| 8 | All existing tests still pass — full suite green | VERIFIED | `345 passed in 0.33s` — zero failures, zero errors |

**Score:** 8/8 truths verified

---

### Required Artifacts

| Artifact | Status | Details |
|----------|--------|---------|
| `odoo_fast_report_mapper/_connection.py` | VERIFIED | BUG-01 guard at line 665; BUG-03 annotation at line 203; caller at line 289 correct |
| `odoo_fast_report_mapper/_report.py` | VERIFIED | BUG-02: no `self_clean` in `add_calculated_fields`; BUG-06: `dict[str, str]` annotation + `isinstance` guard |
| `odoo_fast_report_mapper/_lang_utils.py` | VERIFIED | BUG-04: `ValueError` guard at lines 82-83 |
| `odoo_fast_report_mapper/_utils.py` | VERIFIED | BUG-05: `parsed.hostname` at lines 95-100; BUG-07: `api_key` resolution at lines 282-299 |
| `tests/test_connection.py` | VERIFIED | `TestAddFieldToDictionary::test_empty_model_search_skips_and_returns_dict` (BUG-01); `TestCheckDependencies::test_map_reports_skips_report_when_dependency_missing` (BUG-03) |
| `tests/test_report.py` | VERIFIED | `TestAddCalculatedFields::test_calculated_fields_parameter_list_preserved` (BUG-02); `TestReportEntryNameGuard` (3 tests, BUG-06) |
| `tests/test_lang_utils.py` | VERIFIED | `TestBuildNameSearchDomain::test_empty_dict_raises_value_error` uses `pytest.raises(ValueError, match="empty name_dict")` (BUG-04) |
| `tests/test_utils.py` | VERIFIED | `TestPrepareConnection::test_url_with_path_component_strips_path` (BUG-05); `TestCreateConnectionFromYamlObject` (3 tests, BUG-07) |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `add_field_to_dictionary` | `logger.warning` + early return | `if not model_id:` guard at line 665 | WIRED | Guard fires before `IR_FIELDS.search`; field entry already recorded before guard (correct behavior: no crash, no dependency corruption) |
| `add_calculated_fields` | `self._calculated_fields[field_name]` | direct assignment, no `self_clean` | WIRED | Comment at line 143 confirms intentional absence of `self_clean` |
| `check_dependencies` | `map_reports` caller | `dependencies_installed, not_installed_modules = self.check_dependencies(...)` at line 289 | WIRED | Tuple unpacking correct; `return None, False` when dep missing blocks report creation |
| `build_name_search_domain` | `ValueError` raise | `if not name_dict:` as first statement | WIRED | Tested via `pytest.raises(ValueError, match="empty name_dict")` |
| `prepare_connection` | `ODOO(hostname, ...)` | `parsed.hostname` from `urlparse` result | WIRED | Path `/web` in URL stripped by `urlparse`; `hostname` variable used in `ODOO()` call |
| `Report.__init__` | `TypeError` raise | `isinstance(entry_name, dict) or not entry_name` guard | WIRED | Guard is first body statement before any use of `entry_name` |
| `create_odoo_connection_from_yaml_object` | `OdooConnection(auth_method=auth_method)` | `server.get("api_key")` resolution block | WIRED | `auth_method` keyword arg at line 299 |

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| BUG-04: empty dict raises ValueError | `python -c "from odoo_fast_report_mapper._lang_utils import build_name_search_domain; build_name_search_domain({})"` | `ValueError: Cannot build name search domain from empty name_dict` | PASS |
| BUG-06: string entry_name raises TypeError | `python -c "from odoo_fast_report_mapper._report import Report; Report(entry_name='bad', report_name='x', report_type='fast_report', model_name='sale.order', company_id=False)"` | `TypeError: entry_name must be a non-empty dict mapping language codes to names, got str` | PASS |
| Full test suite | `.venv/bin/python -m pytest tests/ -q` | `345 passed in 0.33s` | PASS |

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| BUG-01 | 01.1-01 | `add_field_to_dictionary()` guards `IR_MODEL.search()` before indexing | SATISFIED | Guard at `_connection.py:665`; regression test passes |
| BUG-02 | 01.1-02 | `self_clean()` no longer destroys calculated-field parameter lists | SATISFIED | `self_clean` absent from `add_calculated_fields`; regression test passes |
| BUG-03 | 01.1-03 | `check_dependencies` return contract explicit; missing dep blocks `map_reports` | SATISFIED | `-> tuple[bool, list[str]]` annotation; regression test confirms blocking behavior |
| BUG-04 | 01.1-04 | `build_name_search_domain({})` raises `ValueError` | SATISFIED | Guard at `_lang_utils.py:82`; test updated to `pytest.raises(ValueError)` |
| BUG-05 | 01.1-05 | `prepare_connection` strips scheme AND path | SATISFIED | `parsed.hostname` replaces fragile `str.replace`; regression test passes |
| BUG-06 | 01.1-06 | `Report.__init__` guards against non-dict `entry_name` | SATISFIED | `isinstance` guard + `dict[str, str]` annotation; 3 regression tests pass |
| BUG-07 | 01.1-07 | `create_odoo_connection_from_yaml_object` honors `api_key` auth | SATISFIED | `server.get("api_key")` block; `auth_method` keyword arg; 3 regression tests pass |

**All 7 requirement IDs (BUG-01 through BUG-07) accounted for. No orphaned requirements.**

---

### Anti-Patterns Found

No blockers identified. Specific checks:

- No `TBD`, `FIXME`, or `XXX` markers in modified files.
- No stub patterns (`return null`, `return {}`, empty handlers) in bug-fix paths.
- `add_calculated_fields` contains a deliberate comment explaining the intentional absence of `self_clean` — this is documentation, not a debt marker.
- The `_utils.py:prepare_connection` fallback branch (no-scheme URL) retains while-loop slash stripping — this is correct, it handles a separate input case where `urlparse` returns no hostname. Not a regression.

---

### Human Verification Required

None. All correctness criteria are verifiable programmatically via code inspection and test execution.

---

### Gaps Summary

No gaps. All 8 phase success criteria are VERIFIED against the actual codebase.

---

## Gap-Closure Addendum — 2026-05-29

The Phase 1.1 code review (`01.1-REVIEW.md`) surfaced two correctness gaps adjacent to the BUG-01 fix in `add_field_to_dictionary()`. Both were closed by gap-closure plan `01.1-08`:

- **BUG-08 (CR-02)** — `IR_FIELDS.search()` result now guarded (`if not field_id:` at `_connection.py:669`) before `.browse(...).modules.replace(...)`; an empty field search logs at debug and returns the dictionary instead of crashing with `AttributeError`. Regression test confirms no crash.
- **BUG-09 (CR-03)** — empty/whitespace module names filtered (`[m for m in raw_modules if m]` at `_connection.py:674`) with an early return on empty result; `""` no longer pollutes exported dependency lists. Regression test asserts `""` never appears in `dependencies`.

**Verification:** both guards confirmed present in source; full suite **347 passed, 0 failures** (was 345 + 2 new regression tests). CR-01 (`add_dependencies()` False-default, `_report.py`) was deliberately NOT closed in this gap round — documented in `01.1-REVIEW.md` for future consideration.

---

_Verified: 2026-05-29_
_Verifier: Claude (gsd-verifier; gap-closure addendum verified directly via source + full suite)_
