---
phase: 01.1-correctness-bug-fixes-resolve-data-corruption-and-crash-bugs
plan: 03
subsystem: testing
tags: [python, type-annotation, regression-test, dependency-checking]

# Dependency graph
requires:
  - phase: 01.1-01
    provides: consolidated OdooConnection with check_dependencies returning tuple[bool, list[str]]
provides:
  - explicit -> tuple[bool, list[str]] return annotation on check_dependencies
  - regression test locking the dependency-blocking path in map_reports
affects:
  - Phase 2 (type safety): check_dependencies now mypy-clean with explicit annotation
  - Any caller of check_dependencies: contract is now statically verifiable

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Return-type annotations on public methods for static verifiability"
    - "Regression tests in existing test class (not new class) for contract-locking"

key-files:
  created: []
  modified:
    - odoo_fast_report_mapper/_connection.py
    - tests/test_connection.py

key-decisions:
  - "Annotation only — no logic change to check_dependencies body or caller at line 289 (per CONTEXT.md D-Claude discretion)"
  - "Regression test added to existing TestCheckDependencies class, not a new class"
  - "Test passed immediately (GREEN without RED) because Phase 1.1-01 already fixed the dependency-blocking path — test is a regression guard, not a new fix"

patterns-established:
  - "Regression tests for contract-locking belong in the same class as the unit tests for that method"

requirements-completed:
  - BUG-03

# Metrics
duration: 5min
completed: 2026-05-29
---

# Phase 01.1 Plan 03: check_dependencies Contract Annotation + Regression Test Summary

**check_dependencies() annotated -> tuple[bool, list[str]] and a regression test locks the dependency-blocking path in map_reports() against future contract violations.**

## Performance

- **Duration:** 5 min
- **Started:** 2026-05-29T09:30:00Z
- **Completed:** 2026-05-29T09:35:00Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments
- Added `-> tuple[bool, list[str]]` annotation to `check_dependencies` — future callers cannot accidentally treat the return as a bare bool
- Added `test_map_reports_skips_report_when_dependency_missing` to `TestCheckDependencies` — proves a missing dependency prevents `ir.actions.report.create()` from being called
- All 106 `test_connection.py` tests pass with no regressions

## Task Commits

Each task was committed atomically:

1. **Task 1: Add explicit return annotation to check_dependencies** - `9a837d7` ([FIX])
2. **Task 2: Add map_reports dependency-blocking regression test** - `4b19c8f` ([FIX])

## Files Created/Modified
- `odoo_fast_report_mapper/_connection.py` - Added `-> tuple[bool, list[str]]` to `check_dependencies` signature
- `tests/test_connection.py` - Added `test_map_reports_skips_report_when_dependency_missing` to `TestCheckDependencies`

## Decisions Made
- Annotation only, no logic change — per CONTEXT.md D-Claude discretion note; caller at line 289 already unpacks correctly
- Regression test in existing class — keeps related tests co-located

## Deviations from Plan

None - plan executed exactly as written.

**TDD note:** The regression test passed on first run without any implementation change. This is expected: Phase 1.1-01 already fixed the dependency-blocking logic. The test is a contract-locking regression guard, not a new fix. Per plan objective: "the base-class LSP-violation surface was already eliminated by Phase 1 consolidation; this plan locks the contract statically."

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- BUG-03 formally closed with annotation + regression test
- `check_dependencies` is now mypy-ready for Phase 2 (type safety)
- No blockers

---

## Self-Check: PASSED

- `odoo_fast_report_mapper/_connection.py` exists and contains `-> tuple[bool, list[str]]` ✓
- `tests/test_connection.py` contains `test_map_reports_skips_report_when_dependency_missing` ✓
- Commits `9a837d7` and `4b19c8f` exist in git log ✓
- 106 tests pass ✓

---
*Phase: 01.1-correctness-bug-fixes-resolve-data-corruption-and-crash-bugs*
*Completed: 2026-05-29*
