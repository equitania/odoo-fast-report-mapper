---
phase: 01.1-correctness-bug-fixes-resolve-data-corruption-and-crash-bugs
plan: 02
subsystem: testing
tags: [python, report, yaml, calculated-fields, tdd, bug-fix]

# Dependency graph
requires:
  - phase: 01.1-correctness-bug-fixes-resolve-data-corruption-and-crash-bugs
    provides: "Plan 01 context: BUG-01 fix already executed, project structure stable"

provides:
  - "BUG-02 fixed: add_calculated_fields no longer calls self_clean on _calculated_fields"
  - "Regression test TestAddCalculatedFields::test_calculated_fields_parameter_list_preserved passing"
  - "Corrected test_add_calculated_fields_single to assert correct behavior"

affects:
  - 01.1-03-plan
  - 01.1-06-plan

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Intentional self_clean absence: comment explains why dict-valued containers must not be deduplicated via list(dict.fromkeys())"

key-files:
  created: []
  modified:
    - odoo_fast_report_mapper/_report.py
    - tests/test_report.py

key-decisions:
  - "Removed self_clean from add_calculated_fields only — not from add_fields where it is correct (values are lists)"
  - "Updated existing test_add_calculated_fields_single to assert correct post-fix behavior instead of the buggy behavior it previously documented"
  - "Both TDD tasks committed in a single atomic commit since both files were modified together in the TDD RED→GREEN cycle"

patterns-established:
  - "Do NOT call self_clean on dict-valued containers — self_clean uses dict.fromkeys which iterates keys, destroying nested values"

requirements-completed:
  - BUG-02

# Metrics
duration: 8min
completed: 2026-05-29
---

# Phase 1.1 Plan 02: BUG-02 Summary

**Removed broken self_clean call from add_calculated_fields, preventing silent destruction of {function_name: [params]} parameter lists**

## Performance

- **Duration:** 8 min
- **Started:** 2026-05-29T09:20:00Z
- **Completed:** 2026-05-29T09:28:00Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments
- Removed `self._calculated_fields = self_clean(self._calculated_fields)` from `add_calculated_fields` — `dict.fromkeys()` iterates dict keys only, converting `{"eq_get_payment_terms": ["p1","p2"]}` to `["eq_get_payment_terms"]` and causing downstream `AttributeError` in `set_calculated_fields`
- Added explanatory comment documenting the intentional absence of `self_clean`
- Added regression test `test_calculated_fields_parameter_list_preserved` asserting full dict structure is preserved
- Updated `test_add_calculated_fields_single` to assert correct behavior (no longer documents the buggy behavior)
- All 63 test_report.py tests pass

## Task Commits

Each task was committed atomically:

1. **Task 1+2: Remove broken self_clean call + regression test** - `251ac1d` (fix + test in single TDD cycle)

**Plan metadata:** pending docs commit

_Note: Tasks 1 and 2 were committed together because the TDD RED→GREEN cycle touched both files in the same logical unit._

## Files Created/Modified
- `odoo_fast_report_mapper/_report.py` - Removed `self_clean(self._calculated_fields)` call; added clarifying comment
- `tests/test_report.py` - Added `test_calculated_fields_parameter_list_preserved`; updated `test_add_calculated_fields_single` to assert correct behavior

## Decisions Made
- Committed Tasks 1 and 2 atomically since both files were part of the same TDD RED→GREEN cycle (test update and fix are inseparable)
- `self_clean` import in `_report.py` retained — still needed for `add_fields`
- No changes to `self_clean` function body in `_utils.py`

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Updated existing test asserting buggy behavior**
- **Found during:** Task 1 (TDD RED phase)
- **Issue:** `test_add_calculated_fields_single` (pre-existing test) asserted the buggy behavior `["eq_get_payment_terms"]`. After fixing `_report.py`, this test would have broken, violating the done criteria "all pre-existing tests still pass".
- **Fix:** Updated `test_add_calculated_fields_single` to assert the correct behavior `{"eq_get_payment_terms": ["partner_id.lang", "currency_id"]}` and updated its docstring to describe the correct contract.
- **Files modified:** `tests/test_report.py`
- **Verification:** 63/63 tests pass
- **Committed in:** `251ac1d`

---

**Total deviations:** 1 auto-fixed (Rule 1 - bug in existing test asserting incorrect behavior)
**Impact on plan:** Necessary to maintain a consistent correct test suite. No scope creep.

## Issues Encountered
None - fix was surgical and TDD cycle proceeded cleanly.

## Known Stubs
None.

## Threat Flags
None — no new network endpoints, auth paths, file access patterns, or schema changes introduced.

## Self-Check: PASSED
- `odoo_fast_report_mapper/_report.py`: found, `add_calculated_fields` has no `self_clean` call
- `tests/test_report.py`: found, `TestAddCalculatedFields::test_calculated_fields_parameter_list_preserved` present
- Commit `251ac1d`: verified in git log

## Next Phase Readiness
- BUG-02 resolved; `_report.py` ready for BUG-06 fix (entry_name dict guard) in plan 01.1-06
- `tests/test_report.py` has 63 passing tests; no regressions
- Remaining plans in phase 01.1: BUG-03, BUG-04, BUG-05, BUG-06, BUG-07

---
*Phase: 01.1-correctness-bug-fixes-resolve-data-corruption-and-crash-bugs*
*Completed: 2026-05-29*
